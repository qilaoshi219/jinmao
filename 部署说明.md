# 金茂教材处理系统 — 部署说明

> **适用版本**：jinmao v1.50.2+（后端） / web v0.40.0+（前端）
>
> **适用环境**：Windows / Linux / 宝塔面板（三套部署方案，按需选择其一）
>
> **阅读对象**：需要自行部署本系统的运维人员、站长、开发者
>
> **最后更新**：2026-08-04

---

## 目录

1. [系统简介](#一系统简介)
2. [部署方式总览（先选环境）](#二部署方式总览先选环境)
3. [部署前准备（所有环境通用）](#三部署前准备所有环境通用)
4. [方案 A：Windows 部署](#四方案-awindows-部署)
5. [方案 B：Linux 部署](#五方案-blinux-部署)
6. [方案 C：宝塔面板部署](#六方案-c宝塔面板部署)
7. [升级与回滚](#七升级与回滚)
8. [常见问题排查（FAQ）](#八常见问题排查faq)
9. [部署检查清单](#九部署检查清单)
10. [附录 A：.env 完整字段说明](#附录-aenv-完整字段说明)
11. [附录 B：常用命令速查](#附录-b常用命令速查)

---

## 一、系统简介

### 1.1 功能概述

金茂教材处理系统是一个 **AI 驱动的互动课程生成平台**：

- 用户上传教材文件（支持 **PDF / Markdown / ZIP / RAR / 7Z**），系统自动完成
  **大纲生成 → 章节拆分 → 口播稿扩写 → PPT 生成 → 语音合成 → 字幕生成** 全流水线；
- 内置 **题库系统**：AI 从教材中提取题目（单选/多选/判断/填空/简答），支持
  随机刷题、顺序刷题、错题本、刷题报告、题库市场共享/借用；
- 用户体系：邮箱验证码注册/登录、VIP 等级、余额计费、兑换码兑换；
- 管理员后台：用户管理、兑换码管理、账单/统计、安全事件、计费价格配置。

### 1.2 技术栈

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端 | Vue 3 + Vite 8 + Element Plus + Tailwind CSS 4 | 构建产物输出到 `WEB/dist/`，API 走相对路径 `/api/v1`，可被任意静态服务器托管 |
| 后端 | Node.js ≥ 18 + Express 4 | 单进程服务，默认端口 `8888`（由 `.env` 中 `PORT` 控制） |
| 数据库 | MySQL 8.0（utf8mb4） | 通过 Prisma ORM 访问，首次部署自动建表 |
| 对象存储 | MinIO | 教材原文件 / PPT / 语音 / 字幕等文件存储，桶由后端自动创建 |
| 邮件 | SMTP（163 / QQ 邮箱） | 发送登录验证码（**未配置则用户无法注册/登录**） |
| AI 服务 | DeepSeek / Doc2x / 火山引擎 TTS / Grsai | 见 [3.3 申请 API 密钥](#33-申请-api-密钥申请密钥) |
| 进程守护 | PM2（Linux/宝塔） | 崩溃自动重启、开机自启 |

### 1.3 目录结构

```
jinmao/                        # 解压后的项目根目录
├── node-jinmao/               # 后端项目（Express + Prisma）
│   ├── app.js                 # 后端入口（含启动自检 + 自动迁移）
│   ├── setup.sh               # 宝塔/Linux 一键初始化脚本
│   ├── setup_first_deploy.sh  # 全新数据库首次建库脚本（推荐）
│   ├── ecosystem.config.js    # PM2 进程配置
│   ├── .env.example           # 环境变量模板（复制为 .env 后填写）
│   ├── prisma/                # 数据库模型 + 迁移脚本
│   ├── API/  service/  utils/ # 路由 / 业务 / 工具层
│   ├── admin/                 # 管理员后台页面（由后端直接托管）
│   └── tools/7z/              # 内置 7za.exe（Windows 解压依赖）
├── WEB/                       # 前端项目（Vue3 + Vite）
│   ├── dist/                  # 预构建的前端产物（可直接部署，无需再构建）
│   ├── src/                   # 前端源码
│   └── vite.config.js         # Vite 配置（开发端口 30000）
├── 部署说明.md                # 本文档
├── API文档.md                 # 接口文档
├── 数据库结构.md              # 数据库设计文档
└── start.ps1                  # Windows 一键启动脚本（开发模式）
```

---

## 二、部署方式总览（先选环境）

| 方案 | 适合场景 | 难度 | 环境要求 | 章节 |
|------|---------|------|---------|------|
| **A. Windows** | 个人电脑 / Windows 服务器，本地体验或小规模使用 | ★★☆ | Windows 10/11/Server | [第四章](#四方案-awindows-部署) |
| **B. Linux** | 云服务器（Ubuntu/CentOS 等），正式对外提供服务 | ★★★ | 云服务器 + 域名（可选） | [第五章](#五方案-blinux-部署) |
| **C. 宝塔面板** | 已装/想用宝塔的站长，可视化操作最省心 | ★★☆ | 宝塔面板（Linux） | [第六章](#六方案-c宝塔面板部署) |

> 三套方案的**核心逻辑完全一致**：准备 MySQL 和 MinIO → 配置 `.env` →
> 安装依赖 → 初始化数据库 → 启动后端 → 部署前端。差异只在"环境准备"环节，
> 请完整阅读第三章，再进入对应方案章节。

---

## 三、部署前准备（所有环境通用）

### 3.1 获取发布包

1. 在项目根目录运行打包脚本（需 Windows + PowerShell）：

   ```powershell
   cd 项目根目录
   .\pack.ps1
   ```

2. 脚本生成 `jinmao.zip`（已包含：后端源码、前端源码、**预构建的前端产物 `WEB/dist/`**、
   `部署说明.md`、接口/数据库文档；已排除：`node_modules`、`.env` 等敏感文件、日志、测试数据）。

3. 将 `jinmao.zip` 分发/上传到目标服务器。

> 发布包约 50~150 MB（取决于前端产物）。**包内不含任何敏感凭据**，可放心传输；
> 所有密钥都在部署时通过 `.env` 配置。

### 3.2 依赖服务清单

| 依赖 | 必须 | 用途 | 获取方式 |
|------|:---:|------|---------|
| Node.js ≥ 18 | ✅ | 运行后端、构建前端 | nodejs.org / nvm / 宝塔软件商店 |
| MySQL 8.0 | ✅ | 业务数据库 | mysql.com / 宝塔软件商店 |
| MinIO | ✅ | 文件对象存储 | min.io 下载二进制 / Docker / 宝塔软件商店 |
| SMTP 邮箱 | ✅ | 发送登录验证码 | 163/QQ 邮箱开启 SMTP 授权码 |
| DeepSeek API Key | ✅ | AI 课程生成核心 | platform.deepseek.com |
| Doc2x API Key | ✅ | PDF → Markdown 转换 | doc2x.noear.org |
| 火山引擎 TTS | ⬜ | 语音合成（缺省时语音功能不可用） | console.volcengine.com |
| Grsai 文生图 | ⬜ | 封面/配图（缺省时图片生成不可用） | Grsai 平台 |
| 7-Zip / p7zip | ✅ | 解压 ZIP/RAR/7Z 教材 | Windows 包内置 7za.exe；Linux 需 `apt install p7zip-full unrar` |

> 后端的 `.env` 与 AI Key 缺失时：**`DATABASE_URL`、`JWT_SECRET` 缺失会拒绝启动**；
> AI Key 缺失只影响对应功能，不影响服务启动（启动日志会给出中文警告）。

### 3.3 申请 API 密钥

1. **DeepSeek**（必须）：注册 [DeepSeek 开放平台](https://platform.deepseek.com) → 创建 API Key。
2. **Doc2x**（必须）：注册 [Doc2x](https://doc2x.noear.org) → 创建 API Key（PDF 转 Markdown 按页计费）。
3. **火山引擎 TTS**（可选）：开通火山引擎语音技术 → 创建应用获取 AppID / AccessKey。
4. **Grsai 文生图**（可选）：开通 Grsai 平台 → 获取 API Key。
5. **SMTP 邮箱**（必须）：以 163 邮箱为例：设置 → POP3/SMTP → 开启服务并获取**授权码**（非登录密码）。
   QQ 邮箱同理（设置 → 账户 → 开启 SMTP 服务）。

### 3.4 准备 MySQL 数据库

1. 安装并启动 MySQL 8.0（各环境安装方式见对应章节）。
2. 创建数据库和专用账号（字符集必须为 `utf8mb4`）：

   ```sql
   CREATE DATABASE jinmao DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   CREATE USER 'jinmao'@'localhost' IDENTIFIED BY '换成你的强密码';
   GRANT ALL PRIVILEGES ON jinmao.* TO 'jinmao'@'localhost';
   FLUSH PRIVILEGES;
   ```

   > 也可直接使用 root 账号，但生产环境强烈建议独立账号。
   > 数据库**不需要手动建表**，后端首次启动/初始化脚本会自动完成。

### 3.5 准备 MinIO 对象存储

1. 启动 MinIO 服务（各环境安装方式见对应章节），端口规划：

   | 环境 | MinIO API 端口 | 说明 |
   |------|---------------|------|
   | 单环境 / 测试 | `19000` | 与 `.env.example` 默认值一致 |
   | 生产 | `9000` | 生产环境建议独立部署 |

2. 获取一对 **Access Key / Secret Key**（MinIO 首次启动打印的 `RootUser/RootPassword` 即可）。
3. **桶不需要手动创建** —— 后端上传文件时自动创建（默认桶名 `jinmao`，可在 `.env` 中修改）。

### 3.6 配置 .env（核心步骤）

解压发布包后，进入后端目录，复制模板并编辑：

```bash
# Windows PowerShell
cd node-jinmao
Copy-Item .env.example .env

# Linux / 宝塔终端
cd node-jinmao
cp .env.example .env
vim .env
```

**最少必填 5 项**（其余可先留空，AI 功能会按需提示）：

| 字段 | 示例 | 说明 |
|------|------|------|
| `PORT` | `8888` | 后端监听端口（默认 8888，与前端反代/代理保持一致） |
| `DATABASE_URL` | `mysql://jinmao:你的密码@127.0.0.1:3306/jinmao?charset=utf8mb4` | MySQL 连接串 |
| `JWT_SECRET` | 随机 32 位以上字符串 | Token 签名密钥，**生产环境必须独立生成** |
| `SMTP_HOST/PORT/USER/PASS` | `smtp.163.com` / `465` / 邮箱 / 授权码 | 登录验证码邮件（163/QQ 均可） |
| `DEEPSEEK_API_KEY` | `sk-xxxx` | AI 生成核心 |
| `DOC2X_API_KEY` | `xxxx` | PDF 转换 |

完整字段说明见 [附录 A](#附录-aenv-完整字段说明)。

---

## 四、方案 A：Windows 部署

> 适用：Windows 10/11 或 Windows Server。全程 PowerShell 操作。

### 4.1 安装运行环境

**① Node.js 18+**

- 官网下载 LTS 版：<https://nodejs.org>（18.x 及以上），一路下一步安装。
- 或命令行安装：

  ```powershell
  winget install OpenJS.NodeJS.LTS
  ```

- 验证（新开一个 PowerShell 窗口）：

  ```powershell
  node -v   # 应输出 v18.x 或更高
  npm -v
  ```

**② MySQL 8.0**

1. 下载 MySQL Installer：<https://dev.mysql.com/downloads/installer/> → 选 **MySQL Server 8.0**。
2. 安装时设置 root 密码；安装完成后在 **MySQL Command Line Client** 中执行 [3.4 的建库 SQL](#34-准备-mysql-数据库)。
3. 验证服务：`Win+R` → `services.msc` → 确认 `MySQL80` 服务处于"正在运行"。

**③ MinIO**

1. 下载 Windows 版：<https://dl.min.io/server/minio/release/windows-amd64/minio.exe>，放到 `D:\minio\minio.exe`。
2. 创建数据目录并启动（API 端口 `19000`，控制台 `19001`）：

   ```powershell
   mkdir D:\minio\data
   cd D:\minio
   .\minio.exe server D:\minio\data --address :19000 --console-address :19001
   ```

3. 首次启动会打印 `RootUser: minioadmin` / `RootPassword: minioadmin`（生产请改用
   `setx MINIO_ROOT_USER xxx` 与 `setx MINIO_ROOT_PASSWORD xxx` 后重启）。
4. 保持此窗口运行（MinIO 即对象存储服务），控制台地址：<http://127.0.0.1:19001>。

### 4.2 解压发布包并配置

```powershell
# 解压到 D:\jinmao
Expand-Archive -Path D:\jinmao.zip -DestinationPath D:\jinmao -Force

# 配置 .env
cd D:\jinmao\node-jinmao
Copy-Item .env.example .env
notepad .env   # 按 3.6 填写必填项；MinIO 三项按 3.5 填写
```

### 4.3 安装后端依赖并初始化数据库

```powershell
cd D:\jinmao\node-jinmao

# ① 安装依赖（国内网络较慢可先执行：npm config set registry https://registry.npmmirror.com）
npm install

# ② 生成 Prisma Client
npx prisma generate

# ③ 首次部署建库（全新空库推荐：直接按模型建全表 + 标记历史迁移）
npx prisma db push
Get-ChildItem prisma/migrations -Directory | ForEach-Object {
    npx prisma migrate resolve --applied $_.Name
}

# ④ 验证迁移状态（应显示 up to date）
npx prisma migrate status
```

> 说明：③ 与 Linux 的 `setup_first_deploy.sh` 等价。以后代码升级时，
> `app.js` 启动会自动执行 `prisma migrate deploy` 增量迁移，无需手动处理。

### 4.4 启动后端

**方式一：前台运行（开发调试）**

```powershell
cd D:\jinmao\node-jinmao
node app.js
```

看到如下日志即成功：

```
✅ .env 文件已找到
✅ Prisma Client 已就绪
✅ MySQL 连接成功
  JinMao Server 已启动
  端口: 8888
```

**方式二：项目自带脚本（自动清理端口、装依赖、迁移）**

```powershell
cd D:\jinmao\node-jinmao
.\start.ps1
```

### 4.5 启动前端

**方式一：开发模式（推荐本地体验）**

```powershell
cd D:\jinmao\WEB
npm install
npm run dev
```

浏览器访问 <http://localhost:30000>（`/api` 自动代理到后端 8888）。

> 根目录还有一键脚本 `.\start.ps1`：自动释放 8888/30000 端口、
> 检查依赖并同时启动前后端两个窗口。

**方式二：生产模式（局域网/服务器对外服务）**

使用发布包内**预构建产物**即可，无需重新构建：

```powershell
cd D:\jinmao\WEB
# 用静态服务器托管 dist/（-s 启用 SPA 回退，刷新 /study 等页面不 404）
npx serve -s dist -l 30000
```

浏览器访问 <http://localhost:30000>。

### 4.6 防火墙放行（局域网/服务器场景）

```powershell
# 管理员 PowerShell
netsh advfirewall firewall add rule name="JinMao-Backend-8888" dir=in action=allow protocol=TCP localport=8888
netsh advfirewall firewall add rule name="JinMao-Frontend-30000" dir=in action=allow protocol=TCP localport=30000
```

> 云服务器（阿里云/腾讯云等）还需在**控制台安全组**中放行对应端口。

### 4.7 （可选）开机自启

生产环境建议用 PM2 守护后端：

```powershell
npm install -g pm2
cd D:\jinmao\node-jinmao
pm2 start ecosystem.config.js
pm2 save
pm2 startup   # 按提示执行输出的命令，实现开机自启
```

### 4.8 验证

见 [第九章 部署检查清单](#九部署检查清单)。

---

## 五、方案 B：Linux 部署

> 适用：Ubuntu 20.04/22.04/24.04、Debian、CentOS 7+/OpenCloudOS 等云服务器。
> 以下命令 Ubuntu/Debian 用 `apt`，CentOS 系用 `yum/dnf`（已分别标注）。

### 5.1 安装运行环境

**① Node.js 18+（推荐 nvm，避免系统源版本过旧）**

```bash
# 安装 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
source ~/.bashrc

# 安装 Node 18 LTS 并验证
nvm install 18
nvm alias default 18
node -v && npm -v
```

> 国内服务器可先执行 `export NODEJS_ORG_MIRROR=https://npmmirror.com/mirrors/node` 加速下载。

**② MySQL 8.0**

```bash
# Ubuntu / Debian
apt update && apt install -y mysql-server
systemctl enable --now mysql

# CentOS / OpenCloudOS
yum install -y mysql-server
systemctl enable --now mysqld
```

执行 `mysql -uroot -p` 进入后按 [3.4](#34-准备-mysql-数据库) 建库建账号。

**③ MinIO**

```bash
# 下载二进制（x86_64）
wget https://dl.min.io/server/minio/release/linux-amd64/minio -O /usr/local/bin/minio
chmod +x /usr/local/bin/minio

# 建数据目录，后台启动（API 19000 / 控制台 19001）
mkdir -p /data/minio
export MINIO_ROOT_USER=minioadmin
export MINIO_ROOT_PASSWORD=你的强密码
nohup minio server /data/minio --address :19000 --console-address :19001 \
  > /var/log/minio.log 2>&1 &

# 验证
curl http://127.0.0.1:19000/minio/health/live
```

**④ 解压工具（后端解压 ZIP/RAR/7Z 教材依赖）**

```bash
# Ubuntu / Debian
apt install -y p7zip-full unrar

# CentOS
yum install -y p7zip p7zip-plugins unrar
```

### 5.2 上传并解压发布包

```bash
# 方式一：scp 上传（本地执行）
scp jinmao.zip root@你的服务器IP:/opt/

# 方式二：服务器直接下载（若发布包已放到可下载地址）
cd /opt && wget https://你的下载地址/jinmao.zip

# 解压
cd /opt
unzip jinmao.zip -d jinmao
```

### 5.3 配置 .env

```bash
cd /opt/jinmao/node-jinmao
cp .env.example .env
vim .env        # 按 3.6 填写必填项（数据库、JWT、SMTP、DeepSeek、Doc2x、MinIO）
```

### 5.4 安装依赖并初始化数据库（首次部署）

```bash
cd /opt/jinmao/node-jinmao

# ① 安装依赖（国内服务器建议先：npm config set registry https://registry.npmmirror.com）
npm install

# ② 首次部署一键建库（自动 npm install + 按模型建全表 + 标记历史迁移已应用）
bash setup_first_deploy.sh
```

> 若数据库已存在且是旧版本升级，则跳过 ②，直接：
> `npx prisma generate && npx prisma migrate deploy`

### 5.5 构建前端

发布包已含 `WEB/dist/` 预构建产物（可直接用）；如需从源码重新构建：

```bash
cd /opt/jinmao/WEB
npm install
npm run build     # 产物输出到 dist/
```

### 5.6 PM2 守护后端

```bash
npm install -g pm2

cd /opt/jinmao/node-jinmao
pm2 start ecosystem.config.js      # 项目名 jinmao-backend，端口取 .env 的 PORT
pm2 save
pm2 startup                         # 按提示执行输出的命令，实现开机自启

pm2 status                          # 查看状态
pm2 logs jinmao-backend             # 查看日志
```

### 5.7 Nginx 反向代理（对外提供服务）

安装 Nginx：

```bash
# Ubuntu / Debian
apt install -y nginx

# CentOS
yum install -y nginx
systemctl enable --now nginx
```

新建站点配置 `/etc/nginx/conf.d/jinmao.conf`：

```nginx
server {
    listen 80;
    server_name 你的域名或服务器IP;          # 可留空，用 IP 访问

    # 前端静态文件（预构建产物）
    root /opt/jinmao/WEB/dist;
    index index.html;

    # 大文件上传 + 长耗时 AI 生成请求
    client_max_body_size 500m;
    proxy_read_timeout 900s;

    # API 反向代理到后端 8888
    location /api/ {
        proxy_pass http://127.0.0.1:8888;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 管理员后台反向代理到后端 8888
    location /admin/ {
        proxy_pass http://127.0.0.1:8888;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # SPA 路由回退（重要：刷新 /study /quiz 等页面不 404）
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

重载配置：

```bash
nginx -t && nginx -s reload
```

> HTTPS：推荐用 certbot 一键签发（`apt install certbot python3-certbot-nginx && certbot --nginx`）。

### 5.8 防火墙与安全组

```bash
# Ubuntu（ufw）
ufw allow 80/tcp
ufw allow 443/tcp

# CentOS（firewalld）
firewall-cmd --permanent --add-port=80/tcp
firewall-cmd --permanent --add-port=443/tcp
firewall-cmd --reload
```

> 云服务器还需在**控制台安全组**放行 80/443（及调试用的 8888/30000）。
> 后端 8888 端口**无需对外放行**（只被 Nginx 本机反代），更安全。

### 5.9 验证

见 [第九章 部署检查清单](#九部署检查清单)。

---

## 六、方案 C：宝塔面板部署

> 适用：已安装宝塔面板（Linux）的服务器。宝塔官网：<https://www.bt.cn>
> 本章为**单环境（测试）部署**；测试/生产双环境方案见 [6.9](#69-可选测试生产双环境部署)。

### 6.1 面板安装软件

打开宝塔面板 → **软件商店**，安装：

| 软件 | 用途 | 备注 |
|------|------|------|
| Nginx | Web 服务器 / 反向代理 | 一般已随面板安装 |
| MySQL 8.0 | 数据库 | 安装后记下 root 密码 |
| Node.js 版本管理器 | 提供 Node 18+ | 安装后选 **Node 18 LTS** 并设为默认 |
| PM2 管理器 | 进程守护 | 或使用"网站 → Node项目"内置管理 |

### 6.2 创建数据库

宝塔 → **数据库** → **添加数据库**：

- 数据库名：`jinmao`
- 用户名 / 密码：自行设置（如 `jinmao` / 强密码）
- 字符集：**utf8mb4**
- 访问权限：本地服务器

记录生成的连接信息，稍后填入 `.env` 的 `DATABASE_URL`。

### 6.3 上传并解压发布包

1. 宝塔 → **文件** → 进入 `/www/wwwroot/` → 新建目录 `jinmao` → 进入。
2. 上传 `jinmao.zip` → 右键 **解压**。

解压后结构：

```
/www/wwwroot/jinmao/
├── node-jinmao/        # 后端
├── WEB/                # 前端（含预构建 dist/）
├── 部署说明.md
└── ...
```

### 6.4 一键初始化（SSH / 宝塔终端）

宝塔 → **网站** → 站点终端（或 SSH），执行：

```bash
cd /www/wwwroot/jinmao/node-jinmao
bash setup.sh
```

脚本自动完成：

| 步骤 | 操作 |
|------|------|
| [1/6] | 检查 Node.js 版本（≥18）与 7z 解压工具 |
| [3/6] | 检查 `.env`（不存在则从 `.env.example` 复制模板并**退出让你编辑**） |
| [4/6] | `npm install` → `prisma generate` → `prisma migrate deploy` |
| [5/6] | 前端 `npm install` → `npm run build`（输出到 `WEB/dist/`） |
| [6/6] | 输出宝塔 Node 项目配置参数 |

**首次部署注意**：全新空库建议先建库再初始化，顺序如下：

```bash
cd /www/wwwroot/jinmao/node-jinmao
cp .env.example .env                       # 先编辑 .env 填写真实配置（见 6.4.1）
vim .env
bash setup_first_deploy.sh                 # 安装依赖 + 按模型直接建全表（推荐）
bash setup.sh                              # 补齐前端构建（重复的 npm install 无害）
```

> 若直接 `bash setup.sh` 报 `P3018 表不存在`，说明迁移链无法在全新空库上建表，
> 按上面顺序先执行 `setup_first_deploy.sh` 即可（原因见 FAQ）。

#### 6.4.1 .env 必填项（宝塔版示例）

```ini
# 端口（单环境默认 8888）
PORT=8888

# MySQL 连接（填 6.2 创建的数据库信息）
DATABASE_URL="mysql://jinmao:数据库密码@127.0.0.1:3306/jinmao?charset=utf8mb4"

# JWT 签名密钥（随机字符串，至少 32 位）
JWT_SECRET="替换为随机密钥"

# SMTP（163 邮箱示例，用于发送登录验证码）
SMTP_HOST="smtp.163.com"
SMTP_PORT="465"
SMTP_USER="你的邮箱@163.com"
SMTP_PASS="你的SMTP授权码"
SMTP_FROM="创客工作室 <你的邮箱@163.com>"

# AI 服务
DEEPSEEK_API_KEY=你的deepseek_key
DOC2X_API_KEY=你的doc2x_key

# MinIO（地址填服务器内网 IP，端口 19000，见 6.6）
MINIO_ENDPOINT=127.0.0.1
MINIO_PORT=19000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=你的minio密码
MINIO_BUCKET=jinmao
```

### 6.5 添加 Node 项目（后端）

宝塔 → **网站** → **Node 项目** → **添加 Node 项目**：

| 字段 | 值 |
|------|-----|
| 项目名称 | `jinmao-backend` |
| 启动文件 | `/www/wwwroot/jinmao/node-jinmao/app.js` |
| 运行目录 | `/www/wwwroot/jinmao/node-jinmao` |
| 端口 | `8888` |
| Node 版本 | 自动 |
| 项目类型 | 默认 |

提交后点击 **启动**。状态为"运行中"即成功。

### 6.6 安装 MinIO（宝塔）

**方式一：Docker 安装（推荐）**

宝塔 → **Docker** → 终端执行：

```bash
docker run -d --name minio \
  -p 19000:9000 -p 19001:9001 \
  -e MINIO_ROOT_USER=minioadmin \
  -e MINIO_ROOT_PASSWORD=你的强密码 \
  -v /www/minio/data:/data \
  --restart always \
  minio/minio server /data --console-address ":9001"
```

**方式二：二进制安装**

```bash
wget https://dl.min.io/server/minio/release/linux-amd64/minio -O /usr/local/bin/minio
chmod +x /usr/local/bin/minio
mkdir -p /www/minio/data
nohup minio server /www/minio/data --address :19000 --console-address :19001 \
  > /var/log/minio.log 2>&1 &
```

> 若宝塔软件商店自带 MinIO，也可直接安装使用（注意端口与 `.env` 保持一致）。

### 6.7 添加网站（前端 + 反向代理）

1. 宝塔 → **网站** → **添加站点**：
   - 域名：你的域名（或 IP）
   - 根目录：`/www/wwwroot/jinmao/WEB/dist`
   - PHP 版本：**纯静态**
2. 点击该站点 → **配置文件**，在 `server` 块中追加：

```nginx
# API 反向代理到后端 8888
location /api/ {
    proxy_pass http://127.0.0.1:8888;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    client_max_body_size 500m;
    proxy_read_timeout 900s;
}

# 管理员后台反向代理到后端 8888
location /admin/ {
    proxy_pass http://127.0.0.1:8888;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}
```

3. **SPA 路由回退（重要）**：在 `server` 块中**默认的** `location / { ... }` 块内
   追加一行（不要新增第二个 `location /` 块，否则 Nginx 报 duplicate location）：

```nginx
try_files $uri $uri/ /index.html;
```

4. 保存并重载 Nginx。若站点已开启 SSL（宝塔可一键申请），访问 `https://域名` 即可。

### 6.8 验证

见 [第九章 部署检查清单](#九部署检查清单)。

### 6.9 （可选）测试/生产双环境部署

如需在**同一台服务器**上同时运行测试环境（日常开发）与生产环境（线上用户），
参考项目内 `双环境部署指南.md` 的完整方案，核心参数：

| 维度 | 测试环境 | 生产环境 |
|------|---------|---------|
| 目录 | `/www/wwwroot/jinmao` | `/www/wwwroot/jinmao-prod` |
| 站点端口 | 30080 | 30081 |
| 后端端口 | 8888 | 8889 |
| Node 项目 | `jinmao-backend` | `jinmao-backend-prod` |
| 数据库 | `jinmao` | `jinmao_prod` |
| MinIO 桶 | `jinmao` | `jinmao-prod` |

生产首次部署流程与本章一致，仅需：独立数据库 → 复制目录或全量上传 →
按上表修改 `.env` → `bash setup_first_deploy.sh` → 添加 Node 项目（8889）→
添加站点（30081）→ 反代到 8889。

---

## 七、升级与回滚

### 7.1 升级（以 宝塔 为例，Linux 同理）

1. **备份**（重要）：

   ```bash
   # 数据库备份（宝塔 → 数据库 → 备份 亦可）
   mysqldump -u jinmao -p jinmao > /www/backup/jinmao_$(date +%F).sql
   ```

2. 上传新版 `jinmao.zip` 解压覆盖（**保留**：`.env`、`data/` 目录、`config/admin_config.json`；
   **删除**：`node_modules/`、`WEB/dist/` 旧产物、`.migration_version`、`.schema_hash`）。

3. 同步数据库并重启：

   ```bash
   cd /www/wwwroot/jinmao/node-jinmao
   npm install
   npx prisma generate
   npx prisma migrate deploy      # 增量迁移（app.js 启动时也会自动执行）
   cd ../WEB && npm install && npm run build
   ```

4. 宝塔 → Node 项目 → `jinmao-backend` → **重启**。

> 前端若使用发布包内预构建 `dist/`，第 3 步可跳过 `npm run build`。

### 7.2 回滚

1. 宝塔 → Node 项目 → 停止 `jinmao-backend`；
2. 用旧版本文件覆盖（或重新上传旧包解压）；
3. 若涉及数据库变更，用 7.1 的备份恢复：

   ```bash
   mysql -u jinmao -p jinmao < /www/backup/jinmao_备份日期.sql
   ```

4. 重新启动。

---

## 八、常见问题排查（FAQ）

### 8.1 通用问题

| 现象 | 原因 | 解决 |
|------|------|------|
| 启动报 `.env 文件未找到` | 未复制模板 | `cp .env.example .env` 后填写 |
| 启动报 `DATABASE_URL 未设置` | .env 缺字段 | 补齐 `DATABASE_URL` 后重启 |
| 启动报 `Prisma Client 未生成` | 未执行 prisma generate | `npx prisma generate`（app.js 也会自动尝试） |
| 启动报 `数据库迁移失败，服务器拒绝启动` | 全新空库迁移链无法建表 / MySQL 未启动 / 连接串错误 | 全新库先跑 `setup_first_deploy.sh`；检查 MySQL 状态与连接串 |
| 注册/登录收不到验证码 | SMTP 未配置或授权码错误 | 检查 `.env` 的 SMTP 四项；163/QQ 需用**授权码**而非登录密码 |
| 上传教材后一直"处理中"或失败 | AI Key 缺失 / MinIO 不通 | 看后端日志中对应 API Key 警告；`curl http://127.0.0.1:19000/minio/health/live` 测 MinIO |
| 上传 ZIP/RAR 教材失败 | 7z 未安装 | Windows 包内置 7za.exe；Linux `apt install p7zip-full unrar` |
| 语音/封面生成不可用 | 火山引擎 / Grsai Key 未配置 | 补齐 `.env` 对应字段（不影响其他功能） |

### 8.2 Windows 专属

| 现象 | 解决 |
|------|------|
| 端口 8888/30000 被占用 | `netstat -ano \| findstr 8888` 找到 PID → `taskkill /PID 进程号 /F`；或直接使用 `start.ps1`（自动清理） |
| MySQL 连不上 | `services.msc` 确认 MySQL80 服务已启动；检查连接串密码 |
| 局域网其他电脑访问不了 | 放行防火墙端口（4.6）+ 云安全组；前端用 `npx serve -s dist -l 30000` 生产模式 |
| npm install 超时 | `npm config set registry https://registry.npmmirror.com` |

### 8.3 Linux 专属

| 现象 | 解决 |
|------|------|
| npm install 慢/失败 | 换 npmmirror 镜像（见 5.4）；必要时清缓存 `npm cache clean --force` 后重试 |
| MySQL 8 用 root 远程连不上 | 后端与 MySQL 同机时连接串用 `127.0.0.1`，无需远程权限 |
| Nginx 刷新页面 404 | `location /` 内未加 `try_files $uri $uri/ /index.html;` |
| 上传大文件 413 | `client_max_body_size 500m;` 未配置 |
| PM2 重启后丢失 | 未执行 `pm2 save` + `pm2 startup` |

### 8.4 宝塔专属

| 现象 | 解决 |
|------|------|
| Node 项目启动即退出 | 查看项目日志；多为 `.env` 未配置或数据库未创建 |
| 接口 502 Bad Gateway | 后端进程挂了：Node 项目里点重启；`pm2 logs jinmao-backend` 查原因 |
| `P3018 表不存在` | 全新空库请先 `bash setup_first_deploy.sh`（按模型建全表），再启动 |
| 前端页面 404 | 站点根目录需指向 `WEB/dist`；`location /` 加 `try_files` 回退 |
| 上传文件 413 | 站点配置中已含 `client_max_body_size 500m;`（见 6.7） |
| MinIO 连不上 | 确认 MinIO 容器/进程在跑；`.env` 中 `MINIO_ENDPOINT/PORT` 与启动参数一致 |

---

## 九、部署检查清单

### 9.1 后端

- [ ] `node -v` ≥ 18
- [ ] MySQL 8.0 运行中，数据库 `jinmao`（utf8mb4）已创建
- [ ] MinIO 运行中，`curl http://127.0.0.1:19000/minio/health/live` 返回正常
- [ ] `node-jinmao/.env` 已配置（DATABASE_URL / JWT_SECRET / SMTP / DeepSeek / Doc2x / MinIO）
- [ ] `npm install` 与 `npx prisma generate` 执行成功
- [ ] 首次部署已执行 `setup_first_deploy.sh`（或 `db push` + resolve），`prisma migrate status` 显示 up to date
- [ ] 后端启动无红色错误，日志出现 `JinMao Server 已启动 / 端口: 8888`
- [ ] `curl http://127.0.0.1:8888/api/v1/docs/json` 返回 200

### 9.2 前端

- [ ] `WEB/dist/index.html` 存在（发布包已预构建，或用 `npm run build` 重新生成）
- [ ] 浏览器访问站点（Windows `http://localhost:30000`；Linux/宝塔 `http://IP` 或域名）能打开登录页
- [ ] 直接访问 `/study`、`/quiz` 等路径刷新不 404（SPA 回退已配置）
- [ ] 站点内打开 `http://IP:8888/api/v1/docs`（或 `/api/v1/docs`）能看到 API 文档

### 9.3 业务冒烟测试

- [ ] 邮箱验证码能收到 → 注册/登录成功
- [ ] 上传一份 PDF 教材 → 课程生成流水线跑通（大纲 → 章节 → PPT → 语音 → 字幕）
- [ ] 刷题（随机/顺序）→ 交卷 → 出报告
- [ ] 管理员后台可访问：`http://IP/admin/<安全后缀>`（后缀见后端启动日志"🔐 安全后缀"，
      可在管理员页面"安全"设置中修改）

---

## 附录 A：.env 完整字段说明

以下为 `node-jinmao/.env`（由 `.env.example` 复制）的全部字段：

```ini
# ============ 服务端口 ============
# 单环境/测试 8888；生产 8889（双环境时区分）
PORT=8888

# ============ 数据库连接（Prisma） ============
DATABASE_URL="mysql://用户名:密码@127.0.0.1:3306/jinmao?charset=utf8mb4"

# ============ JWT 配置 ============
# 随机字符串，至少 32 位；生产环境独立生成，且不要随意更换（否则已登录用户全部失效）
JWT_SECRET="your-jwt-secret-change-in-production"
JWT_EXPIRES_IN="7d"

# ============ SMTP 邮箱配置（nodemailer，登录验证码） ============
SMTP_HOST="smtp.163.com"          # 163: smtp.163.com / QQ: smtp.qq.com
SMTP_PORT="465"                   # SSL 端口 465
SMTP_USER="your-email@163.com"    # 发件邮箱
SMTP_PASS="your-smtp-auth-code"   # 授权码（不是登录密码）
SMTP_FROM="创客工作室 <your-email@163.com>"

# ============ DeepSeek API（必须） ============
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# ============ Doc2x API（必须，PDF→Markdown） ============
DOC2X_API_KEY=your_doc2x_api_key_here

# ============ 火山引擎 TTS（可选，语音合成） ============
VOLCENGINE_TTS_APP_ID=your_volcengine_app_id_here
VOLCENGINE_TTS_ACCESS_KEY=your_volcengine_access_key_here

# ============ MinIO 对象存储（必须） ============
MINIO_ENDPOINT=127.0.0.1
MINIO_PORT=19000                  # 生产双环境时用 9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=your_minio_access_key_here
MINIO_SECRET_KEY=your_minio_secret_key_here
MINIO_BUCKET=jinmao               # 桶自动创建；生产双环境时用 jinmao-prod

# ============ Grsai 文生图 API（可选） ============
GRSAI_API_KEY=your_grsai_api_key_here
GRSAI_MODEL=gpt-image-2
```

> 提示：API Key 中不要包含中文/非 ASCII 字符，后端启动时会做格式校验并给出中文提示。

---

## 附录 B：常用命令速查

### 后端（node-jinmao 目录）

```bash
# 安装依赖 / 生成客户端 / 建库
npm install
npx prisma generate
npx prisma db push && npx prisma migrate status

# 启动（前台）
node app.js

# PM2 管理
pm2 start ecosystem.config.js      # 启动
pm2 status                         # 状态
pm2 logs jinmao-backend            # 日志
pm2 restart jinmao-backend         # 重启
pm2 stop jinmao-backend            # 停止
```

### 前端（WEB 目录）

```bash
npm install
npm run dev       # 开发模式（端口 30000）
npm run build     # 生产构建（输出 dist/）
```

### 数据库

```bash
npx prisma migrate deploy          # 应用增量迁移（升级时）
npx prisma migrate status          # 查看迁移状态
```

### 验证接口

```bash
curl http://127.0.0.1:8888/api/v1/docs/json    # API 文档 JSON（200 即后端正常）
```

---

*本文档随发布包分发。部署中遇到问题，先对照 [第八章 FAQ](#八常见问题排查faq) 排查；仍无法解决时，携带后端日志（`pm2 logs jinmao-backend` 或启动窗口输出）联系技术支持。*
