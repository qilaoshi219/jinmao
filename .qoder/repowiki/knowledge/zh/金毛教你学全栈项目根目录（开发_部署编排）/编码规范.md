- 所有 PowerShell 脚本以 $PScriptRoot 为基准路径，再通过相对路径拼接 node-jinmao/ 与 WEB/ 子模块目录。
- 打包与同步均显式排除 node_modules、.backend.log/.frontend.log、.backend.pid/.frontend.pid、jinmao.zip 及 .git 等无关文件。
- WSL 交互统一通过 wsl -u root bash -c "..." 执行，避免 sudo 密码提示。
- 脚本采用分步输出（[Step X/Y]）与彩色 Write-Host 标识成功/警告/错误，失败时 Read-Host 暂停等待用户确认。