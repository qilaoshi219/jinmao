运行端到端测试需先启动 MySQL、MinIO、DeepSeek、Doc2x、TTS 等服务，然后执行 `powershell -File test/e2e-test.ps1`，脚本会提示输入邮箱验证码并轮询流水线状态直至 completed/partial_completed。MinIO 测试通过 `node test/code/minio-crud.js` 运行，默认连接 `127.0.0.1:9000`，密钥为 `minioadmin`。