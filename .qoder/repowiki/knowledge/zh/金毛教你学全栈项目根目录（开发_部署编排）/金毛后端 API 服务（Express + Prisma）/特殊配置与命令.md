启动前需复制 `.env.example` 为 `.env` 并填写 DATABASE_URL、JWT_SECRET、DEEPSEEK_API_KEY、DOC2X_API_KEY 等环境变量；首次运行会自动执行 `npx prisma generate` 生成 Client 和 `npx prisma migrate deploy` 应用数据库迁移；可通过 `bash setup.sh` 自动初始化，或通过 `npm start` 启动服务。