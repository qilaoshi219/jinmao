# 开发日志

> 版本号说明：后端 `node-jinmao/package.json` 和前端 `WEB/package.json` 各自独立维护独立的版本号序列。前端版本标注为 `[前端] vX.Y.Z`，后端版本标注为 `[后端] vX.Y.Z`。

## 2026-07-10 — [前端] v0.3.4 — 修复课程卡片3个Bug

### 修改原因
1. 状态标签显示英文原始值（如 `partial_completed`、`course_generating` 等），缺少完整中文映射
2. 中间状态不在轮询白名单内，正在生成的课程不会自动刷新
3. 三点菜单展开后点击外部无法关闭
4. 删除确认弹窗堆在左上角（ElMessageBox CSS 未加载）

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `WEB/src/components/CourseCard.vue` | 修改 | `statusLabel` 完整映射后端 pipeline 全部 21 种状态到中文；`isProcessing` 改为终端白名单反向判断 |
| `WEB/src/components/CourseCard.vue` | 修改 | 引入 `onClickOutside`，菜单容器加 `ref="menuRef"`，点击外部自动关闭 |
| `WEB/src/pages/home/script.js` | 修改 | `hasProcessingCourses` 同样改为终端白名单反向判断，自动覆盖所有中间状态 |
| `WEB/src/main.js` | 修改 | 新增 `el-message-box.css` 和 `el-overlay.css` 导入，修复确认弹窗居中 |
| `WEB/package.json` | 修改 | 版本号 0.3.3 → 0.3.4（修订号+1，3个bug修复） |

### 设计决策
- **反向判断模式**：定义终端状态白名单 `["completed","partial_completed","failed","error"]`，凡不在此列表内的一律视为"处理中"。后端新增中间状态时前端无需手动同步。

---

## 2026-07-10 — [前端] v0.3.3 — 修复 1024×600 视口登录页输入框过窄

### 修改原因
1024px 刚好进入 lg 断点（左栏 `max-lg:hidden` 失效，Hero 区恢复显示），但卡片 `xl:max-w-5xl` 要到 1280px 才扩大，导致 1024-1279px 区间卡片仅 672px、右栏按 0.8 份额仅 176.8px 可用宽度。

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `WEB/src/pages/login/index.vue` | 修改 | 卡片 `xl:max-w-5xl` → `lg:max-w-5xl`，让卡片在 lg(1024px) 断点就扩大到 1024px |
| `WEB/package.json` | 修改 | 版本号 0.3.2 → 0.3.3（修订号+1，bug 修复） |

### 修复效果
- **1024-1279px**：卡片从 672px 扩大到 1024px，右栏可用从 177px → 318px
- **≥1280px**：无变化
- **<1024px**：无变化（左栏隐藏，卡片仍为 672px）

---

## 2026-07-10 — [前端] v0.3.2 — 进一步修复登录页中段宽度不足问题

### 修改原因
v0.3.1 将 md-lg 断点改为等比例 1:1 后，768px 宽度下右栏可用 276px，但邮箱行中 `el-button size="large"` 固定占约 120px，导致 `el-input flex-1` 实际可用仅约 148px（减去 prefix 图标后约 118px），在 1:1 方形屏幕（如 iPad）下仍然无法正常输入。

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `WEB/src/pages/login/index.vue` | 修改 | 左栏 `max-md:hidden` → `max-lg:hidden`，1024px 以下完全隐藏 Hero 区 |
| `WEB/package.json` | 修改 | 版本号 0.3.1 → 0.3.2（修订号+1，bug 修复） |

### 修复效果
- **768-1023px**：左栏完全隐藏，右栏独占 672px 卡片宽度，邮箱输入可用 484px（+227%）
- **1024px 以上**：恢复完整两栏布局，无变化
- **768px 以下**：无变化（原本即隐藏）

---

## 2026-07-10 — [前端] v0.3.1 — 修复登录页 1:1 屏幕布局问题

### 修改原因
登录页在 1:1（方形）屏幕尺寸（768-1023px 断点范围）下，右侧表单区被挤压过窄（仅 177px 可用宽度），导致：
1. 输入框过于狭窄
2. Element Plus 输入框的 prefix 图标 span 遮挡了 input 区域，用户无法正常输入

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `WEB/src/pages/login/index.vue` | 修改 | 左栏 `flex-[1.2]` → `flex-[1] lg:flex-[1.2]`，lg 以下等比例 1:1 |
| `WEB/src/pages/login/index.vue` | 修改 | 右栏 `flex-[0.8]` → `flex-[1] lg:flex-[0.8]`，与左栏对称 |
| `WEB/src/pages/login/index.vue` | 修改 | 右栏 `p-[46px]` → `p-[30px] lg:p-[46px]`，lg 以下缩减内边距 |
| `WEB/package.json` | 修改 | 版本号 0.3.0 → 0.3.1（修订号+1，bug 修复） |

### 修复效果
- lg 以上（≥1024px）屏幕：无变化
- 768-1023px：右栏可用宽度 177px → 276px（+56%）
- 输入框可正常显示和输入

---

## 2026-07-10 — [后端] v1.10.2 — 修复幻灯片数为 0 导致 PPT/TTS/MP3/SRT 无法生成、images 未落盘等 3 个 bug

### 修改原因
服务器部署测试反馈 3 个问题：
1. **幻灯片数为 0**：`generate_outline.js` 中 `JSON.parse` 解析大模型返回的纯数组 `[...]` 后得到 JavaScript Array，但 `course_pipeline.js` 通过 `outline.slides` 消费（Array 没有 `.slides` 属性 → undefined → 0），导致 PPT、TTS、MP3、SRT 全被跳过
2. **文本细化未启动**：问题 1 的连锁反应，`outline.slides` 为空导致扩写条件判断失败
3. **images 文件夹未落盘**：PDF 分支只检查 `image` 目录名、Word 分支完全缺失、压缩包分支用错误目录搜索 MD 文件

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `node-jinmao/utils/generate_outline.js` | 修改 | JSON.parse 后增加 `Array.isArray(outline)` 检测，纯数组自动包装为 `{slides: [...]}`；移除 `json_object` response_format 改为默认 text 模式 |
| `node-jinmao/config/outline_prompt.txt` | 修改 | prompt 输出格式从纯数组 `[...]` 改为对象 `{"slides": [...]}`，与代码预期格式一致 |
| `node-jinmao/service/POSTbook.js` | 修改 | 新增 `uploadImageDir()` 通用辅助函数（兼容 image/images 目录名 + 诊断日志）；PDF/Word/压缩包三分支统一使用；修复压缩包分支 `findMdFile(tempDir)` → `findMdFile(extractResult.extractDir)` |
| `node-jinmao/service/course_pipeline.js` | 修改 | Phase3 增加 slides 为空时的诊断日志（输出 outline 类型、keys、内容摘要） |
| `node-jinmao/package.json` | 修改 | 版本号 1.10.1 → 1.10.2（修订号+1，bug 修复） |

### 根因链路图
```
prompt 要求数组 [...] → JSON.parse → Array → outline.slides === undefined
    → totalSlides = 0 → 跳过扩写 + PPT + TTS + MP3 + SRT
```

### 修复后保障
- `Array.isArray` 兜底：无论大模型返回纯数组还是对象包裹的数组，都能正确消费
- `uploadImageDir` 统一：三个分支共用，支持 image/images 双目录名，找不到时打印目录内容诊断
- 双格式保障：prompt 改为要求对象格式 + 代码做数组兜底

---

## 2026-07-10 — [后端] v1.10.1 — 修复教材文件列表只显示一个文件的 bug

### 修改原因
教材文件列表 API 只返回了 1 个文件，原因是 `directoryPrefix` 从 `textbookPath`（如 `/usercourse/1/2/归一化子目录/output.md`）中提取，只列出了归一化产物子目录下的文件，遗漏了源文件、封面图、章节目录等。

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `node-jinmao/API/book/files.js` | 修改 | 改为使用 `userId` + `courseId` 构建课程根目录路径 `/usercourse/{userId}/{courseId}/` 作为 MinIO listObjects 的 prefix，确保递归列出所有文件 |
| `node-jinmao/package.json` | 修改 | 版本号 1.10.0 → 1.10.1（修订号+1，bug 修复） |

### 修复前后对比

| | 修复前 | 修复后 |
|------|--------|--------|
| 目录前缀来源 | textbookPath 提取子目录 | userId + courseId 构建根目录 |
| prefix 示例 | `/usercourse/1/2/归一化目录/` | `/usercourse/1/2/` |
| 列出的文件 | 仅归一化子目录下的文件（如 output.md） | 源文件 + 封面图 + 归一化目录 + 章节目录等所有文件 |

---

## 2026-07-10 — [后端] v1.10.0 / [前端] v0.3.0 — 新增教材文件列表弹窗（临时测试功能）

### 修改原因
点击教材卡片需要弹窗展示该教材在 MinIO 中存储的所有文件，方便测试验证教材文件是否正确上传和处理。此功能为临时测试用途，未来会删除，代码完全独立不耦合。

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `node-jinmao/API/book/files.js` | **新建** | `GET /api/v1/books/:id/files` 端点，使用 MinIO `listObjects` 递归列出教材目录下所有文件，含鉴权和权限校验 |
| `node-jinmao/API/book/index.js` | 修改 | 新增 `filesRouter` 导入和注册（2 行新增） |
| `WEB/src/components/CourseFilesDialog.vue` | **新建** | 独立弹窗组件，展示文件名称、大小、修改时间，支持加载/错误/空状态，暗黑模式双轨适配 |
| `WEB/src/api/books.js` | 修改 | 新增 `getBookFiles(bookId)` API 封装函数 |
| `WEB/src/pages/home/script.js` | 修改 | 引入 CourseFilesDialog 组件，新增弹窗状态变量，修改 `onOpenCourse()` 打开文件弹窗 |
| `WEB/src/pages/home/index.vue` | 修改 | 模板中挂载 CourseFilesDialog 组件 |
| `node-jinmao/package.json` | 修改 | 版本号 1.9.2 → 1.10.0（次版本号+1，新增 API 端点） |
| `WEB/package.json` | 修改 | 版本号 0.2.0 → 0.3.0（次版本号+1，新增功能） |

### 删除指引
未来删除此功能只需：
1. 删除 `node-jinmao/API/book/files.js`
2. 删除 `WEB/src/components/CourseFilesDialog.vue`
3. 在 `node-jinmao/API/book/index.js` 中去掉 filesRouter 的 require 和 use 两行
4. 在 `WEB/src/pages/home/script.js` 中去掉 CourseFilesDialog 的 import、components 注册、状态定义、return 导出
5. 在 `WEB/src/pages/home/index.vue` 中去掉 `<CourseFilesDialog>` 组件标签
6. 在 `WEB/src/api/books.js` 中去掉 `getBookFiles` 函数

---

## 2026-07-10 — [后端] v1.9.2 — 增强请求日志和文件代理诊断输出

### 修改原因
封面 404 问题持续存在，但服务器日志中完全没有 `[API_files]` 或 `[app] GET /api/v1/files/...` 的记录，无法判断请求是否到达 Node.js 服务端。需增强日志以便快速定位问题层级（反向代理 / Node.js / MinIO）。

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `node-jinmao/app.js` | 修改 | 请求日志中间件新增请求到达实时日志 `[app] ← GET /xxx [到达]`（在 res.finish 之前输出） |
| `node-jinmao/app.js` | 修改 | 文件代理路由注册时输出 `[app] ✅ 文件代理路由已注册: /api/v1/files/*` |
| `node-jinmao/API/files.js` | 修改 | 模块加载时输出 MinIO 连接信息（endpoint、port、bucket、useSSL） |
| `node-jinmao/API/files.js` | 修改 | 404 分支增强日志：输出 bucket 名、完整的 key 路径、MinIO 错误 code 和 message |
| `node-jinmao/API/files.js` | 修改 | 500 分支增强日志：额外输出错误 code、bucket、key |
| `node-jinmao/package.json` | 修改 | 版本号 1.9.1 → 1.9.2（修订号+1，调试增强） |

### 诊断方法
部署后观察日志：
- 如果看到 `[app] ← GET /api/v1/files/... [到达]` → 请求到达 Node.js，继续看 files.js 日志判断 MinIO 问题
- 如果看不到 `←` → 请求在反向代理（nginx）层被拦截，需检查 nginx 配置

---

## 2026-07-10 — [后端] v1.9.1 — 修复文件代理路由路径前导斜杠导致 MinIO 404

### 修改原因
刷新页面后封面图片持续返回 404，即使封面已成功上传到 MinIO。排查发现 `API/files.js` 中 `router.get("/*")` 的 `req.params[0]` 在 Express Router 挂载场景下可能带前导 `/`，导致 MinIO 查询 `/usercourse/...`（而非 `usercourse/...`），statObject 找不到文件。

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `node-jinmao/API/files.js` | 修改 | `req.params[0]` 取值后添加 `replace(/^\/+/, "")` 防御性 strip 前导 `/`，同时新增调试日志输出原始路径 |
| `node-jinmao/package.json` | 修改 | 版本号 1.9.0 → 1.9.1（修订号+1，bug 修复） |

### 技术要点
- `app.use("/api/v1/files", router)` 挂载后，`router.get("/*")` 的 `req.params[0]` 在不同 Express 版本下行为不一致，有的带 `/` 有的不带
- 修复后统一使用 `rawPath.replace(/^\/+/, "")` 确保 MinIO object key 不以 `/` 开头
- 新增日志 `代理请求: {filePath}（原始: {rawPath}）` 方便后续排查路径问题

---

## 2026-07-10 — [后端] v1.9.0 — 大模型调用添加心跳机制与扩写进度提示

### 修改原因
Phase3（课程大纲生成）在调用 DeepSeek 大模型时使用 `stream: false` 阻塞等待，期间控制台无任何输出，用户无法判断程序是否仍在运行。需添加心跳机制每 2 秒输出一次状态，同时在逐页扩写循环中显示进度。

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `node-jinmao/utils/generate_outline.js` | 修改 | API 调用前后添加 `setInterval` 心跳定时器（每 2 秒输出 `[generate_outline] 心跳 — 仍在等待 DeepSeek 大模型响应...`），API 返回或异常时 `clearInterval` 清除 |
| `node-jinmao/utils/elaboration.js` | 修改 | 同上，API 调用前后添加心跳定时器（前缀 `[elaboration]`） |
| `node-jinmao/service/course_pipeline.js` | 修改 | Phase3 扩写循环中添加进度日志 `扩写进度 X/N — 正在调用大模型扩写幻灯片...` 和空口播稿跳过提示 |
| `node-jinmao/package.json` | 修改 | 版本号 1.8.1 → 1.9.0（次版本号+1，新增功能） |

### 技术要点
- 心跳定时器使用 `setInterval` 2000ms，在 `await` 前后分别放置启动和清除逻辑
- catch 分支中也需 `clearInterval`，避免异常时定时器泄漏
- 扩写循环中新增 `totalSlidesCount` 变量缓存总页数，避免重复读取 `outline.slides.length`
- 每个扩写的 `elaborateText()` 内部也有独立心跳，双层保障

---

## 2026-07-10 — [后端] v1.8.1 — 启动时基于版本号自动执行数据库迁移

### 修改原因
`app.js` 原有迁移逻辑每次启动都无条件运行 `prisma migrate deploy`，且失败时仅 `console.warn` 不阻止启动。这导致生产环境中 Prisma 迁移未成功应用时（如 `subtitle` 列缺失），服务器带病运行，后续 API 调用返回 500 错误。

现改为基于 `package.json` 版本号的条件迁移：仅在版本变更时执行迁移，迁移失败时 `process.exit(1)` 拒绝启动。

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `node-jinmao/app.js` | 修改 | 替换第71-94行迁移逻辑：读取 `package.json` 版本号与 `.migration_version` 对比，版本未变则跳过迁移，版本变更则执行 `prisma migrate deploy`；迁移失败时打印详细错误信息并 `process.exit(1)` |
| `node-jinmao/.migration_version` | 新建 | 运行时版本记录文件，JSON 格式 `{"version":"1.8.1","appliedAt":"..."}`，由启动流程自动更新 |
| `node-jinmao/.gitignore` | 修改 | 添加 `.migration_version` 忽略规则（每台机器独立记录） |
| `node-jinmao/package.json` | 修改 | 版本号 1.8.0 → 1.8.1（修订号+1，配置/稳定性修复类改动） |

### 技术要点
- `.migration_version` 文件不存在时视为首次启动，强制执行一次迁移
- 版本号读取失败（package.json 损坏）时使用默认值 `0.0.0` 兜底，仍执行迁移
- `.migration_version` 写入失败不阻止启动（迁移本身已成功），仅记录警告
- `prisma migrate deploy` 超时从 30s 上调至 60s（生产环境首次部署可能需要更长时间）
- `start.ps1` 第75行的 `prisma migrate deploy` 保留不动（作为额外保障层，幂等无副作用）

---

## 2026-07-09 — [后端] v1.8.0 — Pipeline 接入异步标题生成和封面生成

### 修改原因
`service/create_title.js` 和 `service/create_cover_image.js` 原本只在教材上传阶段（POSTbook）触发了标题生成，封面生成则完全未接入任何流程。现将其接入课程流水线，在 Phase 1（数据获取 + MinIO 下载 MD 文件）完成后异步启动，不阻塞流水主线。

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `service/course_pipeline.js` | 修改 | 顶部导入 `startTitleGeneration` 和 `startCoverGeneration`；Phase 1 完成后异步启动标题生成（输入：courseId/userId/filename/textbookPath）和封面生成（输入：courseId/userId/title/MD前2000字符样本），失败不影响流水线 |
| `package.json` | 修改 | 版本号 1.7.1 → 1.8.0 |
| `FILE.md` | 修改 | 更新 course_pipeline.js 描述（补充 Phase1 后异步启动说明）和最后修改时间 |

### 技术要点
- 标题生成和封面生成均使用 `startXxx()` 异步模式（fire-and-forget），通过 `.catch()` 捕获异常防止 unhandledRejection
- 封面生成的样本从 Phase 1 已下载到本地的 MD 文件中直接读取，无需额外 MinIO 下载
- 封面生成使用当前课程名作为 title 参数，后续标题生成完成会异步更新数据库 name/subtitle

---

## 2026-07-09 — [前端] v0.2.0 — 前端布局框架改造（参考老项目结构）

### 修改原因
将老项目 `F:\代码\金毛教你学\web` 的布局框架/结构迁移到新项目，包括弹窗尺寸、栏位布局、组件嵌套关系等，不改配色和业务逻辑。

### 改造文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `WEB/src/components/CourseCard.vue` | 修改 | 封面 16:9 宽高比、添加处理中遮罩层、状态进度条、三点操作菜单（删除教材）、光效叠加 |
| `WEB/src/components/UploadBookDialog.vue` | 修改 | 单栏→左右两栏（268px 模式选择 + 上传区），弹窗宽度 520px→860px，添加 AI 模型选择、自定义 header |
| `WEB/src/components/HomeTopbar.vue` | 修改 | 添加通知铃铛骨架按钮、用户区改为下拉菜单模式（含退出登录） |
| `WEB/src/components/HomeSidebar.vue` | 修改 | 上传按钮从底部移到品牌区下方、添加会员版徽章、导航分主/次级菜单（分隔线） |
| `WEB/src/pages/home/index.vue` | 修改 | 新增 4 个统计卡片（学习时长/章节/正确率/连续天数）、排序下拉、添加教材卡片（网格末尾） |
| `WEB/src/pages/login/index.vue` | 修改 | 单栏→左右两栏卡片布局（Hero 装饰区 + 登录表单），顶部品牌区，底部版权 |
| `WEB/src/pages/home/script.js` | 修改 | 新增 `sortBy` ref 排序变量 |
| `WEB/package.json` | 修改 | 版本号 0.1.3 → 0.2.0 |
| `WEB/开发日志.md` | 删除 | 合并入根目录统一开发日志 |

### 技术要点
- 所有圆角统一 `rounded-[10px]`（遵守设计规范），过渡时间 `duration-500`
- 配色（tokens.css）和业务逻辑（script.js/.js）均未修改
- 登录页使用书本 SVG 图标替代老项目的 hero 图片（新项目无该资源）
- 上传弹窗模式卡片中"互动式PPT"为默认选中态，其余为禁用骨架占位

---

## 2026-07-09 — [前端] v0.1.2 — 课程封面图片展示

### 修改原因
后端已实现封面生成服务和文件代理 API，前端需要支持展示课程封面图片。

### 新增/修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `WEB/src/components/CourseCard.vue` | 修改 | 封面区域支持图片展示：有封面显示 `<img>` 标签，无封面或加载失败显示渐变背景 + 首字符占位，使用 `@error` 事件降级处理 |
| `WEB/package.json` | 修改 | 版本号 0.1.1 → 0.1.2 |

### 技术要点
- 封面 URL 直接从后端返回的 `coverUrl` 字段获取（格式：`/api/v1/files/...`）
- 使用 `v-if="coverUrl && !coverLoadFailed"` 条件渲染，图片加载失败时自动回退渐变占位
- 图片使用 `object-cover` 保持比例裁剪，高度固定 128px

---

## 2026-07-09 — [后端] v1.2.2 — 修复缺少 Course/Chapter 表迁移 + 启动自动同步数据库

### 修复
- **数据库迁移缺失**：补全 `prisma/migrations/20260709105504_add_course_and_chapter/migration.sql`
  - 包含 `Course` 表（13 个字段 + user_id 外键索引）
  - 包含 `Chapter` 表（14 个字段 + course_id 外键索引）
  - 外键约束：Course → User、Chapter → Course（RESTRICT + CASCADE）
  - 修复宝塔部署后教材上传/列表查询报 "table course does not exist" 错误

### 修改
- **`node-jinmao/app.js`**：启动自检增强，实现零手动操作的自动化部署
  - Prisma Client 缺失时**自动执行 `prisma generate`**（不再报错退出）
  - Prisma Client 就绪后**自动执行 `prisma migrate deploy`**（同步数据库 schema）
  - 迁移失败仅警告不退出（兼容数据库尚未创建等场景）
  - 部署后只需启动应用，无需 SSH 手动操作数据库

---

## 2026-07-09 — [后端] v1.7.1 — 前端显示教材副标题

### 修改原因
上一轮实现了标题生成服务（service/create_title.js），会将 AI 生成的副标题写入数据库 subtitle 字段。需要修改前端和后端 API，使副标题能够在教材卡片中显示。

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `API/book/list.js` | 修改 | 返回数据中新增 `subtitle` 字段，更新 @openapi 注释 |
| `API/book/detail.js` | 修改 | 返回数据中新增 `subtitle` 字段，更新 @openapi 注释 |
| `WEB/src/components/CourseCard.vue` | 修改 | 在标题下方显示副标题（灰色文字，单行截断） |
| `node-jinmao/package.json` | 修改 | 版本号 1.7.0 → 1.7.1（PATCH，小改动） |
| `WEB/package.json` | 修改 | 版本号 0.1.2 → 0.1.3（PATCH，小改动） |

### 技术细节
- **显示位置**：副标题显示在标题下方、描述上方
- **样式**：使用灰色文字（`text-gray-600 dark:text-gray-300`），比标题浅，比描述深
- **截断**：单行截断（`line-clamp-1`），鼠标悬停显示完整内容
- **可选显示**：仅当 `subtitle` 存在时显示（`v-if="course.subtitle"`）

---

## 2026-07-09 — [后端] v1.7.0 — 新增文件代理 API + 教材封面展示

### 修改原因
课程封面图片存储在 MinIO 中，前端无法直接访问。需要后端提供文件代理 API 将 MinIO 文件代理给前端，并在教材列表/详情 API 中返回封面访问 URL。

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `API/files.js` | **新建** | 文件代理 API（GET /api/v1/files/{path}），后端从 MinIO 获取文件流并返回给前端，无需暴露 MinIO 服务 |
| `app.js` | 修改 | 注册 filesRouter 路由 |
| `API/book/list.js` | 修改 | 新增 coverUrl 字段，更新 @openapi 注释 |
| `API/book/detail.js` | 修改 | 新增 coverUrl 字段，更新 @openapi 注释 |
| `package.json` | 修改 | 版本号 1.6.0 → 1.7.0（新增功能，MINOR +1） |
| `FILE.md` | 修改 | 新增 files.js 索引条目 |

### 技术细节
- **文件代理**：使用 MinIO getObject() 获取文件流，直接 pipe 到 HTTP 响应，不占用服务器内存
- **MIME 类型**：使用内置映射表（基于文件扩展名），不引入额外 npm 依赖
- **缓存策略**：`Cache-Control: public, max-age=86400`，静态资源缓存 1 天
- **错误处理**：文件不存在时返回 JSON 错误，前端 `<img>` 无法解析 JSON 会触发 `@error` 降级显示占位
- **coverUrl 格式**：`/api/v1/files{coverPath}`，如 `/api/v1/files/usercourse/1/1/cover.png`

---

## 2026-07-09 — [后端] v1.6.0 — 新增标题生成服务 service/create_title.js

### 修改原因
根据 `service/create_title.js` 文件头部注释描述，需要实现"标题生成服务"模块。该服务在用户上传教材文件后、MD 文件落盘后异步执行，自动为课程生成标题和副标题并写入数据库。

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `service/create_title.js` | **重写** | 从 8 行注释存根实现为完整标题生成服务（244行）：输入验证 → MinIO 下载 MD → 提取前 1000 行 → 调用 AI 生成标题 → 更新数据库 |
| `prisma/schema.prisma` | **修改** | Course 模型新增 `subtitle` 字段（VARCHAR(200)，可选） |
| `utils/repo/book_repo.js` | **修改** | `createCourse` 函数支持 `subtitle` 字段写入 |
| `service/POSTbook.js` | **修改** | 在 `runNormalization()` 中 MD 文件落盘后异步调用标题生成服务 |
| `package.json` | **修改** | 版本号 1.5.0 → 1.6.0（新增功能，MINOR +1） |
| `FILE.md` | **修改** | 更新 service/create_title.js 索引条目 |

### 技术细节
- **触发时机**：在 `runNormalization()` 中 MD 文件落盘后立即异步调用，不阻塞流水线
- **内容提取**：使用 `extractor_md.js` 提取前 1000 行作为原文
- **MinIO 下载**：从 MinIO 下载 MD 文件到临时目录，处理完成后清理
- **错误处理**：标题生成失败不影响主流程，仅记录日志
- **字段映射**：title → name，subtitle → subtitle（新增字段）

### 函数接口

```javascript
// 核心函数：生成标题并写入数据库
generateCourseTitle(courseId, userId, filename, textbookPath) → { code, title?, subtitle?, message }

// 异步启动函数：不阻塞调用方
startTitleGeneration(courseId, userId, filename, textbookPath) → void
```

### 错误码
- 200：成功生成标题并写入数据库
- 400：输入参数不合法
- 500：MinIO 下载失败、API 调用失败或数据库更新失败
- 502：AI 返回内容解析失败
- 503：AI 返回 JSON 不包含必要字段

### 测试结果

```
====================================
开始测试 service/create_title.js 模块
====================================

【测试1】验证模块导出
generateCourseTitle 类型: function
startTitleGeneration 类型: function
状态: ✓ 通过

【测试2】输入验证 - 空 courseId
返回结果: { "code": 400, "message": "课程 ID 不能为空" }
状态: ✓ 通过（正确拦截空 courseId）

【测试3】输入验证 - 空 userId
返回结果: { "code": 400, "message": "用户 ID 不能为空" }
状态: ✓ 通过（正确拦截空 userId）

【测试4】输入验证 - 空文件名
返回结果: { "code": 400, "message": "文件名不能为空" }
状态: ✓ 通过（正确拦截空文件名）

【测试5】输入验证 - 空 MD 路径
返回结果: { "code": 400, "message": "MD 文件路径不能为空" }
状态: ✓ 通过（正确拦截空 MD 路径）

====================================
测试完成
====================================
```

---

## 2026-07-09 — [后端] v1.5.0 — 新增标题生成模块 create_title.js

### 修改原因
根据 `create_title.js` 文件头部注释描述，需要实现"调用 DeepSeek 小模型生成书籍标题"的功能模块。该模块用于为教材内容自动生成合适的标题和副标题。

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `utils/create_title.js` | **重写** | 从 5 行注释存根实现为完整标题生成模块（173行）：输入验证 → 加载提示词 → 调用 DeepSeek 小模型 → 解析 JSON → 返回标题和副标题 |
| `config/title_prompt.txt` | **新建** | 标题生成提示词模板，使用 `{{filename}}` 和 `{{content}}` 占位符 |
| `config/prompt.json` | **修改** | 新增 `title_prompt` 字段指向提示词模板文件 |
| `package.json` | **修改** | 版本号 1.4.0 → 1.5.0（新增功能，MINOR +1） |
| `FILE.md` | **修改** | 新增 create_title.js 和 title_prompt.txt 索引条目 |

### 技术细节
- **模型选择**：使用 DeepSeek 小模型（deepseek-v4-flash），速度快成本低，适合标题生成这类简单任务
- **输入验证**：使用 `input_validator.js` 公共验证模块，支持注入攻击检测、空值检查、长度限制
- **JSON 格式输出**：使用 `response_format: {"type": "json_object"}` 确保 API 返回合法 JSON
- **纯函数设计**：仅返回数据，不负责文件持久化（与项目其他工具模块一致）
- **惰性加载**：OpenAI 客户端使用动态 import，首次调用时初始化

### 函数接口

```javascript
// 核心函数：生成标题和副标题
createTitle(filename, content) → { code, title?, subtitle?, message }
```

### 错误码
- 200：成功生成标题
- 400：输入参数不合法（空值/类型错误/注入攻击/长度超限等）
- 500：DeepSeek API 调用失败
- 502：返回内容不是合法 JSON
- 503：返回 JSON 不包含必要字段（title 或 subtitle）

### 测试结果

```
====================================
开始测试 create_title.js 模块
====================================

【测试1】正常输入测试
输入：文件名='机器学习基础教程.pdf'，内容长度=199 字符
返回结果：{
  "code": 200,
  "title": "机器学习基础入门",
  "subtitle": "掌握监督学习、无监督学习与强化学习等核心算法"
}
状态： ✓ 通过

【测试2】空文件名验证测试
返回结果：{
  "code": 400,
  "message": "filename不能为空，请提供有效的filename内容。"
}
状态： ✓ 通过（正确拦截空文件名）

【测试3】空内容验证测试
返回结果：{
  "code": 400,
  "message": "content不能为空，请提供有效的content内容。"
}
状态： ✓ 通过（正确拦截空内容）

【测试4】注入攻击检测测试
返回结果：{
  "code": 400,
  "message": "检测到可能的注入攻击行为，输入已被拦截。"
}
状态： ✓ 通过（正确拦截注入攻击）

====================================
测试完成
====================================
```

---

## 2026-07-09 — [后端] v1.4.0 — 新增图书封面图片生成服务 create_cover_image.js

### 修改原因
课程需要封面图片功能，在 md 文件转换结束后自动生成封面图片，上传到 MinIO 后将 URL 写入数据库。项目之前只有注释存根，需要实现完整逻辑。

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `service/create_cover_image.js` | **重写** | 从 9 行注释存根实现为完整封面生成服务（378行）：输入验证 → 加载提示词 → 调用文生图 API → 下载图片 → 上传 MinIO → 更新数据库 |
| `config/cover_prompt.txt` | **新建** | 封面生成提示词模板，使用 `{{title}}` 和 `{{sample}}` 占位符 |
| `config/prompt.json` | **修改** | 新增 `cover_prompt` 字段指向提示词模板文件 |
| `package.json` | **修改** | 版本号 1.3.0 → 1.4.0（新增功能，MINOR +1） |
| `FILE.md` | **修改** | 新增 create_cover_image.js 和 cover_prompt.txt 索引条目 |

### 技术细节
- **异步执行**：提供 `startCoverGeneration()` 函数，不阻塞调用方，适合后台异步执行
- **提示词模板**：从 `config/cover_prompt.txt` 加载模板，替换 `{{title}}` 和 `{{sample}}` 占位符
- **图片比例**：固定使用 16:9 比例，适合课程封面
- **MinIO 路径**：封面图片存储路径为 `/usercourse/{userId}/{courseId}/cover.png`
- **错误处理**：封面生成失败不影响课程主流程，仅记录日志
- **临时文件清理**：使用 `finally` 块确保临时目录被清理
- **重定向支持**：下载图片时自动处理 HTTP 重定向

### 函数接口

```javascript
// 核心函数：生成封面并返回结果
createCoverImage(courseId, userId, title, sample) → { code, coverUrl, message }

// 异步启动函数：不阻塞调用方
startCoverGeneration(courseId, userId, title, sample) → void
```

### 错误码
- 200：成功生成封面并上传
- 400：输入参数不合法
- 500：API 调用失败或上传失败
- 502：生成任务失败或内容违规
- 504：轮询超时

---

## 2026-07-09 — [后端] v1.3.0 — 新增文生图模块 create_image.js（Grsai gpt-image-2 API）

### 修改原因
课程大纲中的 `PIC` 字段需要文生图能力，将文本描述转换为图片 URL 供 PPT 嵌入。项目之前缺少文生图功能。

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `utils/create_image.js` | **重写** | 从 3 行注释存根实现为完整文生图模块（238行）：输入验证 → POST 异步任务 → 轮询结果 → 返回图片 URL |
| `config/grsai_config.json` | **新建** | Grsai 文生图 API 非敏感配置：API BASE（国内节点）、MODEL（gpt-image-2）、默认比例、轮询间隔/次数 |
| `.env` | **修改** | 新增 `GRSAI_API_KEY=你的APIKEY` 占位符 |
| `config/index.js` | **修改** | 加载 grsai_config.json + 注入 GRSAI_API_KEY + API Key 格式校验 + 导出 grsai 配置 |
| `package.json` | **修改** | 版本号 1.2.8 → 1.3.0（新增功能，MINOR +1） |
| `FILE.md` | **修改** | 新增 create_image.js 和 grsai_config.json 索引条目 |

### 技术细节
- **API 节点**：默认使用国内节点 `https://grsai.dakka.com.cn`，可在 `grsai_config.json` 切换为全球节点
- **异步模式**：使用 `replyType: "async"` 提交任务后轮询结果，避免长连接超时
- **轮询策略**：间隔 3 秒，最多 60 次（共约 3 分钟），支持配置调整
- **原生 fetch**：使用 Node.js 18+ 内置的 `fetch` API，无需额外安装依赖
- **错误码**：200（成功）/ 400（参数不合法）/ 500（API/网络错误）/ 502（任务失败/违规）/ 504（轮询超时）

---

## 2026-07-09 — [后端] v1.2.3 — MinIO 端口号改为环境变量可配置

### 修改原因
生产环境的 MinIO 端口号不是默认的 9000，导致 `upload_minio.js` 上传文件时报 `connect ECONNREFUSED` 错误。原代码从 `config/minio_config.json` 硬编码读取端口，生产环境无法灵活修改。

### 修改内容

| 文件 | 操作 | 说明 |
|------|------|------|
| `utils/upload_minio.js` | **修改** | (1) 新增 dotenv 主动加载，确保模块独立调用时也能读取 .env；(2) `loadConfig()` 改为双轨策略——优先从环境变量（MINIO_ENDPOINT/MINIO_PORT/MINIO_USE_SSL/MINIO_ACCESS_KEY/MINIO_SECRET_KEY/MINIO_BUCKET）读取，环境变量不完整时回退到 JSON 配置文件作为后备；(3) 控制台日志明确标注配置来源（env/json） |
| `.env` | **修改** | 补全 MinIO 配置项：新增 MINIO_ENDPOINT(127.0.0.1)、MINIO_PORT(9000)、MINIO_USE_SSL(false)、MINIO_BUCKET(jinmao)，同时填写 MINIO_ACCESS_KEY 和 MINIO_SECRET_KEY 的本地开发默认值 |
| `.env.example` | **修改** | 补全 MinIO 配置模板：新增 MINIO_ENDPOINT、MINIO_PORT、MINIO_USE_SSL、MINIO_BUCKET 示例值 |
| `package.json` | **修改** | 版本号 1.2.2 → 1.2.3（PATCH：bug 修复） |

### 技术细节
- 环境变量与 `course_pipeline.js` 中的 MinIO 初始化方式保持一致（同一套变量名）
- 生产环境部署时只需修改 `.env` 中的 `MINIO_PORT` 即可解决连接问题
- JSON 配置文件保留作为后备方案，本地开发无需 `.env` 配置也能正常工作

### 配置优先级
1. 环境变量 `.env`（生产/开发环境主要配置方式）
2.  JSON 配置文件 `config/minio_config.json`（本地开发后备方案）

---

## 2026-07-09 — [后端] v1.2.0 — 宝塔部署优化：一键初始化 + 启动自检

### 新增
- **`node-jinmao/setup.sh`**：宝塔部署一键初始化脚本
  - 自动检查 Node.js 版本（>= 18）、系统依赖（LibreOffice、p7zip）
  - 自动从 `.env.example` 创建 `.env` 模板（首次部署时提示编辑）
  - 自动 `npm install` + `prisma generate` + `prisma migrate deploy`（后端）
  - 自动 `npm install` + `npm run build`（前端构建）
  - 完成后输出宝塔Node项目配置参数，直接复制粘贴即可

- **`node-jinmao/ecosystem.config.js`**：PM2 进程管理配置
  - 标准化 PM2 配置，宝塔PM2管理器可直接识别
  - 包含自动重启、内存限制（500MB）、日志配置等生产级参数

### 修改
- **`node-jinmao/app.js`**：新增启动前友好自检（`runStartupChecks` 同步 IIFE）
  - 检查 .env 文件是否存在 → 缺失则输出中文错误提示 + 退出
  - 检查 DATABASE_URL 等关键环境变量 → 缺失则输出修复建议
  - 检查 Prisma Client 是否已生成 → 未生成则提示运行 prisma generate
  - 非阻塞检查 MySQL 连接 → 连接失败只打印警告，不阻止启动
  - AI 服务 Key 缺失仅警告（不影响服务器启动）

- **`pack.ps1`**：更新打包完成后的输出提示为宝塔3步部署流程

- **`宝塔部署指南.md`**：完全重写，精简为 3 步流程
  - 步骤1：上传解压 → 步骤2：运行 setup.sh → 步骤3：宝塔添加Node项目
  - 新增常见问题排查表（错误信息 → 解决方法对应表）
  - 新增部署检查清单

### 目标
- 用户手动上传压缩包解压后，运行 `bash setup.sh` 一键初始化
- 宝塔Node项目「添加即运行」，不再需要手动 Nginx 配置/npm install/prisma 等繁琐步骤

### 版本号
- `node-jinmao/package.json`：1.1.0 → **1.2.0**（MINOR bump，新增宝塔部署优化功能）

---

## 2026-07-08 — [后端] v1.1.0 / [前端] v0.1.1 — 修复教材上传 504 超时 + 异步归一化重构

### 问题
- **教材上传（PDF/Word）出现 504 Gateway Timeout**：上传 16MB PDF 时，前端等待约 2 分钟后收到 504 错误

### 根因分析
- **Node.js HTTP Server 默认 120 秒超时**：[app.js](file:///f:/代码/金毛教你学重构版/node-jinmao/app.js) 中 `app.listen()` 创建 `server` 后未设置 `server.timeout`，Node.js 默认 2 分钟后自动关闭 TCP 连接
- **Doc2x 转换耗时过长**：PDF 上传需要经过 `预上传 → OSS上传(5min超时) → 轮询解析(最长15min) → 触发导出 → 轮询导出(最长5min) → 下载zip(5min超时) → 解压 → 上传MinIO`，整个过程可能超过 20 分钟
- **Vite 代理无超时配置**：[vite.config.js](file:///f:/代码/金毛教你学重构版/WEB/vite.config.js) 的 proxy 中未设置 `timeout`

### 修复
- **`node-jinmao/app.js`**：在 `server` 创建后添加超时配置
  - `server.timeout = 0`：禁用 HTTP 请求超时
  - `server.headersTimeout = 0`：禁用请求头超时（默认继承 timeout）
  - `server.keepAliveTimeout = 0`：禁用 Keep-Alive 超时
  - 启动时输出确认日志

- **`WEB/vite.config.js`**：在 `/api` 代理配置中添加 `timeout: 0`（不超时）

### 重构
- **`node-jinmao/service/POSTbook.js`**：**教材上传流程重构为异步归一化**
  - **旧架构**：`uploadBook()` 函数串行执行全部 6 个步骤（含耗时极长的 Doc2x 转换），完成后才返回 HTTP 响应
  - **新架构**：
    - `uploadBook()`：同步完成步骤 1-3（输入校验 + 创建 Course + 上传源文件到 MinIO），然后启动异步归一化，**立即返回 `courseId`** 给前端
    - `runNormalization()`（新增）：后台异步执行步骤 4-6（格式归一化 + 更新路径 + 启动流水线）
    - 归一化过程中实时更新 `pipelineStatus` 字段（`normalizing` → `idle`/`error`），前端通过 `GET /book/:id/status` 轮询获取进度
  - **4 个文件分支均改为异步**：MD 直接上传、压缩包解压上传、PDF Doc2x 转换、Word→PDF→Doc2x 转换
  - **错误处理**：异步归一化异常时自动将 `pipelineStatus` 更新为 `error`
  - **临时目录管理**：归一化在独立的 `mkdtempSync` 目录中进行，完成后通过 `finally` 清理

### 技术细节
- 文件开头添加了「超过 300 行」说明注释，注明函数高内聚不宜拆分
- `runNormalization()` 使用 `.catch()` 确保异步异常不会导致 `unhandledRejection`
- 流水线启动仍然保持 `setTimeout(100ms)` 异步模式，在归一化完成后触发
- 用户体验改进：上传 16MB PDF 只需几秒即可收到成功响应（而非等待数分钟）

### 版本号
- `node-jinmao/package.json`：1.0.1 → **1.1.0**（MINOR bump，新增异步归一化功能）
- `WEB/package.json`：0.1.0 → **0.1.1**（PATCH bump，代理 timeout 配置）

---

## 2026-07-08 — [后端] v1.0.1 — 修复 Scalar 文档 404 + 启动日志显示版本号

### 修改原因
1. 宝塔部署后访问 `/api/v1/docs` 返回 404，原因是 `@scalar/express-api-reference` 的 `apiReference` 中间件在使用 `spec` 参数时路由匹配异常，官方推荐使用 `url` 参数
2. 需要在启动日志中显示版本号和文件修改时间，方便运维确认当前运行的是哪个版本

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `app.js` | **修改** | (1) `apiReference` 中间件配置从 `spec: { content: swaggerSpec }` 改为 `url: "/api/v1/docs/json"`（官方推荐方式，修复 404）；(2) 新增 `fs` 和 `path` 模块引入；(3) 启动横幅新增版本号（从 package.json 读取）和 app.js 最后修改时间（从 fs.statSync 获取） |
| `package.json` | **修改** | 版本号 1.0.0 → 1.0.1（PATCH：bug 修复 + 小改进） |
| `..\.trae\rules\项目规则.md` | **修改** | 新增版本号管理规则（语义化版本 + 开发日志同步要求） |
| `..\WEB\package.json` | **修改** | 版本号 0.0.0 → 0.1.0（前端初始版本号） |

### 启动横幅效果
```
========================================
  JinMao Server 已启动
  版本: v1.0.1  |  最后修改: 2026/7/8 17:30:00
  端口: 8888
  ...
========================================
```

---

## 2026-07-07 — [后端] — 修复 ESM 模块 CommonJS require 兼容性问题（ERR_REQUIRE_ESM）

### 问题描述
在 WSL/宝塔 Linux 服务器上启动项目时报 `ERR_REQUIRE_ESM` 错误，原因是多个 npm 包已升级为纯 ESM（无法在 CommonJS 中使用 `require()`），包括：
- `swagger-jsdoc` v7+ — 纯 ESM
- `openai` v6+ — 纯 ESM
- `p-queue` v9+ — 纯 ESM
- `dotenv` v17+ — 纯 ESM

### 修改内容
将所有 ESM 包的 `require()` 改为惰性动态 `import()`，确保 CommonJS 环境下正常加载。

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `config/swagger.js` | **修改** | `require("swagger-jsdoc")` → 异步工厂函数 `getSwaggerSpec()`，内部使用 `await import("swagger-jsdoc")` |
| `app.js` | **修改** | `require("./config/swagger")` → `require("./config/swagger")`（现返回异步函数）；`/api/v1/docs/json` 路由移入 async IIFE，先 `await getSwaggerSpec()` 获取 spec |
| `utils/elaboration.js` | **修改** | `require("openai")` → 惰性初始化函数 `getOpenAI()`，调用前 `await getOpenAI()` |
| `utils/generate_outline.js` | **修改** | 同上 |
| `utils/get_line.js` | **修改** | 同上 |
| `utils/htmlppt.js` | **修改** | 同上 |
| `service/course_pipeline.js` | **修改** | `require("p-queue").default` → 惰性初始化函数 `getPQueue()`，使用前 `await getPQueue()` |
| `package.json` | **修改** | `dotenv` 版本从 `^17.4.2` 降级到 `^16.4.5`（v17 为纯 ESM，`require()` 在 app.js 第一行无法改为异步） |

### 技术要点
- **惰性初始化模式**：首次调用时 `await import()` 加载 ESM 模块并缓存，后续调用复用缓存实例
- **dotenv 特殊处理**：由于 dotenv 必须在所有其他 `require()` 之前同步加载（注入环境变量），无法改为异步 import，因此降级到 v16 仍支持 CJS

---

## 2026-07-06 — 修复 `ERR_REQUIRE_ESM` + 新增 WSL 同步脚本

### 修复
- **`node-jinmao/app.js`**：修复 `@scalar/express-api-reference` 的 `ERR_REQUIRE_ESM` 错误
  - **问题**：该包是纯 ESM 模块，CommonJS 的 `require()` 导致 `ERR_REQUIRE_ESM`
  - **修复**：改用 `await import("@scalar/express-api-reference")` 动态导入，将 Scalar UI 挂载和服务器启动逻辑包裹在 async IIFE 中

### 新增
- **`sync_wsl.ps1`**：WSL 自动同步脚本，一键将本地修改同步到 WSL `/www/wwwroot/jinmao/` 并重启服务
  - 步骤 1：确保 WSL 目标目录存在
  - 步骤 2：检查/安装 rsync
  - 步骤 3：rsync 增量同步（自动排除 node_modules/logs/pid/git/traetest）
  - 步骤 4：如 package.json 有变化则自动 `npm install` + `prisma generate` + `prisma migrate deploy`，始终 `npm run build`
  - 步骤 5：通过 PM2 或 nohup 重启后端，等待就绪后输出访问地址

- **`pack.ps1`**：打包脚本修复
  - 修复中文编码问题导致 PowerShell 5.1 解析错误（输出全部改为英文）
  - 改用 robocopy 镜像到临时目录再压缩，保留完整目录结构且正确排除 node_modules

---

## 2026-07-06 — 新增宝塔面板部署指南

### 新增
- **`宝塔部署指南.md`**（根目录）：面向宝塔面板的完整生产部署指南
  - **部署架构**：Nginx（80/443）托管前端静态文件 + 反向代理 `/api/` 到 PM2 守护的 Express 后端（8888）+ MySQL 8.0
  - **内容结构**（11 个章节 + 附录）：
    1. 前置条件（Nginx/MySQL/Node.js版本管理器/PM2管理器/LibreOffice）
    2. 上传项目到服务器（宝塔文件管理 / Git 克隆）
    3. 宝塔面板创建数据库（utf8mb4 字符集）
    4. 环境变量配置（14 个必填项 + JWT 密钥生成）
    5. 安装系统依赖（LibreOffice、p7zip、unrar）
    6. 数据库初始化（Prisma migrate + generate）
    7. 部署后端（PM2 进程守护 + 开机自启）
    8. 部署前端（Nginx 静态文件 + 反向代理完整配置 + HTTPS/SSL）
    9. 验证部署（API 测试 + 前端页面 + 文件上传）
    10. 防火墙放行端口（80/443）
    11. 常见问题排查（后端启动失败、前端 404、502 Bad Gateway、文件上传报错、Node.js 管理器故障）
  - **附录**：`deploy_bt.sh` 一键部署脚本（6 步自动执行：系统依赖 → .env 检查 → 后端依赖+Prisma → 前端构建 → PM2 配置 → 验证）
  - **Nginx 配置要点**：SPA `try_files`、500MB 文件上传限制、900s 超时、gzip 压缩、静态资源 30 天缓存、HTTP/HTTPS 双协议
  - **关联更新**：FILE.md 新增文件索引条目

---

## 2026-07-06 — 修复 WSL 部署上传 500 错误（2 个 bug）

### 修复
- **Bug 1：`extract_zip.js` 硬编码 Windows `7za.exe` 路径，Linux/WSL 上无法找到解压工具**
  - **问题**：第 23 行 `const SZ_EXE_PATH = path.join(__dirname, "..", "tools", "7z", "7za.exe")` 硬编码了 Windows PE 可执行文件路径，在 Linux/WSL 上该 .exe 文件可能无法通过 binfmt_misc 运行
  - **修复**：新增 `find7zBinary()` 函数，实现跨平台动态查找 7z 可执行文件
    - **Windows 平台**：优先项目内置 `7za.exe`，回退系统 PATH 中的 `7z`/`7za`
    - **Linux/WSL 平台**：优先系统 `7z`/`7za`（通过 `which` 验证），回退项目内置 `7za.exe`
    - 使用模块级缓存避免每次解压重复扫描
  - **影响范围**：所有 PDF/Word/ZIP 文件上传流程（均需调用 extractZip 解压）

- **Bug 2：`POSTbook.js` 使用 `doc2xResult.zipPath` 但 `doc2x.js` 返回 `downloadUrl`**
  - **问题**：`doc2x.convertPdfToMarkdown()` 返回 `{ downloadUrl: "https://..." }`（OSS 预签名下载链接），但 `POSTbook.js` 第 211/255 行检查 `doc2xResult.zipPath` 并将它作为本地文件路径传给 `extractZip`
  - **修复**：
    - 在 `POSTbook.js` 中新增 `downloadFile()` 函数，支持 HTTP/HTTPS 文件下载（含 302 重定向处理、超时控制）
    - PDF 分支：增加下载步骤 `doc2x → download zip → extractZip`，使用 `downloadUrl` 替换 `zipPath`
    - Word 分支：同上
  - **影响范围**：PDF/Word 文件上传（MD 文件和压缩包不受影响）

### 技术细节
- WSL 中 `p7zip-full` 已安装（`/usr/bin/7z`、`/usr/bin/7za`），`find7zBinary()` 优先使用系统原生二进制
- Doc2x API 返回的 `downloadUrl` 是 OSS 预签名 URL（HTTPS），`downloadFile()` 自动处理协议和重定向
- 后端已在 WSL 中成功重启（PID 1230，端口 8888），API 健康检查通过

---

## 2026-07-06 — 修复文件名编码乱码

### 修复
- **`node-jinmao/API/POSTbook.js`**：在 multer 回调中添加文件名编码修复逻辑
  - **问题**：multer 底层 busboy 解析 `Content-Disposition` 头时，若浏览器未使用 RFC 5987 编码（`filename*=UTF-8''...`），会将 UTF-8 字节按 Latin-1 逐字节错误解码，导致中文文件名变成乱码（如 `闸门运行工30.pdf` → `é¸é¨è¿è¡å·¥30.pdf`）
  - **修复**：在 `upload.single("file")` 回调中，`next()` 之前插入 `Buffer.from(file.originalname, "latin1").toString("utf8")` 转换，将 Latin-1 错误解码的字符串恢复为正确的 UTF-8
  - **幂等性**：纯 ASCII 文件名不受影响（`latin1→utf8` 对 ASCII 是幂等的）

### 技术细节
- 修复位置在 [API/POSTbook.js](file:///f:/代码/金毛教你学重构版/node-jinmao/API/POSTbook.js) 第 220 行附近（multer 回调中 `next()` 之前）
- 修复点唯一：`file.originalname` 是传给 `uploadBook()` 的唯一文件名字段，入口处修复后下游所有代码自然获得正确编码
- 测试：15 个用例全覆盖（中文/中英混合/特殊字符/纯ASCII/带空格），全部通过

---

## 2026-07-06 — WSL 部署指南新增国内网络优化策略

### 修改
- **`WSL部署指南.md`**：新增「部署前必读：国内网络优化策略」章节，解决国内网络环境下下载卡死问题
  - **新增章节**（位于指南开头）：包含 6 个小节
    - 0.1 apt 源替换（Ubuntu 官方源 → 清华/中科大/阿里云镜像），自动检测版本代号
    - 0.2 npm 源替换（npmjs.org → npmmirror.com），含持久化验证
    - 0.3 raw.githubusercontent.com DNS 解析（hosts 修改 + jsdelivr CDN 备选）
    - 0.4 Node.js 二进制镜像（nvm 下载 node 包加速）
    - 0.5 Prisma Engine 二进制镜像（prisma generate 下载引擎加速）
    - 0.6 网络连通性测试脚本（一键检测所有镜像源可用性）
  - **章节交叉引用**：在 2.1(nvm)、2.2(MySQL)、5(Prisma)、6(npm install) 节添加「国内网络提示」引用块，AI 智能体执行到下载步骤时会看到警告
  - **故障排查扩展**：新增 10.7 节「网络下载卡死/超时」，含通用排查流程、按卡住位置对照表、WSL 网络重置终极方案
  - **快速命令参考更新**：「一键完整部署」步骤列表新增 `步骤0: 【国内必做】网络优化`

### 设计思路
- 所有网络优化策略集中在开头的独立章节，AI 智能体部署时**先读先做**，再进入常规步骤
- 每个下载相关章节有 `> 国内网络提示：...` 引用块，不改变原有步骤结构，仅增加条件提醒
- 提供多级备选方案（清华→中科大→阿里云），确保至少有一个源可用

---

## 2026-07-06 — 新增 WSL 部署指南

### 新增
- **`WSL部署指南.md`**（根目录）：AI 智能体专用的 WSL 环境部署操作指南
  - **背景**：此前尝试过脚本部署但局限性太大（无法处理环境差异、MySQL 未安装、端口冲突、依赖缺失等），改为 AI 智能体自主部署
  - **内容结构**（10 个章节）：
    1. 前置条件检查（WSL 环境、发行版、基础工具）
    2. 环境依赖安装（Node.js 18+、MySQL 8.0、LibreOffice、unzip）
    3. 项目文件准备（通过 `/mnt/` 挂载 Windows 文件系统）
    4. 环境变量配置（`.env.example` → `.env`，引导填写 API Key）
    5. 数据库初始化（MySQL 建库 + Prisma migrate + generate）
    6. 依赖安装（后端 + 前端 `npm install`）
    7. 服务启动（nohup 后台运行 + PID 文件管理 + 端口等待）
    8. 部署验证（curl 健康检查）
    9. 自动重启策略（存活检测、停止、完整重启、单服务重启、崩溃恢复）
    10. 常见问题排查（端口占用、MySQL 连接、Prisma 迁移、npm install、LibreOffice）
  - **关键决策**：
    - 不写 bash 脚本，提供步骤性指令和决策逻辑
    - 使用 `nohup` + PID 文件管理进程，不引入 pm2/systemd
    - WSL 推荐从 `/mnt/` 访问 Windows 文件系统，避免重复 clone
  - **关联更新**：FILE.md 新增文件索引条目

---

## 2026-07-06 — 拆分 API/book.js 超大文件（模块化重构）

### 修改内容
`API/book.js` 共 702 行（非注释行约 317 行），超过项目规定的 300 行硬性上限。按照高内聚低耦合原则，参照 `service/auth/` 的成功拆分模式，将 4 个路由端点拆分为独立文件。

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `API/book.js` | **删除** | 原 702 行文件，内容已拆分到 `API/book/` 目录 |
| `API/book/index.js` | **新建**（24行） | 统一入口，组合 4 个子路由模块，保持 `require("./API/book")` 向后兼容 |
| `API/book/list.js` | **新建**（193行） | GET /api/v1/books — 教材列表路由（分页+关键词搜索+BigInt转换） |
| `API/book/detail.js` | **新建**（241行） | GET /api/v1/books/:id — 教材详情路由（ID校验+权限校验+完整章节列表+BigInt转换） |
| `API/book/update.js` | **新建**（128行） | PUT /api/v1/books/:id — 更新教材路由（存根，待实现） |
| `API/book/delete.js` | **新建**（194行） | DELETE /api/v1/books/:id — 删除教材路由（参数校验→查询教材→权限校验→软删除） |
| `config/swagger.js` | **修改** | `apis` 扫描路径增加 `"./API/book/*.js"`，确保子目录中的 OpenAPI 注释也被 swagger-jsdoc 扫描 |
| `app.js` | **无需修改** | `require("./API/book")` 自动解析到 `API/book/index.js` |
| `FILE.md` | **修改** | 更新 `API/book.js` 条目为 `API/book/` 目录结构说明 |

### 技术架构

```
API/book/
├── index.js     ← 统一入口（组合子路由，保持向后兼容）
├── list.js      ← GET /books 列表（分页+搜索）
├── detail.js    ← GET /books/:id 详情（含章节列表+权限）
├── update.js    ← PUT /books/:id 更新（存根）
└── delete.js    ← DELETE /books/:id 删除（软删除+权限）
```

**拆分后各文件均在 300 行以内。**

### 向后兼容性
- **`app.js` 零改动**：Node.js 模块解析自动路由到 `index.js`
- **API 端点路径完全不变**：所有端点 `/books`、`/books/:id` 保持不变
- **OpenAPI 文档不变**：Scalar UI 中接口列表和文档完全不变
- **控制台日志前缀微调**：`[API_book]` → 细分为 `[API_book_list]`/`[API_book_detail]`/`[API_book_update]`/`[API_book_delete]`

---

## 2026-07-06 — 实现删除教材 API

### 修改内容
将 `API/book.js` 中的 `DELETE /api/v1/books/:id` 端点从 TODO 占位逻辑替换为实际实现（软删除 + 所有者权限校验），并完善 OpenAPI JSDoc 注释。

### 修改文件
- **`API/book.js`** — 实现 DELETE 路由处理函数（参数校验 → 查询教材 → 权限校验 → 软删除 → 标准响应），更新 OpenAPI 注释（补充 data 字段和 400 响应，去掉 summary 中的"待实现"），更新文件头注释

### 技术细节
- 软删除：调用 `bookRepo.softDeleteCourse()`，标记 `isDeleted=true`，不删除 MinIO 文件
- 权限：仅教材所有者（`course.userId === req.userId`）可删除，非所有者返回 403
- 响应码：200（成功）、400（ID 格式无效）、401（未认证）、403（越权）、404（不存在）、500（服务端错误）
- 幂等：已删除的教材再删返回 404（因 `getCourseById` 过滤了 `isDeleted=true` 的记录）
- 参考 `GET /books/:id` 的校验模式，保持代码风格一致

---

## 2026-07-06 — 实现教材列表和详情 API

### 新增
- **`GET /api/v1/books`** 接口：获取用户教材列表，支持分页（page/pageSize）和关键词搜索（keyword），按创建时间倒序排列，列表项返回 `chapterCount` 字段替代完整章节列表
- **`GET /api/v1/books/:id`** 接口：获取教材详情（含完整章节列表），包含权限校验（只能查看自己的教材），ID 格式校验（仅允许纯数字）

### 修改
- **`node-jinmao/API/book.js`**：将列表和详情的 TODO 占位逻辑替换为实际实现，新增 `bookRepo` 导入，添加参数校验、BigInt→String 转换、权限校验等完整逻辑
- **新增 `开发日志.md`**（根目录）— 项目整体开发日志
- **新增 `FILE.md`**（根目录）— 项目文件索引

### 技术细节
- 列表接口参数校验：`page` 默认 1（NaN→1），`pageSize` 默认 10（上限 50）
- 详情接口鉴权：校验 `course.userId === req.userId`（BigInt 转 String 比较），跨用户返回 403
- 详情接口格式校验：ID 必须为纯数字字符串，否则返回 400
- BigInt 全量转 String：防止前端精度丢失
- 响应格式遵循项目规范：成功 `{ code: 0, data: {...} }`，失败 `{ code: 4xx/5xx, message: "..." }`

---

## 2026-07-06 — [前端] — 首页完整设计与实现

### 修改原因
后端教材列表、详情、删除 API 已实现，需要前端首页展示教材列表、支持上传/删除教材、以及后续功能的骨架占位。参考 `F:\代码\金毛教你学` 的布局设计（侧边栏 + 顶部栏 + 内容区）。

### 新增/修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `WEB/src/pages/home/index.vue` | 新建 | 首页模板：全屏flex三块式布局（侧边栏 + 顶部栏 + 内容区） |
| `WEB/src/pages/home/script.js` | 新建 | 首页逻辑：教材列表加载、上传/删除、状态轮询、用户信息 |
| `WEB/src/components/HomeSidebar.vue` | 新建 | 左侧侧边栏：品牌LOGO、导航菜单（骨架占位）、用户信息、上传按钮 |
| `WEB/src/components/HomeTopbar.vue` | 新建 | 顶部栏：搜索框（骨架占位）、主题切换、用户头像、退出按钮 |
| `WEB/src/components/CourseCard.vue` | 新建 | 教材卡片：封面渐变、状态标签、名称、描述、章节数、操作按钮 |
| `WEB/src/components/UploadBookDialog.vue` | 新建 | 上传弹窗：el-upload拖拽、格式校验、名称/描述输入、loading防重复 |
| `WEB/src/App.vue` | 修改 | 替换占位主页视图为 `<HomePage v-else />` |
| `WEB/src/styles/tokens.css` | 修改 | html 过渡时间 200ms → 500ms（遵守设计规范规则3） |

### 技术要点
- **布局**：全屏 flex 行布局（无 max-w 限制），侧边栏 252px 固定宽，<860px 自动收缩至 84px
- **API 对接**：`listBooks()`（GET /books，code:0）、`uploadBook()`（POST /book/upload）、`getBookStatus()`（GET /book/:id/status）、`getProfile()`（GET /auth/profile）
- **状态轮询**：有处理中的教材时每 3s 自动刷新列表，全部完成自动停止
- **骨架屏**：加载中显示 6 个自定义 animate-pulse 占位卡片
- **空状态**：无教材时显示虚线边框 + SVG 空图标提示
- **设计规范**：所有组件严格遵守 text-black/dark:text-white、rounded-[10px]、duration-500、暗黑双轨适配

### 后续计划
- 学习概览、学习统计区域（当前为骨架占位）
- 教材详情页（当前点击查看显示"即将上线"提示）
- 搜索功能（当前搜索框为 disabled 骨架）

---

## 2026-07-06 — [前端] — 登录页视觉美化设计

### 修改原因
登录页已完成 design-spec 结构性重构（Element Plus + dark: 双轨），但视觉上较为朴素，缺乏品牌感和视觉层次。

### 美化内容

| 美化点 | 实现方式 |
|--------|----------|
| 背景装饰 | 两个大尺寸蓝色渐变光晕（右上角 + 左下角），暗黑模式下透明度更高 |
| Logo 品牌区 | 蓝色圆形背景 + 书本 SVG 图标，带蓝色阴影光晕 |
| 卡片装饰 | 顶部蓝色渐变装饰条（`h-1`）+ 标题下蓝色分割线 |
| 输入框图标 | `el-input` 的 `#prefix` 插槽：邮箱图标（Message）+ 锁图标（Lock） |
| 表单分区 | 邮箱区和验证码区各自用浅灰背景 `bg-gray-50` 包裹，视觉分区清晰 |
| 入场动画 | 卡片 fade-in + slide-up（500ms），内联 `<style scoped>` 实现 |
| 响应式按钮 | 手机端竖排（按钮在输入框下方全宽）、桌面端横排 |
| 按钮统一 | 登录按钮改为 `type="primary"` 蓝色，与整体风格统一 |

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `WEB/src/pages/login/index.vue` | 重写 | 全面美化：背景装饰、Logo区、卡片装饰、输入框图标、入场动画 |
| `WEB/package.json` | 修改 | 新增依赖 `@element-plus/icons-vue`（Element Plus 官方图标库） |

### 不修改的文件
- `script.js` — 逻辑无变化，仅模板美化

---

## 2026-07-05 — 教材上传到第一章课程生成 — 完整实施

### 修改内容

实现从用户上传教材文件到第一章课程全部产物生成的完整功能链路。新增 7 个文件，修改 2 个文件，安装 2 个新依赖。涵盖数据库层、Repository 层、Service 层、API 层和流水线调度器。

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `prisma/schema.prisma` | **修改** | 新增 Course 模型（13 字段）和 Chapter 模型（14 字段），User 模型追加虚拟关系字段 courses |
| `config/minio_config.json` | **新建** | MinIO 连接配置（127.0.0.1:9000, jinmao bucket） |
| `utils/repo/book_repo.js` | **新建**（250行） | Course Repository：createCourse、getCourseById（含chapters）、listCoursesByUser（分页+搜索）、updateCourse、updateEndline、updatePipelineStatus、softDeleteCourse |
| `utils/repo/chapter_repo.js` | **新建**（190行） | Chapter Repository：createChapter、getChapterById、listChaptersByCourse、updateChapter、updateChapterTotalPages（outlinePath+totaLPages）、softDeleteChapter |
| `service/POSTbook.js` | **新建**（268行） | 教材上传核心服务：输入校验 → 创建 Course 记录 → 上传源文件到 MinIO → 格式归一化（4分支：MD/压缩包/PDF/Word）→ 异步启动流水线 |
| `API/POSTbook.js` | **新建**（410行） | 教材上传 + 状态查询路由：POST /api/v1/book/upload（multer+JWT）、GET /api/v1/book/:book_id/status（轮询流水线进度），含完整 OpenAPI 注解 |
| `API/book.js` | **新建**（214行） | 教材 CRUD 占位路由：GET/PUT/DELETE /api/v1/books，均返回"待实现" |
| `service/course_pipeline.js` | **新建**（661行，特批超300行） | 6阶段课程生成流水线：数据获取→文本提取+行号识别→大纲生成+条件扩写→PPT生成(并发5)→TTS生成(并发3)→完整性校验，使用 p-queue 并发控制 |
| `app.js` | **修改** | 新增 POSTbookRouter 和 bookRouter 导入与挂载 |
| `FILE.md` | **更新** | 新增 6 个文件索引条目 |

### 技术架构

```
用户 → POST /api/v1/book/upload → API/POSTbook.js → service/POSTbook.js
                                                        │
                        ┌───────────────────────────────┤
                        ▼                               ▼
                  MinIO 存储                      MySQL (Prisma)
                  /usercourse/{uid}/{cid}/        Course / Chapter 表
                        │
                        ▼
              service/course_pipeline.js
              Phase 1→2→3→4∥5→6
```

### 新增 API 端点

| 方法 | 端点 | 状态 | 说明 |
|------|------|:---:|------|
| POST | /api/v1/book/upload | 已实现 | multer 文件上传 + JWT 鉴权 + 格式归一化 |
| GET | /api/v1/book/:book_id/status | 已实现 | 轮询流水线进度（pipelineStatus + chapter 信息） |
| GET | /api/v1/books | 占位 | 教材列表 |
| GET | /api/v1/books/:id | 占位 | 教材详情 |
| PUT | /api/v1/books/:id | 占位 | 更新教材 |
| DELETE | /api/v1/books/:id | 占位 | 删除教材 |

### 设计决策

1. **MinIO 目录结构**：采用 `/usercourse/{userid}/{bookid}/` 方案 A（资源类型顶层隔离，便于未来扩展 /bank/ 题库）
2. **文本细化可选**：用户上传时通过 `elaboration` 表单字段控制，存入 Course.elaborationEnabled
3. **流水线异步执行**：POST /upload 返回后通过 setTimeout 异步启动 pipeline，不阻塞 HTTP 响应
4. **PPT/TTS 并行**：Phase 4 和 Phase 5 使用 Promise.all 并行启动，p-queue 控制各自并发

### 安装依赖

```bash
npm install p-queue multer
```

---

## 2026-07-05 — 实现教材上传与状态查询 API（API/POSTbook.js、API/book.js）

### 修改内容

实现教材相关的完整 API 路由层，包含教材上传（multer 文件处理 + JWT 鉴权）、处理状态查询，以及预留的 CRUD 占位端点。遵循项目现有的 Router 模式、中间件模式、OpenAPI 注解规范和错误处理风格。

### 新增文件

| 文件 | 说明 |
|------|------|
| `API/book.js` | 教材 CRUD 路由（占位，214 行）：4 个端点 GET/PUT/DELETE /api/v1/books，均返回 "待实现"，含完整 OpenAPI 注解 |

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `API/POSTbook.js` | **重写**：从 49 行注释存根 → 410 行完整实现，包含：(1) multer 配置文件上传处理（dest=os.tmpdir()，500MB 限制，fileFilter 白名单 pdf/docx/doc/md/zip/rar/7z）；(2) POST /api/v1/book/upload 端点（JWT 鉴权 → multer 单文件上传 → 解析 form 字段 → 调用 uploadBook Service）；(3) GET /api/v1/book/:book_id/status 端点（JWT 鉴权 → bookRepo.getCourseById → 格式化返回 pipeline_status + chapter 信息）；(4) 自定义 multer 错误处理函数 handleMulterError；(5) 完整 OpenAPI 注解 |
| `app.js` | 新增 `POSTbookRouter` 和 `bookRouter` 的 import 与 mount（`app.use("/api/v1", ...)`）；更新启动日志列出 5 个新端点 |
| `package.json` | 新增 `multer` 依赖（multipart/form-data 文件上传处理中间件） |
| `FILE.md` | 新增 `API/book.js` 条目，更新 `API/POSTbook.js`、`app.js`、`package.json` 的用途描述和修改时间 |

### 技术架构

```
POST /api/v1/book/upload → API/POSTbook.js (路由层)
  ├── middleware/auth.js → authenticateToken (提取Bearer Token → 验证 → 注入 req.userId)
  ├── multer → fileFilter (扩展名白名单校验) → limits.fileSize (500MB限制) → dest (os.tmpdir())
  ├── handleMulterError (自定义 multer 错误 → 422 Unprocessable Entity)
  └── service/POSTbook.js → uploadBook(userId, file, name, description, elaborationEnabled)
        ├── book_repo.js → createCourse (数据库记录)
        ├── upload_minio.js → upload (源文件上传 MinIO)
        ├── 格式归一化 4 分支 (MD/ZIP/PDF/Word)
        ├── book_repo.js → updateCourse (更新路径信息)
        └── course_pipeline.js → pipeline (异步启动流水线)

GET /api/v1/book/:book_id/status → API/POSTbook.js (路由层)
  ├── middleware/auth.js → authenticateToken
  └── book_repo.js → getCourseById(book_id)
        ├── 返回 pipeline_status + elaboration_enabled
        └── 返回第一个章节信息 { chapter_id, name, status, total_pages } 或 null

GET/PUT/DELETE /api/v1/books[ /:id] → API/book.js (路由层，占位)
  └── authenticateToken → 返回 { code: 0, message: "待实现" }
```

### API 端点一览

| 方法 | 端点 | 状态 | 说明 |
|------|------|------|------|
| POST | /api/v1/book/upload | **已实现** | 上传教材文件，multer 处理 multipart/form-data，调用 Service 层格式归一化 + 启动流水线 |
| GET | /api/v1/book/:book_id/status | **已实现** | 查询教材流水线处理状态和章节信息 |
| GET | /api/v1/books | 占位 | 获取用户教材列表（分页 + 关键词搜索） |
| GET | /api/v1/books/:id | 占位 | 获取教材详情 |
| PUT | /api/v1/books/:id | 占位 | 更新教材信息 |
| DELETE | /api/v1/books/:id | 占位 | 软删除教材 |

### Multer 错误码映射

| multer 错误码 | HTTP 状态码 | 业务 code | 说明 |
|--------------|-----------|----------|------|
| `LIMIT_FILE_SIZE` | 422 | 422 | 文件大小超过 500MB 限制 |
| `LIMIT_UNEXPECTED_FILE` (自定义) | 422 | 422 | 文件扩展名不在白名单中（pdf/docx/doc/md/zip/rar/7z） |
| 其他 multer 错误 | 422 | 422 | 文件上传错误（通用） |
| 无文件 (`req.file` 为空) | 422 | 422 | 未提供文件或字段名不是 "file" |

### 测试结果

11 项测试全部通过：
- book.js 4 个 CRUD 端点：无 Token → 401，无效 Token → 401
- POSTbook.js 上传/状态端点：无 Token → 401，无效 Token → 401
- 不存在的路由 → 404（含正确的 JSON 响应格式）

---

## 2026-07-05 — 实现 course_pipeline.js：6阶段课程生成流水线

### 修改内容

**重写文件：`service/course_pipeline.js`（由 182 行设计文档 → 完整实现，661 行含注释）**

完整的6阶段课程生成流水线，接收课程 ID 后自动完成从教材文本到 PPT+语音+字幕的全流程生成。文件头部添加了 `// 本文件因流水线编排逻辑复杂且不可分割，特批允许超过 300 行限制` 声明。

**6个阶段概览：**

| 阶段 | 函数 | 功能 |
|------|------|------|
| Phase 1 | `phase1_fetchData` | 数据库查询课程信息 + MinIO 下载归一化 MD 文件 |
| Phase 2 | `phase2_extractAndIndex` | extractLines 提取文本 → addLineNumbers 行号索引 → getLine AI 识别章节起止行 → 创建 Chapter 记录 |
| Phase 3 | `phase3_generateCourse` | generateOutline AI 生成大纲 → 条件性 elaborateText 扩写口播稿 → 上传 outline.json 到 MinIO |
| Phase 4 | `phase4_generatePpt` | p-queue 并发5 → generateHtmlPpt AI 生成 HTML PPT → 后处理 `\n` 还原 → 上传 MinIO |
| Phase 5 | `phase5_generateTts` | p-queue 并发3 → synthesize TTS 合成 MP3+SRT → 上传 MinIO → 清理临时文件 |
| Phase 6 | `phase6_validate` | minioClient.statObject 逐文件检查完整性 → 更新 completed/partial_completed 状态 |

**核心特性：**

1. **Phase 4 & 5 并行执行**：PPT 和 TTS 在 Phase 3 完成后同时启动（`Promise.all`），互不等待，大幅缩短总耗时
2. **并发控制**：Phase 4 PPT 队列 `concurrency=5`，Phase 5 TTS 队列 `concurrency=3`，避免 API 过载
3. **容错隔离**：单个 slide 的 PPT/TTS 失败不影响其他 slide，catch 异常后继续处理下一个
4. **MinIO 独立客户端**：流水线使用独立初始化的 `minioClient`（环境变量配置），与 `upload_minio.js` 的配置互相独立
5. **临时文件清理**：每个阶段使用 `os.tmpdir()` 存储中间文件，阶段完成后自动清理
6. **详细控制台日志**：每个阶段记录开始/结束时的时间戳和耗时，格式为 `[course_pipeline][PhaseN]`

**状态机流转：**

```
idle → 1000_extracting → 1000_extracted → 1000_indexing → 1000_indexed
→ getting_line → get_line_done → course_generating → course_generated
→ ppt_generating + tts_generating (并行) → ppt_generated + tts_generated
→ data_validating → completed / partial_completed / failed
```

**新增依赖：** `p-queue@8.1.0`（并发任务队列）

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `service/course_pipeline.js` | 重写 | 完整实现 6 阶段流水线（661 行含注释） |
| `package.json` | 更新 | 新增 `p-queue` 依赖 |
| `FILE.md` | 更新 | 更新 course_pipeline.js 的用途描述 |

---

## 2026-07-05 — 拆分 service/auth.js（高内聚低耦合模块化重构）

### 修改内容

`service/auth.js` 原有 593 行（含注释），超过项目规定的 300 行限制。按照高内聚低耦合原则将其拆分为 3 个业务模块 + 1 个统一导出入口，**同时消除 login 和 updateProfile 中重复的验证码校验逻辑**（提取为 `verifyAndConsumeOtp` 公共函数）。

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `service/auth.js` | **删除** | 原 593 行文件，内容已拆分 |
| `service/auth/otp.js` | **新建**（257行） | 验证码核心模块：常量、otpStore Map、清理定时器、createTransporter、sendCode、**新增 verifyAndConsumeOtp** 公共函数 |
| `service/auth/login.js` | **新建**（132行） | 登录/注册模块：login 函数，内部改用 verifyAndConsumeOtp 替代手动 otpStore 操作 |
| `service/auth/profile.js` | **新建**（220行） | 用户资料模块：getProfile、updateProfile，updateProfile 内部改用 verifyAndConsumeOtp |
| `service/auth/index.js` | **新建**（16行） | 统一导出入口，聚合 4 个函数，保持 `require("../service/auth")` 向后兼容 |
| `API/auth.js` | **无需修改** | Node.js 自动解析 `service/auth/index.js` |
| `FILE.md` | **更新** | 更新 service/ 目录结构（auth.js → auth/ 目录下 4 个文件）、文件索引表 |

### 技术架构

```
service/auth/
├── index.js     ← 统一导出（sendCode, login, getProfile, updateProfile）
├── otp.js       ← 验证码核心（存储、发送、校验、清理）
├── login.js     ← 登录业务（调用 otp.verifyAndConsumeOtp）
└── profile.js   ← 用户资料业务（调用 otp.verifyAndConsumeOtp）
```

**拆分后各文件均在 300 行以内。**

### 改进亮点

1. **消除代码重复**：login 和 updateProfile 中 ~25 行几乎完全相同的验证码校验逻辑，提取为 `verifyAndConsumeOtp(email, code)` 公共函数
2. **高内聚低耦合**：每个子模块职责单一明确
   - `otp.js`：只管验证码生命周期
   - `login.js`：只管登录/注册流程
   - `profile.js`：只管用户资料 CRUD
3. **向后兼容**：`API/auth.js` 零改动，Node.js 模块解析自动路由到 `index.js`

---

## 2026-07-05 — 实现 PUT /api/v1/auth/profile 更新用户信息 API（邮箱验证码验证）

### 修改内容

实现 `PUT /api/v1/auth/profile` 接口，用户携带 JWT Bearer Token 和邮箱验证码可更新自己的个人信息（昵称、手机号、密码）。与旧 API 文档中的 `old_password` 方案不同，本实现使用邮箱验证码作为身份验证手段，安全性更高。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `utils/repo/user_repo.js` | 新增 `updateProfile(userId, fields)` 函数（~45行），按用户 ID 更新指定字段，处理 Prisma P2025 错误 |
| `service/auth.js` | 新增 `updateProfile(userId, body)` 函数（~170行），核心逻辑：输入校验 → 白名单过滤字段 → 查用户 → 验 isBanned → 验邮箱验证码 → 更新数据库 → 格式化返回 |
| `API/auth.js` | 新增 `PUT /auth/profile` 路由 + 完整 OpenAPI 注解（~135行），引入 authenticateToken 中间件，更新文件头端点列表 |
| `API文档.md` | 更新 1.4 节，将 `old_password` 验证改为 `code` 邮箱验证码方式，添加安全机制说明和完整响应示例 |
| `FILE.md` | 更新 API/auth.js、service/auth.js、utils/repo/user_repo.js 的用途描述 |

### 安全机制

1. **邮箱验证码验证**：用户每次修改信息都需通过验证码验证身份，验证码发到注册邮箱，只有本人能收到
2. **验证码一次性使用**：验证通过后立即从内存中删除，防止重放攻击
3. **字段白名单**：仅允许更新 `nickname`、`phone`、`password`，`username` 和 `email` 不可通过此接口修改
4. **复用现有基础设施**：验证码存储/发送/频率限制复用 `sendCode()` 的 `otpStore` Map 和 nodemailer 配置

### 技术架构

```
PUT /api/v1/auth/profile → API/auth.js (路由层)
  ├── middleware/auth.js → authenticateToken (提取Bearer Token → 验证 → 注入 req.userId)
  └── service/auth.js → updateProfile(userId, body)
        ├── input_validator.js → validateFields (输入校验)
        ├── user_repo.js → findById (查用户 → 获取邮箱 → 检查 isBanned)
        ├── otpStore Map → 验证邮箱验证码 (正确性 + 有效期 → 删除)
        ├── user_repo.js → updateProfile (更新数据库)
        └── 格式化返回 (排除敏感字段，BigInt→String)
```

### 请求示例

```json
{
    "nickname": "张三（新）",
    "code": "123456"
}
```

### 响应示例

```json
{
    "code": 200,
    "message": "个人信息更新成功。",
    "data": {
        "id": "1",
        "username": null,
        "nickname": "张三（新）",
        "email": "zhangsan@example.com",
        "phone": null,
        "role": "user",
        "create_time": "2026-07-01T08:00:00.000Z",
        "update_time": "2026-07-05T10:30:00.000Z"
    }
}
```

---

## 2026-07-05 — 实现 GET /api/v1/auth/profile 获取当前用户信息 API

### 修改内容

实现 `GET /api/v1/auth/profile` 接口，用户携带 JWT Bearer Token 可获取自己的个人信息（ID、用户名、昵称、邮箱、手机号、角色、注册时间）。按项目分层架构，依次在 Repo / Service / API 三层添加对应实现，复用已有 `authenticateToken` 中间件进行身份验证。

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `utils/repo/user_repo.js` | 新增 `findById(userId)` 函数（~40行），按用户 ID 查询用户，过滤 isDeleted 用户，userId 自动 String→BigInt 转换 |
| `service/auth.js` | 新增 `getProfile(userId)` 函数（~56行），调用 findById → 检查 isBanned → 排除 password 字段 → 格式化返回 { id, username, nickname, email, phone, role, create_time } |
| `API/auth.js` | 新增 `GET /auth/profile` 路由 + OpenAPI 注解（~90行），引入 authenticateToken 中间件，更新文件头端点列表 |
| `API文档.md` | 1.3 节标题更新为【已实现】【测试通过】 |
| `FILE.md` | 更新 API/auth.js、service/auth.js、utils/repo/user_repo.js 的用途描述和最后修改时间 |

### 补充修复：swagger.js 缺少 securitySchemes 定义

**问题**：Scalar UI 中 `GET /api/v1/auth/profile` 接口没有输入 Token 的地方，因为 `config/swagger.js` 的 `definition` 中缺少 `components.securitySchemes` 定义。

**修复**：在 `definition` 中新增 `components.securitySchemes.bearerAuth`，类型为 `http` + `bearer`。Scalar UI 检测到此定义后，会在页面顶部显示 "Authorize" 锁按钮，点击后可输入 JWT Token。

### 技术架构

```
GET /api/v1/auth/profile → API/auth.js (路由层)
  ├── middleware/auth.js → authenticateToken (提取Bearer Token → 验证 → 注入 req.userId)
  └── service/auth.js → getProfile(userId)
        ├── user_repo.js → findById(userId) → Prisma → MySQL (查User表，过滤isDeleted)
        ├── 检查 isBanned → 被禁用返回 403
        └── 格式化返回（排除password，BigInt→String）
```

### 响应示例

```json
{
    "code": 200,
    "message": "ok",
    "data": {
        "id": "1",
        "username": "zhangsan",
        "nickname": "张三",
        "email": "zhangsan@example.com",
        "phone": "13800138000",
        "role": "user",
        "create_time": "2026-07-01T08:00:00.000Z"
    }
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | string | 用户 ID（BigInt 转 String 防止精度丢失） |
| `username` | string/null | 用户名（验证码注册用户为 null） |
| `nickname` | string/null | 昵称 |
| `email` | string | 邮箱（一定存在） |
| `phone` | string/null | 手机号 |
| `role` | string | 角色：admin / user |
| `create_time` | string | 注册时间（ISO 8601） |

### 错误码

| code | HTTP 状态码 | 说明 |
|------|-----------|------|
| `200` | 200 | 获取成功 |
| `401` | 401 | Token 缺失/无效/过期（由中间件返回） |
| `403` | 403 | 账号已被禁用 |
| `404` | 404 | 用户不存在（已删除或不存在） |
| `500` | 500 | 服务器内部错误 |

---

## 2026-07-04 — 集成 Scalar API Reference 交互式文档（swagger-jsdoc + @scalar/express-api-reference）

### 修改内容

集成现代化 API 文档方案：swagger-jsdoc 从 JSDoc 注释生成 OpenAPI 3.0 规范，@scalar/express-api-reference 渲染现代 UI 页面。访问 `http://localhost:8888/api/v1/docs` 即可查看和在线测试所有接口。

**方案选择**：对比了 swagger-ui-express（经典UI）和 @scalar/express-api-reference（现代UI），选择了后者，因为 UI 更美观、支持暗色主题、体验更流畅。

### 新增文件

| 文件 | 说明 |
|------|------|
| `config/swagger.js` | OpenAPI 配置，定义 API 元信息、服务器地址、标签分组，扫描 `API/*.js` 的 `@openapi` 注释生成规范 |

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `API/auth.js` | 为 POST /smtpcode 和 POST /login 添加 `@openapi` JSDoc 注解（请求体/响应体/错误码 schema），纯注释，不改业务逻辑 |
| `app.js` | 引入 Scalar 中间件和 swaggerSpec；新增 `/api/v1/docs`（Scalar UI）和 `/api/v1/docs/json`（OpenAPI JSON）路由；启动日志新增文档地址 |
| `package.json` | 新增 swagger-jsdoc、@scalar/express-api-reference 依赖 |
| `FILE.md` | 目录树和说明表新增 `config/swagger.js` 条目 |

### 技术架构

```
API/auth.js (JSDoc @openapi 注释)
        │
        ▼
config/swagger.js (swagger-jsdoc 扫描 → 生成 OpenAPI 3.0 spec)
        │
        ▼
app.js (@scalar/express-api-reference 渲染 UI)
        │
        ▼
http://localhost:8888/api/v1/docs (交互式文档页面)
```

### 文档页面功能

- 展开/折叠接口详情、在线填写参数并发送测试请求（Try it）
- 查看请求/响应示例、多语言代码生成（curl/Python/JS 等）
- 暗色/亮色主题切换

### 邮箱验证码登录/注册模块

在 `node-jinmao` 项目中构建基于邮箱验证码的登录/注册模块。用户只需输入邮箱获取验证码，后台自动判断新用户注册还是老用户登录。

**API 端点**：
- `POST /api/v1/smtpcode` — 发送邮箱验证码（频率限制：5分钟≤3次，有效期10分钟）
- `POST /api/v1/login` — 验证码登录/注册（验证码一次性使用，自动判断注册/登录，返回JWT Token + is_new_user标识）

**OTP 方案决策**：评估了 `express-email-verify-secure`（周下载10次/仅1个版本，成熟度太低）vs 手写方案，最终选择 **nodemailer + 自建OTP**。nodemailer是Node.js邮件发送事实标准（周下载200万+），手写验证码逻辑仅60-80行，完全可控。

### 新增文件

| 文件 | 说明 |
|------|------|
| `prisma/schema.prisma` | 数据库Schema定义（User模型，对应MySQL User表） |
| `utils/prisma.js` | Prisma Client全局单例实例 |
| `utils/jwt.js` | JWT Token工具：generateToken / verifyToken / extractBearer |
| `utils/repo/user_repo.js` | 用户Repository：findByEmail / createByEmail，默认过滤isDeleted |
| `service/auth.js` | 认证业务逻辑：sendCode（nodemailer发送 + 内存Map存储验证码）、login（验证码校验 + 自动注册/登录 + JWT签发） |
| `middleware/auth.js` | JWT鉴权Express中间件（仅验证Token，不检查用户状态） |
| `API/auth.js` | Auth路由（Express Router）：POST /smtpcode、POST /login |

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `app.js` | **重构**：原生http模块 → Express 4 服务器，集成 helmet/cors/JSON解析(1MB限制)/请求日志/404处理/全局错误处理/优雅关闭 |
| `package.json` | 新增依赖：express、nodemailer、cors、helmet、@prisma/client、jsonwebtoken、prisma(dev) |
| `.env.example` | 新增配置节：DATABASE_URL（MySQL连接）、JWT_SECRET/JWT_EXPIRES_IN（JWT配置）、SMTP_HOST/PORT/USER/PASS/FROM（邮件配置） |
| `start.ps1` | 新增 Prisma 初始化步骤（prisma generate + prisma migrate deploy），放在 npm install 之后、node app.js 之前 |
| `FILE.md` | 新增7个文件索引条目（prisma/schema.prisma、prisma.js、jwt.js、user_repo.js、auth.js服务层、auth.js中间件、auth.js路由层），更新 app.js、package.json 描述 |

### 技术架构

```
HTTP Request → app.js (Express + dotenv + helmet + cors)
  ├── POST /api/v1/smtpcode → API/auth.js → service/auth.js.sendCode()
  │     ├── input_validator.js (邮箱格式校验)
  │     ├── 内存Map (频率限制: 5分钟≤3次)
  │     ├── crypto.randomInt() (生成6位验证码)
  │     └── nodemailer (SMTP发送邮件)
  │
  ├── POST /api/v1/login → API/auth.js → service/auth.js.login()
  │     ├── input_validator.js (邮箱+验证码格式校验)
  │     ├── 内存Map (验证码查找/匹配/删除)
  │     ├── user_repo.js → Prisma (查用户/创建新用户)
  │     └── jwt.js (JWT Token签发)
  │
  └── 其他路径 → 404 JSON
```

### 技术决策

| 项目 | 决策 | 原因 |
|------|------|------|
| Web框架 | Express 4 | 路由管理清晰，中间件生态丰富 |
| 邮件发送 | nodemailer | 周下载量200万+，Node.js邮件发送事实标准 |
| OTP方案 | 手写（拒绝express-email-verify-secure） | 该库仅1个版本/周下载10次，不可用 |
| OTP存储 | 内存Map | 有效期仅10分钟，重启丢失影响极小 |
| 数据库 | Prisma 6 + MySQL | Schema集中管理，类型安全 |
| 认证 | JWT（jsonwebtoken） | 无状态，避免每次查数据库 |

---

## 2026-07-04 — 安全加固：敏感配置迁移到 .env + dotenv

### 修改内容

**安全背景**：`config/deepseek_config.json`、`config/doc2x_config.json`、`config/volcengine_config.json` 中存在明文 API Key / 认证凭据，项目没有 `.gitignore` 保护，存在误提交到 git 的安全风险。

**方案**：敏感字段迁移到 `.env`（被 `.gitignore` 忽略），非敏感字段保留在 `config/*.json` 中，通过统一配置入口 `config/index.js` 合并。

#### 新建文件

| 文件 | 说明 |
|------|------|
| `.env` | 敏感凭据配置文件（含 DEEPSEEK_API_KEY 真实值、DOC2X_API_KEY、火山引擎 APP_ID/ACCESS_KEY、MinIO 凭据），已被 .gitignore 忽略 |
| `.env.example` | 配置模板文件，所有敏感值用占位符，可安全提交到 git，供新开发者参考 |
| `.gitignore` | Git 忽略规则：.env、node_modules/、data/output/、系统文件、IDE 配置 |
| `config/index.js` | **统一配置加载入口**：require 各 JSON 配置文件获取非敏感字段，用 process.env 注入敏感字段，导出 `{ deepseek, doc2x, volcengine }` 对象。所有业务模块统一通过此入口获取配置 |

#### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `package.json` | 新增依赖 `dotenv`（自动安装） |
| `app.js` | 在文件最顶部添加 `require("dotenv").config()`，确保所有模块加载前环境变量已注入 |
| `config/deepseek_config.json` | 移除 `DEEPSEEK_API_KEY` 字段（两个模型的 API_KEY 迁移到 .env 的 `DEEPSEEK_API_KEY` 环境变量） |
| `config/doc2x_config.json` | 移除 `DOC2X_API_KEY` 字段（迁移到 .env 的 `DOC2X_API_KEY` 环境变量） |
| `config/volcengine_config.json` | 移除 `APP_ID` 和 `ACCESS_KEY` 字段（迁移到 .env 的 `VOLCENGINE_TTS_APP_ID` / `VOLCENGINE_TTS_ACCESS_KEY`） |
| `utils/generate_outline.js` | 配置引用从 `require("../config/deepseek_config.json")` 改为 `const { deepseek } = require("../config")`，其余代码不变（变量名保持 `config`） |
| `utils/get_line.js` | 同上 |
| `utils/elaboration.js` | 同上 |
| `utils/htmlppt.js` | 同上 |
| `utils/doc2x.js` | 移除 `loadConfig()` 函数的 fs.readFileSync 加载逻辑和 `CONFIG_PATH` 常量，改为 `const { doc2x: doc2xConfig } = require("../config")`；main 函数中的配置校验保留（检查占位符 sk-xxx），改为从 doc2xConfig 直接读取 |
| `service/text_tts.js` | 移除 `loadConfig()` 函数和 `CONFIG_PATH` 常量，改为 `const { volcengine: volcengineConfig } = require("../config")`；synthesize 函数改为从 volcengineConfig.VOLCENGINE_TTS 直接读取配置 |
| `start.ps1` | 在依赖检查步骤前新增 `.env` 文件存在性检查，缺失时提示用户从 `.env.example` 复制 |
| `FILE.md` | 新增 `.env`、`.env.example`、`.gitignore`、`config/index.js` 条目，更新受影响的 JSON 配置文件说明 |

#### 配置迁移对照

| 敏感字段 | 迁移前位置 | 迁移后位置 |
|---------|----------|----------|
| DeepSeek API Key | `config/deepseek_config.json` → `DEEPSEEK_API_BIG.DEEPSEEK_API_KEY` | `.env` → `DEEPSEEK_API_KEY` |
| Doc2x API Key | `config/doc2x_config.json` → `DOC2X_API_KEY` | `.env` → `DOC2X_API_KEY` |
| 火山引擎 APP_ID | `config/volcengine_config.json` → `VOLCENGINE_TTS.APP_ID` | `.env` → `VOLCENGINE_TTS_APP_ID` |
| 火山引擎 ACCESS_KEY | `config/volcengine_config.json` → `VOLCENGINE_TTS.ACCESS_KEY` | `.env` → `VOLCENGINE_TTS_ACCESS_KEY` |
| MinIO Access Key | 尚未创建 | `.env` → `MINIO_ACCESS_KEY`（预留） |
| MinIO Secret Key | 尚未创建 | `.env` → `MINIO_SECRET_KEY`（预留） |

#### 不受影响的文件

- `config/prompt.json`：纯路径映射，不含敏感信息，保持不变
- `config/*.txt`：4 个 Prompt 模板文件，不含敏感信息，保持不变
- `utils/upload_minio.js`：引用尚未创建的 `minio_config.json`，本次暂不处理

---

## 2026-07-04 — LibreOffice Portable 便携版支持

### 修改内容

**背景**：原先 word2pdf 依赖系统级安装 libreoffice，部署到 Linux 服务器时需要 `sudo apt install libreoffice`，增加运维成本且版本不可控。

**方案**：新增 `findLibreofficeBin()` 自动发现函数，按优先级查找：

1. 项目 `libreoffice-portable/` 目录下的 `.AppImage` 文件（Linux 便携版）
2. 项目 `libreoffice-portable/` 目录下的解压版 `libreoffice/program/soffice`
3. 项目 `libreoffice-portable/` 目录下的独立 `soffice` / `soffice.exe`
4. 系统 PATH 中的 `libreoffice` 命令（回退）
5. 系统 PATH 中的 `soffice` 命令（回退）

**部署方式**：下载 LibreOffice AppImage 放入 `node-jinmao/libreoffice-portable/` 目录，部署到 Linux 后 `chmod +x` 即可运行，完全无需系统级安装。

**技术细节**：
- 查找结果通过模块级变量缓存，避免每次转换重复扫描文件系统
- AppImage 模式自动设置 `APPIMAGE_EXTRACT_AND_RUN=1` 环境变量，兼容不支持 FUSE 的 Docker 容器
- 转换超时时间从 30 秒提升到 120 秒（AppImage 首次运行需解压）
- 新增 `libreoffice-portable/` 的 `.gitignore` 规则

#### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `utils/word2pdf.js` | 新增 `findLibreofficeBin()` 函数（~100行）；convert() 改用动态查找的二进制路径；新增 AppImage 模式支持（APPIMAGE_EXTRACT_AND_RUN 环境变量）；超时时间延长至 120 秒；更新错误提示信息 |
| `.gitignore` | 新增 `libreoffice-portable/` 忽略规则（避免将大文件提交到 git） |
| `测试命令.md` | 更新 word2pdf 说明：标注便携版优先级和部署方式；修复路径转义问题（`\t` 改为 `/`） |

---

## 2026-07-03 — 实现 extract_zip.js：压缩包解压 + 主文档查找 + 自动清理机制

### 修改内容

**重写文件：`utils/extract_zip.js`（由 12 行注释存根 → 完整实现，370 行）**

1. **新增 `validateInput(filePath)` 输入校验函数**
   - 复用 `input_validator.js` 的 `validateString()` 进行基础安全检查（空值、类型、注入攻击、Unicode 控制字符）
   - 检查文件是否存在（`fs.existsSync`）
   - 检查扩展名是否属于支持格式（.zip / .rar / .7z，大小写不敏感）
   - 返回 `{ valid, resolvedPath?, errorCode?, error? }` 结构

2. **新增 `generateTempDir()` 临时目录生成函数**
   - 使用 `crypto.randomBytes(4)` 生成 8 位随机 hex ID
   - 目录路径格式：`/data/temp/extract_<randomId>/`
   - 递归创建目录（含根目录 `/data/temp/`）
   - 返回 `{ success, dirPath?, errorCode?, error? }`

3. **新增 `executeExtract(filePath, extractDir)` 解压执行函数**
   - 使用 `child_process.exec` 调用项目内置 `tools/7z/7za.exe`
   - 命令格式：`7za.exe x <archive> -o<dir> -y`
   - 设置 5 分钟超时（应对大文件解压）
   - 容错处理：即使 7z 返回非零退出码，若输出包含 "Everything is Ok" 仍视为成功
   - 返回 Promise

4. **新增 `findMainDoc(extractDir)` + `walkDir(dirPath)` 主文档查找函数**
   - 递归遍历解压目录所有文件
   - 筛选 `.md` 扩展名文件
   - 返回第一个找到的 .md 文件路径，未找到返回 null
   - 不输出逐文件日志，避免控制台刷屏

5. **新增 `extractZip(filePath, timeoutMs?)` 核心入口异步函数**
   - 完整流程：输入校验 → 生成临时目录 → 7z 解压 → 查找主文档 → 注册超时清理 → 返回结果
   - 解压失败时自动清理已创建的临时目录
   - 第二个参数 `timeoutMs` 控制超时清理时间（默认 30 分钟，传 0 不启用）
   - 错误码体系：

   | code | 含义 |
   |------|------|
   | `200` | 解压成功，找到 .md 主文档，返回 `extractDir` 和 `mainDocPath` |
   | `201` | 解压成功，但未找到 .md 文件，仅返回 `extractDir` |
   | `400` | 输入参数不合法（空值/类型错误/不支持的压缩格式/cleanUp非安全路径） |
   | `404` | 压缩包文件不存在 |
   | `500` | 解压过程失败（7z 工具不存在/解压命令执行失败/临时目录创建失败） |

6. **新增 `cleanUp(extractDir)` 目录清理函数**
   - 安全检查：仅允许删除 `/data/temp/` 下的目录，防止误删
   - 调用前自动取消对应的超时定时器（避免重复清理）
   - 使用 `Map<dir, timerId>` 管理定时器映射
   - 返回 `{ code: 200, message? }` 或 `{ code: 400, message? }`

7. **新增 `removeDirSync(dirPath)` 递归删除辅助函数**
   - 使用 `fs.rmSync` 递归强制删除

8. **新增超时自动清理机制**
   - `DEFAULT_TIMEOUT_MS = 30 * 60 * 1000`（30 分钟）
   - 解压成功后自动注册 `setTimeout`，到期自动调用 `cleanUp`
   - `cleanUp` 被显式调用时取消对应定时器
   - 防止解压目录被占用过久导致硬盘占满

9. **详细的控制台输出**
   - 使用 `[extract_zip]` 前缀，二级标记 `[validateInput]` / `[generateTempDir]` / `[executeExtract]` / `[findMainDoc]` / `[extractZip]` / `[cleanUp]` / `[removeDirSync]`
   - 复用 `input_validator.js` 的注入检测日志（`[extract_zip][validateString]`）
   - 解压时输出 7z 统计摘要（最后 3 行），避免刷屏

**新增目录：`tools/7z/`**
- 下载并内置 7-Zip 命令行工具 `7za.exe`（v9.20）
- 支持 .zip / .rar / .7z 格式的解压
- 项目部署时无需额外安装 7-Zip

### 文件变更

| 文件 | 操作 | 说明 |
|------|------|------|
| `utils/extract_zip.js` | 重写 | 完整实现压缩包解压模块（370 行） |
| `tools/7z/` | 新增 | 7-Zip 命令行工具目录 |
| `FILE.md` | 更新 | 新增 tools/7z/ 目录条目，更新 extract_zip.js 说明 |

---

## 2026-07-03 — 实现 word2pdf.js：Word 文件转 PDF（LibreOffice 命令行）

### 修改内容

**重写文件：`utils/word2pdf.js`（由 11 行注释存根 → 完整实现）**

1. **新增 `validateInput(wordPath)` 输入校验函数**
   - 复用 `input_validator.js` 的 `validateString()` 进行基础安全检查（空值、类型、注入攻击、Unicode 控制字符）
   - 扩展名校验：仅允许 `.docx` 和 `.doc`（不区分大小写）
   - 文件存在性校验：检查路径指向的文件是否存在且为文件（非目录）
   - 路径长度限制：最大 1024 字符
   - 返回 `{ valid: boolean, errorCode?: number, error?: string }`

2. **新增 `convert(wordPath)` 核心异步函数**
   - 流程：输入校验 → 确定输出路径 → 执行 libreoffice → 检查输出 → 返回结果
   - 使用 `child_process.execFile()` 调用 libreoffice 命令行（比 `exec` 更安全，避免 shell 注入）
   - 命令：`libreoffice --headless --convert-to pdf --outdir <输出目录> <输入文件>`
   - 超时控制：30 秒超时，超时后自动终止子进程并返回 `code 500`
   - 输出 PDF 路径：与源文件同目录、同名、`.pdf` 扩展名
   - 错误码体系：

   | code | 含义 |
   |------|------|
   | `200` | 转换成功，返回 pdfPath |
   | `400` | 输入参数不合法（路径为空/类型错误/不支持的扩展名/文件不存在/指向目录） |
   | `500` | 转换过程失败（libreoffice 未安装/命令执行失败/超时/输出文件未生成） |

3. **详细的控制台输出**
   - 使用 `[word2pdf]` 主前缀，`[validateInput]` / `[convert]` 二级标记
   - 信息级日志：输入校验通过、文件信息、命令执行、转换成功
   - 错误级日志：校验拦截、命令失败、文件未生成

4. **模块导出**
   - `module.exports = { convert, validateInput }`

**更新文件：`FILE.md`**
- 更新 `word2pdf.js` 条目描述：从简要描述改为详细的输入输出说明

**测试情况**
- 编写 `utils/test_word2pdf.js` 测试脚本，共 19 条测试用例
- validateInput 测试（16 条）：空值、类型错误、不支持扩展名、无扩展名、文件不存在、大写扩展名、null 字节、注入攻击、目录拦截、超长路径
- convert 测试（3 条）：null 输入、不存在文件、非 Word 扩展名
- 全部 19 条测试通过
- 测试结束后已删除测试脚本

---

## 2026-07-03 — 实现 doc2x.js：PDF 转 Markdown 压缩包（Doc2x API v2）

### 修改内容

**新增文件：`config/doc2x_config.json`**
- Doc2x API 配置文件，存储 DOC2X_API_KEY（占位符 `sk-xxx`）和 DOC2X_API_BASE

**重写文件：`utils/doc2x.js`（由 12 行注释存根 → 完整实现）**

1. **新增 `loadConfig()`** — 加载并校验 Doc2x 配置，检查 Key 是否仍为占位符
2. **新增 `httpRequest()`** — 通用 HTTP/HTTPS 请求封装，自动选择协议，30 秒超时
3. **新增 `apiRequest()`** — 封装 Doc2x API 鉴权 + JSON 解析，检查业务错误码
4. **新增 `uploadFileToOss()`** — 流式 PUT 上传 PDF 到 OSS 预签名 URL，5 分钟超时
5. **新增 `pollParseStatus()`** — 轮询 PDF 解析状态（最长 15 分钟，间隔 3 秒）
6. **新增 `pollExportResult()`** — 轮询导出结果获取 zip 下载 URL（最长 5 分钟，间隔 2 秒）
7. **新增 `main(localPdfPath)` 核心函数**（导出为 `convertPdfToMarkdown`），编排 5 步流程：
   - 预上传 → 上传 OSS → 轮询解析 → 触发导出 → 轮询导出结果
   - 输入校验：路径检查 + 文件存在性 + .pdf 扩展名校验
   - 返回 `{ code, downloadUrl?, message? }`
8. **错误码体系**：200（成功）/ 400（参数不合法）/ 404（文件不存在）/ 500（API 失败/超时）/ 502（解析失败）/ 504（配置失败）

**更新文件：`FILE.md`** — 新增 `config/doc2x_config.json` 条目，更新 `utils/doc2x.js` 用途描述

---

## 2026-07-02 — 实现 text_tts.js：火山引擎 TTS 文本转语音 + SRT 字幕生成服务

### 修改内容

**新增文件：`service/text_tts.js`**

1. **新增 `validateInput(text)` 输入校验函数**
   - 空值检查：拦截 text 为 null / undefined / 空字符串
   - 类型检查：text 必须为 string 类型
   - 长度限制检查：最大 1024 字符（火山引擎 TTS 单次合成限制）
   - 返回 `{ valid, errorCode, error }` 结构

2. **新增 `synthesize(text)` 核心异步函数**
   - 流程：输入校验 → 加载配置 → 调用 TTS API → 写入文件 → 返回路径
   - 采用火山引擎 HTTP Chunked 单向流式接口（`/api/v3/tts/unidirectional`）
   - 使用 Node.js 内置 `https` 模块，无需额外依赖
   - 流式接收响应，逐行解析 JSON：
     - `code: 0` + `data` 字段 → 音频 base64 数据，追加到缓冲区
     - `code: 0` + `sentence` 字段 → 字幕数据，收集到列表
     - `code: 20000000` → 合成结束，记录用量
   - 启用字幕功能（`enable_subtitle: true`），需要 TTS 2.0 音色
   - 音频数据 base64 解码后写入 MP3 文件
   - 字幕 words 按标点分句生成 SRT 文件
   - 请求超时设置：30 秒
   - 错误码体系：

   | code | 含义 |
   |------|------|
   | `200` | 合成成功，返回 mp3Path 和 srtPath |
   | `400` | 输入参数不合法（空文本/非字符串类型/超长） |
   | `500` | TTS API 调用失败（网络错误/连接失败/写入失败） |
   | `502` | API 返回数据解析失败（JSON 解析错误） |
   | `503` | API 返回格式不符合预期（缺少音频数据） |
   | `504` | 配置加载失败（配置文件不存在/格式错误/密钥未填写） |

3. **新增 `generateSrtContent(sentenceList)` SRT 字幕生成函数**
   - 从 API 返回的 `sentence.words` 逐词遍历
   - 分句规则：遇到以句号、逗号、分号、感叹号、问号、冒号 结尾的 word 时输出一条 SRT 条目
   - 末尾无标点的剩余文本作为最后一个条目
   - 时间格式：`HH:MM:SS,mmm`（从秒浮点数转换）
   - 序号从 1 开始递增

4. **新增 `loadConfig()` 配置文件读取函数**
   - 检查配置文件是否存在
   - JSON 解析与节点校验
   - 检查 APP_ID 和 ACCESS_KEY 是否已填
   - 返回 `{ success, config?, errorCode?, error? }` 结构

5. **新增 `formatSrtTime(seconds)` / `generateTimestamp()` 辅助函数**

6. **详细的控制台输出**
   - 使用 `[text_tts]` 前缀，二级标记 `[validateInput]` / `[loadConfig]` / `[callTtsApi]` / `[generateSrtContent]` / `[synthesize]`
   - 信息级日志：输入验证、配置加载、API 请求、音频/字幕数据统计、文件保存
   - 错误级日志：参数校验失败、配置异常、API 错误、解析失败、写入失败

**新增文件：`config/volcengine_config.json`**
- 参考 `config/deepseek_config.json` 模式，存储火山引擎 TTS 配置
- 包含字段：APP_ID、ACCESS_KEY、RESOURCE_ID、SPEAKER、API_URL
- APP_ID 和 ACCESS_KEY 由用户后续填入实际密钥
- 默认 RESOURCE_ID 为 `seed-tts-2.0`，SPEAKER 为 `zh_female_shuangkuaisisi_moon_bigtts`

**更新文件：`FILE.md`**
- 目录树中新增 `service/` 目录及 `text_tts.js`、`config/volcengine_config.json`
- 文件说明表中新增对应条目

---

## 2026-07-02 — 实现 htmlppt.js：互动式 HTML PPT 生成模块（DeepSeek 大模型）

### 修改内容

**新增文件：`utils/htmlppt.js`**

1. **新增 `validateInput(pptGuide, originalText, imageUrls)` 输入校验函数**
   - 空值检查：拦截 pptGuide / originalText 为 null / undefined / 空字符串
   - 类型检查：pptGuide 和 originalText 必须为 string；imageUrls 必须为 Array
   - 危险控制字符检查：拦截 null 字节（\x00）
   - 注入攻击检查（9 条正则模式与其他模块完全一致）
   - Unicode 方向控制字符检查（RTL override 等）
   - 长度限制检查：pptGuide 最大 10000 字符，originalText 最大 50000 字符
   - imageUrls 数组校验：单个 URL 最大 2000 字符，数组最多 50 个元素，每个元素必须为非空合法字符串
   - 返回 `{ valid, errorCode, error }` 结构

2. **新增 `main(pptGuide, originalText, imageUrls)` 核心异步函数（导出为 `generateHtmlPpt`）**
   - 使用 `DEEPSEEK_API_BIG`（deepseek-v4-pro）大模型
   - 思考模式已开启（`thinking: { "type": "enabled" }`），最大思考努力
   - 不同于其他模块，不使用 `response_format: json_object`（因为输出是 HTML 代码而非 JSON）
   - 提示词从 `config/html_ppt_prompt.txt` 加载（路径由 `config/prompt.json` 的 `html_ppt_prompt` 字段指定）
   - 替换三个占位符：`{{pptGuide}}`（PPT 生成指引）、`{{originalText}}`（教材原文）、`{{imageUrls}}`（图片 URL JSON 数组）
   - API 调用失败时捕获异常，返回 `code 500`
   - API 返回空内容时返回 `code 500`
   - 自动剥离 markdown 代码块包裹（` ```html ... ``` `）
   - HTML 有效性校验：检查返回内容是否包含 `<!DOCTYPE>`、`<html>` 或 `<body>` 标记，不通过返回 `code 502`
   - 成功时返回 `{ code: 200, html: string }`

**新增文件：`config/html_ppt_prompt.txt`**
- HTML PPT 生成的 System Prompt 模板，引导 DeepSeek 生成自包含的单文件互动式 HTML 幻灯片
- 技术要求：16:9 比例、键盘翻页、按钮导航、页码指示器、过渡动画、响应式布局
- 输出约束：仅输出 HTML 代码，不包裹 markdown 代码块

**修改文件：`config/prompt.json`**
- 新增 `"html_ppt_prompt": "./../config/html_ppt_prompt.txt"` 字段

**更新文件：`FILE.md`**
- 新增 `config/html_ppt_prompt.txt` 和 `utils/htmlppt.js` 的索引条目
- 修正旧路径条目：`outline_generatoir/generate_outline.js` → `utils/generate_outline.js`，`MD_extractor/extractor_md.js` → `utils/extractor_md.js`

---

## 2026-07-02 — 实现 elaboration.js：口播稿扩写细化工具（DeepSeek 大模型）

### 修改内容

**新增文件：`utils/elaboration.js`**

1. **新增 `validateInput(elaboration, original, expectedWords)` 输入校验函数**
   - 空值检查：拦截 elaboration/original/expectedWords 为 null / undefined / 空字符串
   - 类型检查：elaboration 和 original 必须为 string；expectedWords 支持 number 或可解析为正整数的数字字符串
   - 范围检查：expectedWords 必须是正整数，范围 10 ~ 50000
   - 危险控制字符检查：拦截 null 字节（\x00）
   - 注入攻击检查：
     - 中文 prompt 注入模式（如 "忽略以上指令"）
     - 英文 prompt 注入模式（如 "ignore all previous instructions"）
     - ChatML token 注入（`<|im_start|>` / `<|im_end|>`）
     - DAN 越狱模式 / 越狱关键词
     - system 标记注入（`[system]...[/system]`）
   - Unicode 方向控制字符检查（RTL override 等）
   - 长度限制检查：elaboration 最大 20000 字符，original 最大 50000 字符
   - 返回 `{ valid, errorCode, error, parsedWords? }` 结构

2. **新增 `elaborateText(elaboration, original, expectedWords)` 核心异步函数**
   - 使用 `DEEPSEEK_API_BIG`（deepseek-v4-pro）大模型
   - 思考模式已开启（`thinking: { "type": "enabled" }`），处理复杂扩写任务
   - JSON 结构化输出（`response_format: { "type": "json_object" }`），最大思考努力
   - 提示词从 `config/elaboration_prompt.txt` 加载（路径由 `config/prompt.json` 的 `elaboration_prompt` 字段指定）
   - 替换三个占位符：`{{elaboration}}`（原始口播稿）、`{{original}}`（教材原文）、`{{expected_words}}`（预期字数）
   - API 调用失败时捕获异常，返回 `code 500`
   - API 返回 JSON 解析失败时返回 `code 502`
   - API 返回 JSON 不含 script 字段时返回 `code 503`
   - 成功时返回 `{ code: 200, script: "扩写后的口播稿文本" }`

3. **返回值设计**
   - 返回结构化对象 `{ code, script?, message? }`，与项目现有模式一致
   - 调用方根据 `code` 判断成功/失败
   - 错误码体系：

   | code | 含义 |
   |------|------|
   | `200` | 成功扩写，script 包含扩写后的口播稿文本 |
   | `400` | 输入参数不合法 |
   | `500` | DeepSeek API 调用失败 |
   | `502` | API 返回内容 JSON 解析失败 |
   | `503` | API 返回 JSON 不包含 script 字段 |

4. **详细的控制台输出**
   - 使用 `[elaboration]` 前缀（区别于其他模块），二级标记 `[validateInput]` / `[main]`
   - 信息级日志：输入验证通过、提示词加载、API 调用成功、扩写完成
   - 错误级日志：输入拦截、API 调用失败、解析失败、字段缺失

**修改文件：`FILE.md`**
- 目录树中新增 `config/elaboration_prompt.txt` 和 `utils/elaboration.js`
- 文件说明表中新增对应条目
- 移除已废弃的 `test_line_indexer.js` 条目

**测试情况**
- 编写 `utils/test_elaboration.js` 测试脚本，覆盖 validateInput 全面校验
- 测试范围：空值检查、类型检查、字数范围、危险字符、注入攻击、Unicode 控制字符、长度限制
- 用真实参数调用 elaborateText 验证 API 集成
- 全部测试通过后删除测试脚本

---

## 2026-07-02 — 实现 get_line.js：行号识别工具（DeepSeek 小模型）

### 修改内容

**新增文件：`utils/get_line.js`**

1. **新增 `validateInput(indexedMarkdown)` 输入校验函数**
   - 空值检查：拦截 null / undefined / 空字符串
   - 类型检查：确保参数为 string 类型
   - 危险控制字符检查：拦截 null 字节（\x00）
   - 注入攻击检查：
     - 中文 prompt 注入模式（如 "忽略以上指令"）
     - 英文 prompt 注入模式（如 "ignore all previous instructions"）
     - ChatML token 注入（`<|im_start|>` / `<|im_end|>`）
     - DAN 越狱模式 / 越狱关键词
     - system 标记注入（`[system]...[/system]`）
   - Unicode 方向控制字符检查（RTL override 等）
   - 长度限制检查：最大 100,000 字符
   - 返回 `{ valid, errorCode, error }` 结构

2. **新增 `getLine(indexedMarkdown)` 核心异步函数**
   - 使用 `DEEPSEEK_API_SMALL`（deepseek-v4-flash）小模型
   - 思考模式已关闭（`thinking: { "type": "disabled" }`），因为任务简单无需深度推理
   - 提示词从 `config/getline_prompt.txt` 加载（路径由 `config/prompt.json` 的 `getline_prompt` 字段指定）
   - API 调用失败时捕获异常，返回 `code 500`
   - API 返回 JSON 解析失败时返回 `code 502`
   - API 返回格式不符合预期（非数组/空数组/字段缺失）时返回 `code 503`
   - 自动处理 markdown 代码块包裹（`` ```json ... ``` ``）
   - 成功时返回 `{ code: 200, startline: number, endline: number }`

3. **返回值设计**
   - 返回 JSON 字符串（而非对象），与注释中"返回值为字符串"的要求一致
   - 调用方通过 `JSON.parse()` 解析后，根据 `code` 判断成功/失败
   - 错误码体系：
     | code | 含义 |
     |------|------|
     | `200` | 成功识别行号，含 startline 和 endline |
     | `400` | 输入参数不合法 |
     | `500` | DeepSeek API 调用失败 |
     | `502` | API 返回内容 JSON 解析失败 |
     | `503` | API 返回内容格式不符合预期 |

4. **详细的控制台输出**
   - 信息级日志：`[get_line]` 前缀，覆盖提示词加载、验证通过、API 调用成功、行号识别成功
   - 错误级日志：输入拦截、API 调用失败、解析失败、格式异常

**修改文件：`config/getline_prompt.txt`**
- 补充写入提示词内容（原文件为空，导致加载长度为 0 字符）
- 提示词长度：1303 字符，共 42 行

**测试情况**
- 编写 `utils/test_get_line.js` 测试脚本，覆盖 39 条测试用例
- 测试范围：validateInput 全面校验（空值/类型/控制字符/注入攻击/Unicode/长度） + getLine 输入校验集成
- 全部 39 条测试通过
- 测试结束后已删除测试脚本

**修改文件：`FILE.md`**
- 目录树中新增 `config/getline_prompt.txt` 和 `utils/get_line.js`
- 文件说明表中新增对应条目

---

## 2026-07-02 — 实现 line_indexer.js：Markdown 行号索引工具

### 修改内容

**新增文件：`utils/line_indexer.js`**

1. **新增 `validateInput(markdownContent)` 输入校验函数**
   - 空值检查：拦截 null / undefined / 空字符串
   - 类型检查：确保参数为 string 类型
   - 危险控制字符检查：拦截 null 字节（\x00）
   - 注入攻击检查：
     - 中文 prompt 注入模式（如 "忽略以上指令"）
     - 英文 prompt 注入模式（如 "ignore all previous instructions"）
     - ChatML token 注入（`<|im_start|>` / `<|im_end|>`）
     - DAN 越狱模式
     - system 标记注入（`[system]...[/system]`）
   - Unicode 方向控制字符检查（RTL override 等）
   - 长度限制检查：最大 100,000 字符
   - 返回 `{ valid, errorCode, error }` 结构

2. **新增 `addLineNumbers(markdownContent)` 核心函数**
   - 按 `\n` 分割文本为行数组
   - 逐行添加右侧对齐的行号索引，格式为 `行号 | 原始内容`
   - 自动计算行号宽度确保对齐（1-9 行 → 1 位宽，10-99 行 → 2 位宽，100+ 行 → 3 位宽）
   - 返回值格式：`{ code, text, message? }`（与项目现有模式一致）
     - `code 200` — 成功添加行号索引
     - `code 400` — 输入参数不合法
   - 详细的控制台输出：`[validateInput]` / `[addLineNumbers]` 前缀日志

**新增文件：`utils/test_line_indexer.js`**
- 编写 50 条单元测试用例，覆盖所有验证场景和核心逻辑
- 测试范围：
  - 输入校验：null / undefined / 空字符串 / 类型错误 / null 字节 / 注入攻击 / 超长
  - 核心逻辑：单行 / 多行 / 10 行对齐 / 100 行对齐 / 空行处理 / 注入拦截
- 全部测试通过

**修改文件：`FILE.md`**
- 目录树中新增 `utils/line_indexer.js` 和 `utils/test_line_indexer.js`
- 文件说明表中新增对应条目

---

## 2026-07-02 — generate_outline.js 重构：移除文件存储职责，统一错误码模式

### 修改内容

**修改文件：`utils/outline_generatoir/generate_outline.js`**

1. **`main()` 不再负责文件持久化，仅返回 JSON 数据**
   - 删除 `fs.writeFileSync` 保存 `outline.json` 的逻辑
   - 持久化由上层 service 负责，工具层只做「输入 → API 调用 → 返回数据」
   - 模块头注释更新：从「生成大纲并保存为 json 文件」改为「仅返回 JSON 内容，不负责文件持久化」

2. **`main()` 返回值改为 `{ code, outline?, message? }` 结构化对象（参考 `extractor_md.js`）**
   - 不再通过 `throw Error` 报告错误
   - 新增 4 种状态码：

   | code | 含义 | outline | message |
   |------|------|---------|---------|
   | `200` | 成功生成大纲 | 解析后的 JSON 对象 | 无 |
   | `400` | 输入参数不合法 | `undefined` | 具体校验失败原因 |
   | `500` | DeepSeek API 调用失败 | `undefined` | API 错误信息 |
   | `502` | API 返回内容 JSON 解析失败 | `undefined` | 解析错误详情 |

3. **新增 API 调用异常捕获（`try-catch`）**
   - 用 `try-catch` 包裹 `openai.chat.completions.create()`，捕获网络超时、鉴权失败等服务端错误，返回 `code 500`
   - 新增 `choices` 数组空值校验，防止直接索引 `choices[0]` 时出错

4. **`validateInput()` 返回值新增 `errorCode` 字段**
   - 所有校验失败的分支统一返回 `{ valid: false, errorCode: 400, error: "..." }`
   - 与 `extractor_md.js` 的 `validateParams` 返回 `errorCode` 风格一致

5. **`main()` 内增强控制台日志**
   - Prompt 加载完成后输出「准备调用 DeepSeek API」
   - API 调用成功后输出返回内容长度
   - 大纲生成成功后输出「已解析为 JSON 对象」

---

## 2026-07-02 — extractor_md.js 返回值重构：HTTP 风格状态码模式

### 修改内容

**修改文件：`utils/MD_extractor/extractor_md.js`**

1. **`extractLines` 返回值从 `string` 改为 `{ code, text, message? }` 对象**
   - 不再通过 throw Error 报告错误，所有结果通过 `code` 区分
   - 调用方无需 try-catch，直接根据 `code` 判断结果即可

2. **新增 5 种状态码（参考 HTTP 状态码语义）：**

   | code | 含义 | text | message |
   |------|------|------|---------|
   | `200` | 成功提取 | 提取的文本 | 无 |
   | `206` | 成功提取，但结束行超出已自动截断 | 截断后的文本 | 截断警告说明 |
   | `400` | 参数不合法 | `""` | 具体错误原因 |
   | `404` | 文件不存在 | `""` | 文件路径 |
   | `416` | 起始行号超出文件总行数 | `""` | 行号与总行数信息 |

3. **`validateParams` 返回值增加 `errorCode` 和 `truncated` 字段**
   - `errorCode`：与 `extractLines` 返回的 code 一致，便于透传
   - `truncated`：标记是否发生了自动截断
   - 不改动函数签名，仅扩充返回值

---

## 2026-07-02 — 重构 extractor_md.js：内容相关校验移入 validateParams

### 修改内容

**修改文件：`utils/MD_extractor/extractor_md.js`**

1. **`validateParams` 新增第 6 项校验 —— 文件内容相关的行号校验**
   - 6.1 读取文件内容检查是否为空（空文件返回 valid=false）
   - 6.2 检查起始行号是否超出文件总行数（超出返回 valid=false）
   - 6.3 结束行号超出文件总行数时自动截断（仅警告，不抛错）
   - 返回值增加 `totalLines` 和 `actualEndLine` 字段

2. **`extractLines` 简化**
   - 删除了原先内嵌的空文件检查、startLine 超出行数检查、endLine 截断逻辑
   - 直接使用 `validationResult.actualEndLine` 作为提取参数
   - 函数体仅保留纯数据处理逻辑：读文件 → split → slice → join

---

## 2026-07-02 — 重构 extractor_md.js：参数校验抽离为独立函数

### 修改内容

**修改文件：`MD_extractor/extractor_md.js`**

1. **新增 `validateParams(mdFilePath, startLine, endLine)` 函数**
   - 将原本内嵌在 `extractLines` 中的 5 项参数校验抽离为独立函数
   - 返回 `{ valid: boolean, error?: string }` 结构，与项目 `validateInput` 风格一致
   - 日志前缀改为 `[validateParams]`

2. **简化 `extractLines` 函数**
   - 先调用 `validateParams` 进行前置校验，校验通过后再进入核心提取逻辑
   - 核心逻辑保持不变：文件读取 → 行号截断 → 行范围提取 → join 返回

3. **更新模块导出**
   - 同时导出 `extractLines` 和 `validateParams`

---

## 2026-07-02 — 实现 MD_extractor 模块（extractor_md.js）

### 修改内容

**新增文件：`MD_extractor/extractor_md.js`**

1. **新增 `extractLines(mdFilePath, startLine, endLine)` 函数**
   - 参数校验：
     - 文件路径空值/类型检查
     - 起始行号/结束行号的类型与正整数检查
     - 行号范围校验（startLine ≤ endLine）
     - 文件存在性校验（含目录/文件判断）
   - 核心逻辑：
     - 同步读取文件全部内容（UTF-8）
     - 按换行符分割为行数组
     - 超出文件总行数时自动截断并给出警告（不抛错）
     - 使用 slice 提取目标行范围并 join 返回
   - 详细的控制台输出：`[extractLines]` 前缀日志，覆盖开始/完成/警告/错误场景

**修改文件：`FILE.md`**
- 目录树中新增 `MD_extractor/` 目录及 `extractor_md.js`
- 文件说明表中新增 `MD_extractor/extractor_md.js` 条目

---

## 2026-07-02 — 新增输入验证功能（validateInput）

### 修改内容

**修改文件：`outline_generatoir/generate_outline.js`**

1. **新增 `validateInput(yuanwen, pptother)` 函数**
   - 空值检查：拦截 null / undefined / 空字符串
   - 类型检查：确保两个参数均为 string 类型
   - 危险控制字符检查：拦截 null 字节（\x00）
   - 注入攻击检查：
     - 中文 prompt 注入模式（如 "忽略以上指令"）
     - 英文 prompt 注入模式（如 "ignore all previous instructions"）
     - ChatML token 注入（`<|im_start|>` / `<|im_end|>`）
     - DAN 越狱模式
     - system 标记注入（`[system]...[/system]`）
   - Unicode 方向控制字符检查（RTL override 等）
   - 长度限制检查：yuanwen 最大 50,000 字符，pptother 最大 500 字符

2. **在 `main()` 函数中集成前置验证**
   - 核心业务逻辑执行前调用 `validateInput()`
   - 验证不通过时抛出 Error 阻止后续执行

3. **更新模块导出**
   - 同时导出 `generateOutline` 和 `validateInput`

**新增文件：`outline_generatoir/test_validate.js`**
- 编写 39 条单元测试用例，覆盖所有验证场景
- 所有测试均通过

**新增文件：`FILE.md`**
- 项目文件索引，包含文件结构和用途说明

**新增文件：`开发日志.md`**
- 记录项目开发过程中的所有增删改操作

**新增文件：`start.ps1`**
- Windows PowerShell 快速启动脚本

---

## 2026-07-02 — 项目初始化

### 项目概述
- 创建 node-jinmao 项目，基于 Node.js + OpenAI SDK
- 调用 DeepSeek API 生成 PPT 大纲
- 将大纲保存为 JSON 文件

### 文件清单
- `app.js`：HTTP 服务器入口（端口 8888）
- `package.json`：项目配置与依赖
- `config/deepseek_config.json`：API 配置
- `config/prompt.json`：Prompt 路径映射
- `config/outline_prompt.txt`：Prompt 模板
- `outline_generatoir/generate_outline.js`：大纲生成核心逻辑
