# 开发日志

> 版本号说明：后端 `node-jinmao/package.json` 和前端 `WEB/package.json` 各自独立维护独立的版本号序列。前端版本标注为 `[前端] vX.Y.Z`，后端版本标注为 `[后端] vX.Y.Z`。

## 2026-08-04 — [前端] v0.40.0 — 学习页原地展示思维导图（移除独立查看页）

### 改动内容
- 需求调整：不前往独立查看页，在学习页 PPT iframe 所在容器内原地替换展示思维导图，返回课件时 PPT 播放位置/音频不中断
- `pages/study/script.js`：新增 `showMindmap`/`mindmapUrl`/`mindmapLoading` 状态与 `mindmapSrc`（按当前主题拼 `?theme=dark/light`）；「查看思维导图」点击改为原地切换，展示期间按钮文案变「返回课件」；状态接口返回 done 时记录 `mindmapUrl`；切章/重置时一并关闭思维导图视图；移动端点按 PPT 区域在思维导图展示期间不再响应
- `pages/study/index.vue`：`#ppt-container` 内新增思维导图 iframe（可交互，支持折叠/展开/拖动/缩放，`:key` 随主题重载）；展示思维导图时隐藏 PPT iframe、页码指示器、字幕层、播放控件与全屏按钮
- `App.vue`：移除 `/mindmap` 路由、`MindmapPage` 挂载、`mindmapParams` provide/inject（页面不再使用）
- 删除 `pages/mindmap/index.vue` 与 `script.js`（独立查看页废弃）
- 版本号：前端 `v0.39.1` → `v0.40.0`

### 验证
- `npm run build` 通过（WEB/）
- 待手测：查看/返回课件原地切换不丢失播放位置、思维导图交互（折叠/展开/拖动/缩放）、深色模式主题联动、切章重置视图

## 2026-08-04 — [前端] v0.39.1 — 修复思维导图查看页深色模式节点文字不可读

### 改动内容
- 修复“深色模式下思维导图节点文本仍是深色（markmap 默认 #333）导致阅读障碍”：查看页 iframe 地址按当前主题拼接 `?theme=dark/light`（`mindmapSrc` computed），主题切换时通过 `:key` 强制 iframe 重载
- 版本号：前端 `v0.39.0` → `v0.39.1`

### 验证
- `npm run build` 通过（WEB/）
- 待手测：深色/浅色模式切换后思维导图节点文字与背景对比度正常

## 2026-08-04 — [后端] v1.50.1 — 修复思维导图 HTML 深色模式节点文字不可读

### 改动内容
- `service/mindmap_service.js` 的 `buildMindmapHtml`：生成 HTML 内嵌主题脚本，读取 URL 的 `?theme=dark/light`（缺省跟随系统偏好），给 body 添加/移除 markmap 官方暗黑 class `markmap-dark`（节点文字 #eee）并同步 body 背景色；原 `prefers-color-scheme` 媒体查询由脚本接管
- 版本号：后端 `v1.50.0` → `v1.50.1`

### 验证
- `node --check` 通过（mindmap_service.js）
- 本地已有思维导图（`usercourse/9001/9001/chapter_01/MindMap`）已用存储的 mindmap.md 重新打包上传，修复即时生效
- 注意：生产环境其他已生成的思维导图 HTML 为静态文件，需重新生成（删除旧文件或再次生成）后新主题逻辑生效

## 2026-08-04 — [前端] v0.39.0 — 学习页新增一键思维导图（异步生成 + markmap 查看页）

### 改动内容
- 新增 `api/mindmap.js`：`generateMindmap(courseId, chapterId)` 触发生成、`getMindmapStatus(courseId, chapterId)` 查询状态（none/generating/done/failed，done 返回 mindmapUrl）
- 学习页 `pages/study/index.vue`：PPT iframe 上方新增 6 按钮学习工具横条——思维导图（生成/查看双状态）+ 5 个「敬请期待」禁用占位（hover 提示"即将上线"）；页码指示器下移避开横条
- `pages/study/script.js`：思维导图状态机 + 3s 状态轮询（切章重置、卸载停止、生成结束自动提示），点击「生成思维导图」异步触发，完成后按钮变「查看思维导图」并进入查看页
- 新增查看页 `pages/mindmap/`（index.vue + script.js）：返回/主题切换/章节标题 + 全屏 iframe 渲染 markmap HTML，未生成显示空态
- `App.vue`：登记 `/mindmap?courseId=&chapterId=` 路由，新增 `mindmapParams` provide/inject
- 版本号：前端 `v0.38.1` → `v0.39.0`

### 验证
- `npm run build` 通过（WEB/）
- 待手测：横条 6 按钮渲染与占位禁用、生成/查看全流程、思维导图折叠/展开/拖动/缩放、暗黑模式、移动端横条

## 2026-08-04 — [后端] v1.50.0 — 新增一键思维导图生成接口

### 改动内容
- 新增 `config/mindmap_prompt.txt`（注册到 `config/prompt.json`）：基于章节大纲（outline JSON 的 ppt/kbg/zjts）归纳 ≤4 层 Markdown 层级结构
- 新增 `service/mindmap_service.js`：读大纲 → DeepSeek（flash，callTag=mindmap，统一计费）生成 Markdown 大纲（不合规纠错重试 1 次）→ 组装自包含 markmap HTML → 上传 MinIO `{chapterRoot}MindMap/mindmap.md|mindmap.html`；内存任务表 + MinIO 文件存在性作为状态真相
- 新增 `API/course/mindmap.js`：`POST /api/v1/courses/:courseId/chapters/:chapterId/mindmap`（异步启动生成，防重复触发，余额不足 402）与 `GET` 同路径（状态查询，返回 mindmapUrl），均带完整 @openapi 注释；`API/course/index.js` 挂载；`config/swagger.js` 扫描路径加入 `./API/course/*.js`
- 版本号：后端 `v1.49.0` → `v1.50.0`

### 验证
- `node --check` 通过（mindmap_service.js / API/course/mindmap.js / course/index.js / config/swagger.js）
- 待手测：400/401/402/403/404 异常分支与正常生成流程、MinIO MindMap 文件生成、/api/v1/files 可访问

## 2026-08-04 — [前端] v0.38.1 — 修复学习页侧边栏收起后布局错乱

### 改动内容
- 修复用户反馈“学习页点击左侧收起目录按钮后，左侧边栏布局直接乱套”：原 `toggleSidebar` 直接操作 DOM，仅隐藏章节列表 `<nav>`，标题「◤课程目录◢」仍溢出 40px 窄栏、底部「共 N 页/生成下一章」区域被挤压残留，且宽度状态未与 Vue 响应式绑定，任意重渲染后状态易错乱
- `pages/study/index.vue`：左侧栏宽度改为响应式 `:style` 绑定（展开 224px / 收起 40px），容器加 `overflow-hidden` 与 `transition-all duration-500`；收起时隐藏标题组并把展开按钮居中；`ChapterList` 整体 `v-show`（收起时状态与生成按钮一并隐藏）；左拖动手柄改为 `onLeftHandleMouseDown`（收起状态下先展开再拖动）
- `pages/study/script.js`：`toggleSidebar` 精简为只切换 `sidebarExpanded`，删除 `getElementById` DOM 写操作；新增 `onLeftHandleMouseDown` 折叠态防呆
- `pages/study/ChapterList.vue`：移除已无用途的 `nav-id`/`navId` prop（折叠改由父组件 `v-show` 控制）
- 版本号：前端 `v0.38.0` → `v0.38.1`

### 验证
- `npm run build` 通过（WEB/）
- 待手测：学习页点击左侧收起按钮，侧边栏平滑收为 40px 图标栏（仅剩展开按钮居中），无文字溢出/按钮挤压；再次点击恢复 224px 章节列表；折叠态拖动左边界手柄会先展开再调整宽度

## 2026-08-04 — [前端] v0.38.0 — 删除学习页左下角自动生成开关（功能取消）

### 改动内容
- 应产品要求取消自动生成功能，删除学习页左下角「自动生成」开关及其全部逻辑
- `pages/study/ChapterList.vue`：移除底部「自动生成」开关 UI、`autoGenerateEnabled` prop 与 `toggle-auto-generate` emit
- `pages/study/index.vue`：移除桌面/移动端两处 `ChapterList` 的 `auto-generate-enabled` 与 `@toggle-auto-generate` 绑定
- `pages/study/script.js`：删除 `autoGenerateEnabled` 状态、`autoGenerateCheck` 自动生成检查、`onAutoGenerateToggle` 回调及 onMounted 中 localStorage 状态恢复逻辑；`handleGenerateNextChapter` 移除仅自动生成使用的 `silent` 参数
- 保留手动「生成下一章」按钮功能不变
- 版本号：前端 `v0.37.1` → `v0.38.0`

### 验证
- `npm run build` 通过（WEB/）
- 待手测：学习页左下角无「自动生成」开关，学习/切换章节不再自动触发下一章生成，手动「生成下一章」按钮正常

## 2026-08-04 — [前端] v0.37.1 — 修复学习页翻页后播放倍速重置为 1x

### 改动内容
- 修复用户反馈“学习页选 2 倍速后一翻页就变回 1 倍速，只能手动切换其它倍速再改回 2 倍速才恢复”：翻页时 `<audio>` 的 `src` 变化，浏览器会把元素的 `playbackRate` 重置回默认值 1，而倍速下拉框的值未变，原有 `watch(playbackSpeed)` 不会触发
- `pages/study/script.js`：`onAudioLoaded()` 在每页音频元数据加载完成后重新应用当前 `playbackSpeed`，覆盖所有换源场景（翻页、进度条跳转、切换章节）
- `pages/study/script.js`：倍速 watch 中同步设置 `audio.defaultPlaybackRate`，使后续换源加载的音频自动继承所选倍速
- 版本号：前端 `v0.37.0` → `v0.37.1`

### 验证
- `npm run build` 通过（WEB/）
- 待手测：学习页选 2.0x 播放 → 翻下一页/上一页/进度条跳转后倍速保持 2.0x；换其它倍速后翻页同样保持

## 2026-08-04 — [前端] v0.37.0 — 启用首页教材搜索（修复搜索框无响应）

### 改动内容
- 修复用户反馈“电脑端搜索教材点不动”：首页顶部搜索框此前为纯装饰控件（无 v-model、无回车/清除事件），输入后无任何反应
- `HomeTopbar.vue`：搜索框绑定 `v-model`，新增回车与点击清除触发 `handleSearch()`，并通过 `search` 事件冒泡关键词给父组件
- `pages/home/script.js`：新增 `handleSearch(kw)`（写入 `keyword` → 重置第 1 页 → 重新加载教材列表），并导出 `keyword` 与 `handleSearch`
- `pages/home/index.vue`：绑定 `@search` 事件；空状态按是否有搜索关键词区分提示（“没有找到匹配的教材 / 还没有教材”）
- 复用后端既有 `GET /api/v1/books?keyword=` 模糊搜索能力，后端无改动；交互模式与题库市场页一致（回车/清除触发，不做实时过滤）
- 版本号：前端 `v0.36.2` → `v0.37.0`

### 验证
- `npm run build` 通过（WEB/）
- 待手测：Edge/Chrome 输入关键词回车 → 列表只剩匹配项；清除 → 恢复全部；搜索无结果显示匹配提示；分页/排序/轮询不受影响

## 2026-08-04 — [后端] v1.49.0 — 管理后台新增 API 密钥配置（写回 .env）

### 改动内容
- 管理后台新增「🔑 API 密钥」Tab（`node-jinmao/admin/static/tpl/apikeys.html` + `js/tabs/apikeys.js`）：展示 5 个 AI 服务密钥（DeepSeek / Doc2x / 火山引擎 TTS App ID、Access Key / Grsai）的配置状态（脱敏显示），可在线填写新值并保存
- 后端新增 `API/admin/env.js`（`GET/PUT /admin/:suffix/api/env`，沿用管理员双重鉴权、不写 @openapi）：`GET` 从 .env 文件读取白名单字段并脱敏返回；`PUT` 校验后逐行写回 .env（保留注释与其它配置、缺失 key 追加到末尾、先写临时文件再原子重命名），不修改运行中 process.env，重启后端后生效
- `config/index.js` 导出 `validateApiKey` 供 env 路由复用
- 版本号：后端 `v1.48.0` → `v1.49.0`

### 验证
- `node --check` 通过（API/admin/env.js / tabs/apikeys.js）
- 接口实测：GET 返回 5 项脱敏状态；PUT 合法更新后 .env 目标行替换、注释与其它 key 保留；未知 key / 空值 / 非 ASCII / 含引号均返回 400
- 浏览器验证「🔑 API 密钥」Tab 渲染与保存流程正常

## 2026-08-04 — [后端] v1.48.0 — 价格调整页扩展：新增文生图/TTS/doc2x 全部模型单价编辑

### 改动内容
- 「💰 价格调整」Tab 由仅 DeepSeek 两个模型扩展为全部模型：DeepSeek LLM（deepseek-v4-pro / deepseek-v4-flash，输入缓存未命中/命中/输出）、Grsai 文生图（gpt-image-2 / gpt-image-2-vip，元/张）、火山引擎 TTS（seed-tts-2.0，元/字符）、doc2x PDF 解析（doc2x-api-v2，元/页）
- 时段内价格表格改为通用「提供商 / 模型 / 计费项 / 单价」结构（`js/tabs/pricing.js` 的 `modelDefs` + `priceItems()`，仅渲染时段中实际存在的模型；`tpl/pricing.html` 两套卡片同步更新）
- 后端校验扩展（`utils/pricing_schedule.js`）：DeepSeek 两模型三字段必填规则保留；其它已知模型（grsai/volcengine/doc2x）必填字段校验 + 所有价格字段数值必须为 >=0 的有限数字
- 版本号：后端 `v1.47.0` → `v1.48.0`

### 验证
- `node --check` 通过（pricing_schedule.js / tabs/pricing.js）
- 临时脚本新增断言：负数图片单价、缺少 TTS per_char 均被拒绝；原有全部断言与路由冒烟仍通过
- 全部管理端模板（含 pricing.html）Vue 编译器编译通过
- 待实测：浏览器中价格调整页展示全部模型并正常保存

## 2026-08-04 — [后端] v1.47.0 — 新增管理后台「价格调整」页（售价/成本在线编辑 + 定时生效 + 分时计费）

### 改动内容
- 管理后台新增「💰 价格调整」Tab（`node-jinmao/admin/static/tpl/pricing.html` + `js/tabs/pricing.js`）：展示并编辑出售价（`config/billing_pricing.json`）与成本价（`config/model_cost_config.json`）两套配置，针对 deepseek-v4-pro / deepseek-v4-flash 按计费时段维护「输入（缓存未命中）/ 输入（缓存命中）/ 输出」三个单价（元/百万 tokens）
- 分时计费：两套配置各自带 `el-switch` 总开关（对应 `timeBasedPricing.enabled`）；`default` 时段（00:00-24:00）固定为兜底不可删改，其它时段可增删、可改名称与起止时间（支持跨天，如 15:00→08:00）；保存即写文件即时生效（计费每次调用实时读取，无需重启）
- 定时调价：设置生效时间 + 备注后可创建「定时生效出售价 / 成本价」快照，到点自动覆盖对应侧配置；待生效列表支持取消；调度数据持久化在 `node-jinmao/data/pricing_schedule.json`（运行时数据，不进 git），服务重启后到点依然生效
- 后端：新增 `utils/pricing_schedule.js`（配置校验、调度持久化、到点应用、15 秒轮询调度）与 `API/admin/pricing.js`（`GET/PUT /pricing`、`GET/POST/DELETE /pricing/schedule`，沿用管理员双重鉴权、不写 @openapi），`app.js` 启动时启动调度；`admin/static/js/shared.js` 新增 `apiDelete`
- 校验规则：价格为 >=0 的有限数字；时段时间 `HH:MM`（结束时间允许 24:00）；每套配置有且仅有一个 default 时段；`effectiveAt` 必须晚于当前时间
- 版本号：后端 `v1.46.0` → `v1.47.0`

### 验证
- `node --check` 通过（pricing_schedule.js / API/admin/pricing.js / API/admin.js / app.js）
- 临时脚本（`test/pricing_schedule_test.js`，不提交）：当前配置校验通过、非法配置被拒绝、官方双高峰（09:00-12:00、14:00-18:00）+ 跨天时段 + `enabled=false` 兜底匹配符合预期、调度增删查、到点应用、pricing 路由 GET/PUT/POST/DELETE 冒烟全部通过，验证后配置文件字节级恢复
- 待实测：浏览器中 `/admin/:suffix` 新 Tab 的编辑/保存/定时调价交互与暗黑模式

## 2026-08-04 — [后端] v1.46.0 — 文生图失败自动切换 gpt-image-2-vip 重试（最多6次）

### 改动内容
- `node-jinmao/config/grsai_config.json`：新增 `GRSAI_VIP_MODEL: "gpt-image-2-vip"` 配置
- `node-jinmao/config/billing_pricing.json` 与 `config/model_cost_config.json`：`grsai` 下新增 `gpt-image-2-vip: { "per_image": 0.13 }`（售价/成本均为 0.13 元/张，default/peak/off_peak 三个时段一致）
- `node-jinmao/utils/create_image.js`：文生图重试策略由「gpt-image-2 最多重试 3 次（共 4 次）」改为「gpt-image-2 与 gpt-image-2-vip 交替，共 3 轮最多 6 次调用」（gpt-image-2 → gpt-image-2-vip → …）；VIP 尝试自动将比例转换为 API 支持的像素值（16:9 → 2048x1152 2K 档，其余比例按 API 文档 2K 档映射，已是像素值/auto 原样透传，未知比例回退 2048x2048 并告警）；成功计费按实际成功模型与分辨率记录，全部失败按最后一次尝试的模型记 0 元失败账单；内容违规/参数错误等不可重试错误不触发 VIP 重试
- 版本号：后端 `v1.45.4` → `v1.46.0`

### 验证
- `node --check node-jinmao/utils/create_image.js` 通过；`grsai_config.json` / `billing_pricing.json` / `model_cost_config.json` 均可被 `JSON.parse` 解析
- mock `global.fetch` 内联脚本验证：全部失败时恰好 6 次调用且模型交替序列正确、VIP 尝试发送 `aspectRatio=2048x1152`；第 2 次（VIP）成功时计费 `model=gpt-image-2-vip`、`imageResolution=2048x1152`、`retryCount=1`；第 5 次（gpt-image-2）成功时计费 `model=gpt-image-2`、`retryCount=4`
- 待实测：真实 GRS key 下封面生成在 VIP 回退时正常出图（会消耗真实 API 费用）

## 2026-08-04 — [前端] v0.36.2 — 学习页进度条跳转健壮性修复（非有限时长防御）

### 改动内容
- `WEB/src/pages/study/script.js`：`commitSeek`/`applyAudioSeek`/`onAudioLoaded`/`prefetchChapterTotalTime` 增加非有限值防御——`audio.duration` 为 Infinity/NaN（无 Range 支持的流式响应）时按 0 处理，`_duration` 只采纳有限正时长，`currentTime` 赋值前校验目标值为有限数并 try/catch，避免「The provided double value is non-finite」报错；未知时长页继续按 0 参与章节累计
- 版本号：前端 `v0.36.1` → `v0.36.2`

### 验证
- `WEB` 下 `npm run build` 成功（v0.36.2）
- 待实测：暂停态点击当前页附近/跨页跳转不再报错，进度条不跳回；配合后端 v1.45.4 的 Range 支持后音频时长与拖动 seek 正常

## 2026-08-04 — [后端] v1.45.4 — 文件代理支持 HTTP Range（修复音频时长与拖动 seek）

### 改动内容
- `node-jinmao/API/files.js`：`GET /api/v1/files/*` 新增 HTTP Range 支持——`bytes=start-end` / `bytes=start-` / `bytes=-suffix` 返回 206 分段响应（含 `Content-Range`/`Accept-Ranges`/`Content-Length`），范围无效返回 416；完整响应附带 `Accept-Ranges` 与 `Content-Length`。修复前 MP3 走 chunked 无长度/无 Range，浏览器 `audio.duration` 为 Infinity、无法 seek，导致进度条点击报「non-finite」
- `@openapi` 注释补全 206/416 响应
- 版本号：后端 `v1.45.3` → `v1.45.4`

### 验证
- `node --check node-jinmao/API/files.js` 通过
- 独立起 8899 端口的文件代理实例实测：无 Range 请求返回 200 且带 `Accept-Ranges: bytes` 与 `Content-Length`；`bytes=0-10` 返回 206 且带 `Content-Range: bytes 0-10/{size}`；越界范围返回 416

## 2026-08-04 — [前端] v0.36.1 — 手机竖屏学习页 PPT 容器恢复 16:9，释放下方助教/章节空间

### 改动内容
- `WEB/src/pages/study/index.vue`：手机竖屏（常规态）下 PPT 容器由「填满顶部区域」改为固定 16:9 比例（与桌面一致），顶部区域按内容高度收缩；下方移动端面板由固定 42% 高度改为 flex-1 填充剩余空间，AI 助教与章节列表不再被挤占
- 移动全屏态（横屏 3:7）与桌面布局不受影响：全屏时左 70% 播放区仍由 PPT 容器填满
- 版本号：前端 `v0.36.0` → `v0.36.1`

### 验证
- `WEB` 下 `npm run build` 成功（v0.36.1）
- 待实测：手机竖屏下 PPT 呈 16:9、下方助教/章节面板高度显著增加；全屏横屏 3:7 布局与桌面布局回归

## 2026-08-04 — [前端] v0.36.0 — 手机版学习页新增常规上下分栏与全屏横屏3:7布局

### 改动内容
- `WEB/src/pages/study/index.vue`：移除移动端「请在平板或桌面端打开」占位，新增手机版布局——常规态（竖屏）上 PPT 播放区（含字幕）+ 下面板（AI 助教/章节 两个 Tab，默认 AI 助教）；点按 PPT 显示进度条/上下一页/播放暂停控制条，5 秒自动隐藏；PPT 区右上角新增移动端全屏按钮，点击切换为横屏 3:7 分栏（左 70% 播放区 + 右 30% 聊天/章节面板，可退出全屏）。口播稿在手机端不提供入口（桌面端保留）
- 新增 `WEB/src/pages/study/AiChatPanel.vue`：AI 助教面板（头部/历史/新对话/消息区/输入/模型切换/用量环）从桌面右栏原样抽取，供桌面右栏、移动底部面板、移动全屏右侧面板三处复用；历史弹窗宽度响应式（≤78vw）防小屏溢出
- 新增 `WEB/src/pages/study/ChapterList.vue`：章节列表（状态/内联提示/生成进度）+「生成下一章」按钮与自动生成开关原样抽取，三处复用
- `WEB/src/pages/study/script.js`：新增 `isMobileView`（手机 UA 或视口 <768px）、`mobileFullscreen`、`mobilePanelTab`（默认 ai）、移动端控制条显隐与 5 秒自动隐藏定时器、`toggleMobileFullscreen`/`toggleMobileControls` 等；进入/退出全屏仅切换布局类，不重载章节、不中断播放
- 版本号：前端 `v0.35.0` → `v0.36.0`

### 验证
- `WEB` 下 `npm run build` 成功（v0.36.0）
- 待实测：桌面三栏/hover 控制条/口播稿/AI 对话回归；手机竖屏上下分栏默认 AI、章节切换、控制条 5 秒自动隐藏、口播稿屏蔽；手机横屏全屏 3:7 布局、聊天/章节切换、退出全屏后播放状态不中断

## 2026-08-04 — [前端] v0.36.0 — 学习页进度条支持点击/拖动跳转到对应页面的对应时间

### 改动内容
- `WEB/src/pages/study/index.vue`：播放器进度条点击区域由 4px 加高到 16px（视觉轨道不变，圆角 10px）；改用 pointer 事件（pointerdown/move/up/cancel + setPointerCapture），支持点击与按住拖动，拖动中填充/圆点实时预览、松开只提交一次跳转
- `WEB/src/pages/study/script.js`：新增 `commitSeek`——把进度条百分比按章节累计时间换算为目标页与页内偏移（各页时长取 SRT 推算的 `_duration`，未知按 0），目标页=当前页时直接定位当前音频，跨页时通过 `goToPage(page, false, { offset, elapsed })` 精确切换；新增 `applyAudioSeek`（元数据未就绪时等 `loadedmetadata` 后定位，钳制到 `duration-0.05`）；`formattedTime` 拖动期间显示预览章节时间
- 行为：暂停时跳转后保持暂停，播放中从目标位置继续播放；点击章节末尾定位到最后一页末尾附近，不误触发 `ended`；时长未就绪时点击静默忽略
- 版本号：与当日手机版学习页改动共用前端 `v0.36.0`（工作区既有未提交改动，一并提交）

### 验证
- `WEB` 下 `npm run build` 成功（v0.36.0）
- 待实测：桌面/移动端点击与拖动跳转；跨页/同页定位、页内偏移与时间标签一致；暂停/播放状态保持；拖动松开只在释放时切页一次；字幕同步与自动播放回归

## 2026-08-03 — [前端] v0.35.0 — 手机端全站页面移植（/mobile 后缀）

### 改动内容
- `WEB/src/App.vue`：新增 11 个 `/mobile/*` 路由（手机端刷题、刷题报告、题库详情、账单、兑换码、个人设置、题库市场、小工具、PDF 分割器、公开考试数据、公开考试列表），并新增手机端跳转提示——手机 UA/窄屏用户进入有手机版对应页的电脑端页面时，每会话弹一次全屏询问；「是」保留 query 参数跳转对应手机版，「否」留在电脑版（sessionStorage 记忆）
- `WEB/src/components/MobileRedirectDialog.vue`：新增全屏跳转提示弹窗组件（10px 圆角、纯黑纯白文字、暗黑双轨、500ms 过渡）
- 新增手机端页面（`WEB/src/pages/mobile-*`）：`mobile-quiz`（含题库选择 / 答题 / 报告子视图）、`mobile-quiz-detail`、`mobile-billing`、`mobile-redeem`、`mobile-settings`、`mobile-market`、`mobile-tools`、`mobile-tools/pdf-splitter`、`mobile-public-stats`、`mobile-public-exams-list`，全部复用现有 API 与 QuizQuestionCard / QuizAnswerSheet / PdfSplitterView 等组件
- `WEB/src/pages/mobile-home/script.js`：修复「题库市场」坏入口（原 navigate("market") 无目标页面）→ mobile-market；「习题训练」→ mobile-quiz；「我的余额」→ mobile-billing
- `WEB/src/pages/mobile-profile/script.js`：「个人资料」→ mobile-settings；「我的余额」→ mobile-billing
- 按约定不移植：学习页（study，下轮单独做）、上传题库页（quiz-import）、管理员页（node-jinmao/admin 独立控制台）；公开考试页 `/p/:token` 维持单一链接（本身已是手机友好布局）不弹窗
- 版本号：前端 `v0.34.4` → `v0.35.0`

### 验证
- `WEB` 下 `npm run build` 成功（v0.35.0）
- 开发服务器逐个编译 23 个新增/修改模块均 200；`/mobile/*` 9 条路由 SPA 直达（含 `/mobile/quiz/report`、query 参数）均返回正常
- 待实测：手机真机/模拟器访问各 `/mobile` 页面；弹窗「是/否」行为；手机端刷题全流程（随机/顺序/答题卡/交卷/报告）；市场借用；账单、兑换码、个人设置、PDF 分割器、公开考试数据

## 2026-08-03 — [前端] v0.34.4 — 首页教材卡片移除“查看详情”测试按钮

### 改动内容
- `WEB/src/components/CourseCard.vue`：删除卡片底部操作栏（“查看详情”测试按钮 + 右侧重复的上传时间 + 边框容器及其注释）；整卡点击进入学习页的行为保留，生成中点击仍提示“课程内容正在生成中”
- `WEB/src/pages/home/index.vue`：骨架屏移除模拟底部操作栏的两块占位，加载态与实际卡片布局保持一致
- 版本号：前端 `v0.34.3` → `v0.34.4`

### 验证
- `WEB` 下 `npm run build` 成功（v0.34.4）
- 首页教材卡片不再显示“查看详情”按钮与底部操作栏；点击卡片仍可进入学习页，生成中点击仍提示“课程内容正在生成中”；卡片上方元信息区仍显示章节数与上传时间；页脚版本号显示 `v0.34.4`

## 2026-08-03 — [后端] v1.45.3 — 修复首页课件生成进度条卡在 0/39

### 改动内容
- `node-jinmao/API/POSTbook.js`：`GET /api/v1/book/:book_id/progress` 的文件生成进度改为读取流水线实际写入的 `progressData.filesProgress.current`（PPT 每页 +1、MP3/SRT 每页各 +1），并兼容旧数据回退读取 `filesCompleted`。修复前接口读的是从未被写入的 `filesCompleted`，导致 `ppt_generating`/`tts_generating` 阶段首页进度条恒显示 `0/39`，直到完成才跳满
- 版本号：后端 `v1.45.2` → `v1.45.3`

### 验证
- `node --check node-jinmao/API/POSTbook.js` 通过
- 进程内接口级测试（桩掉 bookRepo/auth 后直接调用真实路由）：`filesProgress.current=5/39`、`7/39` 均正确返回；旧字段 `filesCompleted=3` 回退正常；`completed` 状态仍返回 `39/39`；无进度数据返回 `0/0`，5 项全部通过
- 待实测：真实课件生成过程中首页进度条从 `0/39` 逐次递增至 `39/39`

## 2026-08-03 — [后端] v1.45.2 / [前端] v0.34.3 — 修复学习页助教提示乱跑与上下文重复注入

### 改动内容
- `WEB/src/pages/study/script.js`：`ensurePageGreeting()` 由「删除旧提示再重新追加到末尾」改为幂等追加——仅当对话中最近一条助教提示与当前页不同（或不存在）时才追加新消息，不删除、不移动任何既有消息；流式回答期间不追加（避免干扰流式写入），回答结束后兜底确保新页提示只出现一次。修复追问时提示先跑到上面、回答结束后又跳到最下面的乱跑问题
- `node-jinmao/service/course_ai.js`：上下文素材去重——同一对话只保留一条 `context` 消息，追问时按当前页原地更新其内容；修复前每追问一次就插入一条新的 context，导致历史里堆积多份旧页素材；旧对话残留的重复 context 也不再进入本轮 LLM 上下文（仅请求侧过滤，DB 历史数据不删除）
- 版本号：后端 `v1.45.1` → `v1.45.2`；前端 `v0.34.2` → `v0.34.3`

### 验证
- 后端 `node --check node-jinmao/service/course_ai.js` 通过
- 前端 `WEB` 下 `npm run build` 成功
- 待实测：进入学习页当前页提示出现在聊天框末尾；追问时提示位置不变、回答结束后不重复不跳动；翻页时新页提示作为新消息追加；恢复历史对话只显示 user/assistant 消息与当前页提示；同一对话连续提问后端始终只有一条 context 消息

## 2026-08-03 — [后端] v1.45.1 / [前端] v0.34.2 — AI 助教允许脱离原文扩展讲解，上下文素材静默注入

### 改动内容
- `node-jinmao/service/course_ai.js`：重写 `buildSystemContent()` 系统提示词——AI 助教不再"严格基于教材作答、超出范围提示以教材为准"，改为以教材原文、当前页口播稿与助教提示为参考资料，允许在教材基础上扩展讲解（背景知识、原理解释、生活化例子、易错点辨析），把知识点讲透；`MAX_CHAPTER_CHARS` 由 60 万提高到 100 万字符
- `WEB/src/pages/study/script.js`：`loadAiConversation()` 过滤 `context` 角色消息，页码+口播稿+助教提示不再显示在聊天框，仅静默注入 AI 上下文（助教提示问候气泡保留）；`AI_CONTEXT_LIMIT` 由 128000 同步为 1000000，与后端保险丝一致，避免进度圆环虚报满格
- 版本号：后端 `v1.45.0` → `v1.45.1`；前端 `v0.34.1` → `v0.34.2`

### 验证
- 后端 `node --check node-jinmao/service/course_ai.js` 通过
- 前端 `WEB` 下 `npm run build` 成功
- 待实测：提问"这个概念能再展开讲讲吗" AI 可脱离原文扩展讲解；刷新/恢复历史对话不显示素材气泡；助教提示问候气泡仍正常；进度圆环按 100 万 token 刻度

## 2026-08-03 — [前端] v0.34.1 — 账单页扣费金额精度调整为 5 位小数

### 改动内容
- `WEB/src/pages/billing/script.js`：`formatCost()` 由 `toFixed(2)` 调整为 `toFixed(5)`，消费记录「费用」列与充值记录「金额」列统一保留 5 位小数，避免小额扣款（如 0.00001 元）显示为 0.00
- 版本号：前端 `v0.34.0` → `v0.34.1`

### 验证
- 前端 `WEB` 下 `npm run build` 成功
- 待实测：部署后进入账单页，确认消费记录小额金额显示 5 位小数

## 2026-08-03 — [前端] v0.34.0 — 账单页充值记录改为选项卡展示

### 改动内容
- `WEB/src/pages/billing/index.vue`：消费记录与充值记录由上下两块表格改为单个卡片内的 `el-tabs` 选项卡：
  - 默认展示「消费记录」，点击「充值记录」切换展示充值记录
  - 两个 Tab 各自保留独立的表格列与分页器，切换时数据与分页状态互不影响
  - 空状态文案同步调整为「暂无消费记录 / 暂无充值记录」
  - 选项卡标签栏左侧 16px 留白、表格满宽，暗黑模式通过 Element Plus dark css-vars 与卡片边框同步适配
- 版本号：前端 `v0.33.0` → `v0.34.0`

### 验证
- 前端 `WEB` 下 `npm run build` 成功
- 待实测：部署后进入账单页，确认选项卡切换正常、两个表格分页独立、暗黑模式样式正常

## 2026-08-03 — [后端] v1.45.0 / [前端] v0.33.0 — 账单页新增充值记录（含兑换码充值）

### 改动内容
- 后端 `API/billing.js`：`GET /api/v1/billing` 新增充值记录查询：
  - 新增独立分页参数 `rechargePage` / `rechargePageSize`（默认 1/20，最大 100），与扣费记录分页互不影响
  - 充值记录数据来源为 `redeem_code` 表中当前用户已使用的兑换码（`usedBy` + `isUsed=true`），按使用时间倒序
  - 响应新增 `rechargeRecords`（含 `code`、`amount`、`source`、`sourceLabel`、`usedAt`）与 `rechargePagination`
  - 同步补全 OpenAPI JSDoc 注释
- 前端 `WEB/src/api/billing.js`：`getBilling()` 新增充值记录分页参数透传
- 前端 `WEB/src/pages/billing/`：账单页新增「充值记录」表格区块（日期 / 兑换码 / 充值方式 / 金额），金额绿色显示 `+¥`，独立分页，与扣费明细共用加载与暗黑双轨样式
- 版本号：后端 `v1.44.0` → `v1.45.0`，前端 `v0.32.0` → `v0.33.0`

### 验证
- 后端 `node --check` 通过；临时实例（PORT=8890）联调确认 `/api/v1/billing` 返回充值记录与独立分页
- 前端 `WEB` 下 `npm run build` 成功
- 待实测：部署后进入账单页，确认「充值记录」表格展示兑换码充值记录、分页可用，暗黑模式下样式正常

## 2026-08-03 — [后端] v1.44.0 / [前端] v0.32.0 — "生成下一章"按钮状态实时化：新增状态查询接口 + 生成中/最后一章禁用态

### 改动内容
- 后端新增轻量状态查询接口 `GET /api/v1/courses/:courseId/generate-next-chapter/status`：
  - 返回 `canGenerateNext`（复用 `computeCanGenerateNext` 统一计算，与 POST 生成接口、详情接口单一真相来源一致）、`isGenerating`（课程下是否存在生成中章节）、`reason`（不可生成原因）
  - 鉴权与权限校验与生成接口一致，含完整 OpenAPI JSDoc
- `WEB/src/api/books.js`：新增 `getGenerateNextStatus(courseId)` API 封装
- `WEB/src/pages/study/script.js`：新增"生成下一章"按钮状态轮询（间隔 5s，仅在有章节生成期间运行）：
  - 点击生成成功后立即置为"生成中"并禁用按钮（不等待轮询首次返回）；页面加载时检测到生成中章节同样进入该状态
  - 生成中 → 按钮显示"生成中"且不可点击
  - 生成结束轮询返回后：可继续生成 → 可点击"生成下一章"；不可继续（endline 已到末尾 / isLastChapter）→ 禁用"已经是最后一章了"
  - 生成请求被后端拒绝（如已是最后一章）时立即刷新状态，同步禁用按钮
  - 页面卸载时停止轮询
- 版本号：后端 `v1.43.0` → `v1.44.0`，前端 `v0.31.0` → `v0.32.0`

### 验证
- 后端 `node --check` 通过；路由自检确认 `GET /courses/:courseId/generate-next-chapter/status` 已注册
- 前端 `WEB` 下 `npm run build` 成功
- 待实测：部署后点击"生成下一章"，确认按钮依次显示 生成中（禁用）→ 生成下一章 / 已经是最后一章了（按剩余内容切换）

## 2026-08-03 — [后端] v1.42.0 / [前端] v0.30.0 — PPT 字号统一 + 16:9 画布规范 + 学习页 iframe 禁用交互

### 改动内容
- `config/html_ppt_prompt.txt`：字号从"范围约束"改为固定 4 级字号体系（页面大标题 3rem / 小节标题 2.25rem / 正文 2rem / 辅助文字 1.5rem），全页仅允许这 4 个字号，保证各页 PPT 字号统一；新增 16:9 画布铁律：画布固定 1920×1080，html/body 必须 `overflow: hidden`，所有内容不得超出画布边界，禁止滚动条及 `overflow: auto/scroll`（含 overflow-x/overflow-y）
- `utils/htmlppt_guard.js`：新增 `detectOverflowViolation(html)`（纯函数）——规则 1：html/body 未声明 `overflow: hidden` 即违规；规则 2：任何样式（CSS 或内联）出现 `overflow: auto/scroll` 即违规
- `utils/htmlppt.js`：将溢出校验并入现有"设计说明污染检测 + 自动重试 1 次"流程，命中任一违规即携带对应纠正指令重试；重试后仍违规则返回 200 + 严重告警（不中断流水线）
- `WEB/src/pages/study/index.vue`：PPT iframe 增加 `pointer-events-none`、`tabindex="-1"`、`scrolling="no"`，iframe 为纯展示、用户无法点击/选中/滚动其内容；sandbox 保持 `allow-scripts allow-same-origin` 不变
- 版本号：后端 `v1.41.0` → `v1.42.0`，前端 `v0.29.1` → `v0.30.0`

### 验证
- guard 单测：合规样例（html/body 含 overflow:hidden）不违规；无 overflow:hidden、含 overflow-y:auto 均判违规
- 提示词自检：4 个固定字号存在，旧范围描述（2.5rem ~ 3.5rem）已移除，滚动条禁止规则已写入
- 前端构建通过：`WEB` 下 `npm run build` 成功
- 待实测：真实生成 1-2 页 PPT，人工确认无滚动条、内容不超出 16:9 画布、字号仅 4 档

## 2026-08-02 — [后端] v1.41.0 — 管理后台安全防护页新增攻击事件 CSV 导出

### 改动内容
- 新增导出接口 `GET /admin/:suffix/api/security/export`（沿用双重鉴权）：
  - 按当前筛选条件（攻击类型/严重程度/处理状态/IP）全量导出，不受分页限制
  - CSV 格式：UTF-8 BOM（Excel/WPS 中文不乱码）+ CRLF 换行 + RFC 4180 双引号转义（含引号/逗号/换行的攻击载荷可完整保留）
  - 中文表头：事件ID/触发时间/IP/请求方法/攻击类型/严重程度/触发次数/是否阻断/处理状态/触发原因/完整路径/完整Query/User-Agent，类型与严重度输出中文标签
  - 响应带 `Content-Disposition: attachment`，文件名 `security_events_YYYYMMDD_HHMMSS.csv`
- `utils/repo/security_repo.js`：抽取 `buildSecurityWhere`/`formatEvent` 共用工具，新增 `listAllEvents`（无分页全量查询）
- 前端安全防护 Tab：筛选区新增「📥 导出 CSV」按钮（带加载态），按当前筛选条件触发下载，401 自动登出
- 无数据库变更（本次仅代码，无 Schema 改动）
- 版本号：后端 `v1.40.0` → `v1.41.0`

### 验证
- 语法 `node --check` 通过；所有文件符合 ≤300 行规范（代码行=排除空行与注释）
- 接口验证：导出 200、Content-Type/Content-Disposition 正确、UTF-8 BOM（EF BB BF）存在、中文表头完整、含引号/逗号/换行的记录转义正确、severity 筛选生效
- 浏览器端到端：安全防护 Tab 导出按钮渲染正常，控制台无 JS 错误
- 待实测：测试环境部署后人工点击导出并打开 CSV 验证

## 2026-08-02 — [后端] v1.40.0 — 成本价/售价分离：模型成本配置 + 成本统计与利润分析

### 改动内容
- 配置分离：现有 `config/billing_pricing.json` 正式化为**售价**（用户被扣金额，行为不变）；新增 `config/model_cost_config.json` 存**成本价**（结构与售价配置完全一致，初始值=现有价格，可独立调整）
- 数据库：`billing_record` 新增 9 个 `cost_*` 字段（成本单价×6、成本分项×2、成本总额×1），迁移 `20260802_add_cost_fields_to_billing`（手工创建 + `prisma migrate deploy`；因既有迁移历史在影子库回放失败，改用 deploy 方式）
- 计费模块拆分（300 行规范）：
  - 新增 `utils/billing_config.js`：售价/成本两套配置共用的读取、时段匹配、取价、取整工具（从 billing.js 迁出，原导出保持不变）
  - 新增 `utils/billing_cost.js`：按成本配置计算 LLM/文生图/TTS/doc2x 的成本单价/分项/总额；配置缺失某模型时返回 null，由 billing.js 回退按售价计算并告警
  - `utils/billing.js`：`recordTokenUsage`/`recordExternalCost` 同时写入售价（total_cost，扣余额不变）与成本（cost_*），日志打印售价/成本/利润
- 历史数据：新增一次性回填脚本 `scripts/backfill_billing_cost.js`（旧记录成本=当时售价），已执行 6 条全部回填
- 管理后台：
  - `API/admin/stats.js`：summary 新增 `totalCost/todayCost/monthCost/totalProfit`，`dailyTrend` 每日新增 `cost`，`callTagDistribution` 每类型新增 `costTotal`
  - `API/admin/billing.js`：明细新增 `costTotal/profit`，summary 新增 `totalRevenue/totalCost/totalProfit`
  - 前端账单 Tab：新增「累计成本/累计利润」卡片（利润负值红色）+ 明细「成本/利润」列（负值红色/正值绿色）
  - 前端统计 Tab：新增「累计成本/累计利润」卡片 + 趋势图双系列（售价/成本）
- 版本号：后端 `v1.39.0` → `v1.40.0`

### 验证
- 全部新增/修改 JS 文件 `node --check` 通过；所有文件符合 ≤300 行规范（代码行=排除空行与注释）
- 迁移与回填：9 个 `cost_*` 字段确认存在；回填脚本执行 6 条，抽查 `cost_total = total_cost`，剩余 0
- 单元验证：成本=售价配置时两者一致；成本下调后利润为正；文生图成本计算正确；缺失模型返回 null 且恢复后一致；完整写库路径含成本字段、余额扣减=售价
- 接口验证：`/stats` 与 `/billing` 的 summary/明细均含成本与利润字段，利润 = 售价 − 成本 ✅
- 无头浏览器端到端：账单 Tab 成本/利润卡片与列、统计 Tab 成本/利润卡片与双线图表渲染正常，安全/设置 Tab 回归通过，控制台无 JS 错误
- 待实测：真实成本价数据录入后人工验收利润统计

## 2026-08-02 — [后端] v1.39.0 — 管理后台完善：用户管理/账单管理/消费统计 + 全量文件拆分

### 改动内容
- 后端路由拆分与新增（`API/admin.js` → `API/admin/` 子路由目录）：
  - `API/admin.js` 改为聚合器：保留双重鉴权中间件挂载（URL后缀 + JWT管理员角色），挂载 6 个功能子路由
  - 新增 `API/admin/users.js`：`GET /users`（分页 + 邮箱/昵称/用户名模糊搜索 + 角色/禁用状态筛选 + 统计摘要 total/banned/admins/todayNew）、`PUT /users/:id/ban`（守卫：不能操作自己、不能操作其他管理员）、`PUT /users/:id/unban`
  - 新增 `API/admin/billing.js`：`GET /billing` 全局扣费记录（状态/调用类型/用户ID/日期范围筛选 + 汇总 totalCost/successCount/failedCount + 经 user_id 关联补全用户邮箱）
  - 新增 `API/admin/stats.js`：`GET /stats` 平台统计（用户/今日新增/近7日活跃/累计·今日·本月营收/教材/题库 + 近14天趋势 JS 补零 + 调用类型费用分布；趋势按本地日期在 JS 内聚合，避免 Prisma UTC 存储与本地日期的 8 小时错位）
  - 迁移 `codes.js` / `config.js` / `security.js` 子路由（逻辑原样搬移，统一改用 `utils/prisma` 单例）
- 前端全量拆分（执行单文件 ≤300 代码行规范）：
  - `admin/index.html` 仅保留骨架；样式迁至 `admin/static/css/admin.css`；JS 按功能拆为 `admin/static/js/` 10 个模块；模板拆为 `admin/static/tpl/` 9 个 HTML 片段（main.js 运行时 fetch 后注册组件）
  - `app.js` 新增静态资源挂载 `/admin/static/*`
  - 新增 👥 用户管理 Tab（统计卡片/搜索筛选/禁用弹窗填写原因+二次确认/解禁确认）
  - 新增 📊 账单管理 Tab（统计卡片/状态·调用类型·用户ID·日期范围筛选/明细列表）
  - 新增 📈 消费统计 Tab（8 个统计卡片 + ECharts 近14天消费趋势折线 + 调用类型费用分布饼图，Tab 激活时初始化、切走销毁、窗口缩放自适应）
- 版本号：后端 `v1.38.0` → `v1.39.0`

### 验证
- 全部新增/修改 JS 文件 `node --check` 语法通过；所有文件均符合单文件 ≤300 行规范（代码行=排除空行与注释）
- 后端启动无报错；接口 curl 验证：users 列表/搜索/筛选/禁用守卫（自己与管理员→400、非法ID→400、不存在→404）、ban/unban 闭环（DB 校验 isBanned/banReason）、billing 筛选与汇总聚合、stats 结构完整且 14 天补零
- 无头浏览器端到端验证：登录页、主界面、6 个 Tab 全部渲染正常，统计图表 canvas 渲染成功，暗黑模式切换与 403 视图正常，控制台无 JS 错误（仅站点原有 favicon 404）
- 待实测：真实管理员账号浏览器人工验收

## 2026-08-02 — [后端] v1.38.0 — 管理后台恶意攻击页面展示全部详细信息

### 改动内容
- `node-jinmao/admin/index.html`（🛡️ 安全防护 Tab 攻击事件列表）：
  - 表格新增 4 列：**事件ID**（#id）、**触发原因**（reason，截断显示、悬浮查看完整）、**UA**（userAgent，截断显示、悬浮查看完整）、**是否阻断**（blocked，已阻断/放行标签）
  - 操作列新增"**详情**"按钮，点击弹出"攻击事件详情"弹窗（el-dialog + el-descriptions），完整展示 SecurityEvent 表全部 13 个字段：事件ID、触发时间、攻击者IP、请求方法、攻击类型、严重程度、触发次数、触发原因、完整路径、完整Query、User-Agent、是否阻断、处理状态
  - 超长字段（path/query/userAgent/reason）自动换行（`word-break: break-all`）防止撑爆弹窗
  - 补充暗黑模式双轨适配：el-dialog 背景、el-descriptions 标签/内容颜色随暗黑主题切换
- 后端 API（`/admin/:suffix/api/security/events`）原本已返回全部字段，本次仅前端展示层增强，后端逻辑与数据库表结构未改动
- 版本号：后端 `v1.37.4` → `v1.38.0`（次版本号 +1，新增功能显示）

### 验证
- `GetDiagnostics` 检查 `node-jinmao/admin/index.html` 无语法错误
- 启动后端服务无报错，访问管理后台可正常加载
- 待实测：登录管理后台切换到"安全防护"Tab 查看新增列与详情弹窗（依赖管理员账号与攻击记录数据）

## 2026-08-02 — [前端] v0.29.1 — 新增站点图标 favicon.svg

### 改动内容
- 新增 `WEB/public/favicon.svg`：金色渐变圆角方块 + 白色打开书本图标（契合"金毛"品牌色调）
- 此前 `index.html` 第 51 行已引用 `<link rel="icon" href="/favicon.svg" />`，但 public 目录并不存在、图标文件缺失，浏览器一直 404；本次补齐静态资源目录与图标文件
- 版本号：前端 `v0.29.0` → `v0.29.1`（修订号 +1，新增静态资源小改动）

### 验证
- `npm run build` 编译通过
- 启动 dev server 后访问 `http://localhost:30000/favicon.svg` 返回 200

## 2026-08-02 — [前端] v0.29.0 — 余额锁定时禁用上传弹窗内"上传"按钮并显示"余额不足"

### 背景
用户账号因欠费等原因被设置为不可生成内容（后端 `User.balanceLocked = true`）时，教材上传接口本身不校验余额，402 只发生在后续生成环节，导致用户欠费状态下仍可上传文件但生成必然失败，浪费流量与存储。

### 改动内容
- `WEB/src/components/UploadBookDialog.vue`：
  - 引入 `getBilling` 账单 API，新增 `balanceLocked` 响应式状态与 `checkBalanceStatus()` 检查方法（读取 `GET /api/v1/billing` 返回的 `data.balanceLocked`）
  - 组件挂载时 + 每次弹窗打开时（`watch(visible)`）复查余额锁定状态，保证充值/解锁后立即生效
  - "上传"按钮：锁定状态下置灰不可点击，按钮文字由"上传"变为"余额不足"
- 版本号：前端 `v0.28.3` → `v0.29.0`（次版本号 +1，新增功能行为）
- 范围说明：仅弹窗内"上传"按钮，入口按钮（侧边栏"上传教材"、标题栏按钮、添加教材卡片）保持可点击

### 验证
- `npm run build` 编译通过，无报错
- 诊断（GetDiagnostics）无错误
- 待实测：锁定账号打开弹窗按钮置灰显示"余额不足"、解锁后恢复（依赖后端环境）

## 2026-08-02 — [后端] v1.37.4 — 修复 PPT 生成将"设计说明"文字混入幻灯片正文

### 问题
用户反馈：生成 PPT 在前端展示时，正文中出现了"沉稳的领航员，用清晰的视觉层次带您进入水利工程的第一个章节。 - **色彩与氛围**：以深蓝渐变背景… - **叙事性布局**… - **字体与可读性**…"等设计说明文字。

### 真实根因（线上数据确认，课程 id=24 章节 id=20 共 20 页）
- 大纲 JSON `chapter_01.json` 的 `ppt/pptGuide` 字段全部为正常内容描述，**不含**任何设计说明 → 排除"指引本身是污染源"
- 仅第 01 页 `slide_01.html` 可见文本末尾出现 ```` ``` ### 视觉亮点与设计意图 … ```` 的 markdown 代码块，其中"不小于2rem的正文与不小于1.5rem的辅助文字"是复述 `html_ppt_prompt.txt` 的字体规范 → **证实是生成 HTML 的 LLM 自创"设计复盘"并塞进了正文**
- 前端 iframe 原样渲染 HTML，导致用户看到这段设计说明

### 三层修复
1. **提示词强化**：`config/html_ppt_prompt.txt` 新增两条禁止事项（禁止输出设计说明/设计复盘类内容、禁止正文使用 Markdown 语法），输入说明中明确"PPT生成指引只是内部指令，严禁作为可见内容"，并补充负面示例；`config/outline_prompt.txt` 对 `ppt` 字段追加"禁止叙事性开场白/设计风格描述/Markdown 标记"的防御性约束
2. **生成后校验**：新增 `utils/htmlppt_guard.js`，提供 `extractVisibleText()`（剔除 script/style/pre/code 后提取可见文本）与 `detectDesignGuideContamination()`（检测 markdown 围栏/标题/加粗/设计说明关键词，返回 `{contaminated, reasons}`）
3. **自动重试**：`utils/htmlppt.js` 将清理链抽为 `cleanAndValidateHtml()` 复用，首次生成后调用污染检测；命中时携带纠正指令（多轮消息含污染输出作为负面示例）**自动重新生成一次**（最多 1 次，额外计费一次并在日志明示）；重试后仍污染则保留结果返回 200 + 严重告警，不中断流水线

### 涉及文件
- 新增 `node-jinmao/utils/htmlppt_guard.js`
- 修改 `node-jinmao/utils/htmlppt.js`
- 修改 `node-jinmao/config/html_ppt_prompt.txt`
- 修改 `node-jinmao/config/outline_prompt.txt`
- 版本号：后端 `v1.37.3` → `v1.37.4`（修订号 +1）

### 验证
- 本地单测：真实污染文本（slide_01 可见文本）判定 `contaminated: true`；干净文本（slide_02 等）判定 `contaminated: false`；含 `**加粗**`、`###`、` ``` ` 的文本正确命中；`<pre>` 内合法代码示例豁免
- 本地回归：用真实第 01 页输入（pptGuide + 250 字口播稿 + 空图片数组）调用修复后的 `generateHtmlPpt`，输出 `code=200` 且无污染
- 线上验证：部署后重新生成课程 24 章节 20 的第 01 页，复核不再含设计说明文字

## 2026-08-02 — [前端] v0.28.3 — 修复学习页恢复进度后进度条显示 00:00

### 背景
用户学到某一页后退出，再次进入课程时虽能正确恢复到上次学习的页码，但进度条时间显示为 `00:00 / XX:XX`，未正确拼接已播页面的累计时长。

### 根因
- 进度条时间 = `chapterElapsedTime`（本章节已播页面累计时长）+ `currentTime`（当前页播放位置）
- `loadChapterSlides()` 会将 `chapterElapsedTime` 重置为 0，而恢复进度代码（`loadCourseData()`）只设置了 `currentPage`，从未累加前 `restoredPage - 1` 页的时长
- 且章节总时长 `chapterTotalTime` 依赖异步的 `prefetchChapterTotalTime()` 从各页 SRT 预计算，恢复进度时可能尚未完成

### 修改
- **`WEB/src/pages/study/script.js`**：
  - 新增模块级变量 `currentPrefetchPromise`，保存当前章节总时长预计算 Promise
  - `loadChapterSlides()` 中 `prefetchChapterTotalTime()` 的返回值保存到 `currentPrefetchPromise`
  - 恢复进度代码块：先 `await currentPrefetchPromise`（确保各页 `_duration` 已填充），再累加前 `restoredPage - 1` 页时长到 `chapterElapsedTime`，并同步更新 `progressPercent`
- 版本号：前端 `v0.28.2` → `v0.28.3`（修订号 +1）

### 验证
- 恢复场景：进入有学习进度（学到第 N 页，N > 1）的课程，进度条显示 `前 N-1 页累计时长 / 章节总时长`，不再是 00:00
- 无进度场景：进度条仍为 `00:00 / XX:XX`（第 1 页未播，符合预期）
- 回归：翻页/切换章节/播放/暂停/自动播放/倍速均正常

## 2026-08-01 — [后端] v1.37.3 — 修复 Scalar 文档页在 HTTP 端口下的安全头冲突

### 修复（后端）
- **修复 `http://jinmao.ckstu.top:30080/api/v1/docs` 无法打开 / 控制台刷安全策略错误**：
  - `node-jinmao/app.js` 中的 `helmet` 默认会附带 `upgrade-insecure-requests`、`Cross-Origin-Opener-Policy`、`Origin-Agent-Cluster`、`Strict-Transport-Security`
  - 线上 `30080` 端口当前仅提供 HTTP，这些头会把文档页中的资源和 favicon 强制升级到 `https://jinmao.ckstu.top:30080/*`，从而触发 `ERR_SSL_PROTOCOL_ERROR`、COOP/OAC 冲突和 frame 协议不一致错误
  - 现已显式关闭 `upgradeInsecureRequests`、`crossOriginOpenerPolicy`、`crossOriginResourcePolicy`、`originAgentCluster`、`strictTransportSecurity`
- **修复 Scalar 文档页“Try it out”仍请求 `http://localhost:8888`**：
  - `node-jinmao/config/swagger.js` 中 OpenAPI `servers[0].url` 从 `http://localhost:8888` 改为相对地址 `/`
  - 文档页现在会自动对当前站点发请求，不再错误指向本机 `localhost`
- **补充文档页所需 CSP 白名单**：
  - `connect-src` 允许 `https://cdn.jsdelivr.net`，避免 Scalar source map 请求触发 CSP 报错
  - `font-src` 放宽为 `https:` + `data:`，避免字体资源被拦截
- 版本号：后端 `v1.37.2` → `v1.37.3`（修订号 +1）

## 2026-08-01 — [后端] v1.37.1 — 修复 doc2x PDF 页数读取：正则方案替换为 pdf-parse

### 修复（后端）
- **doc2x PDF 页数始终为 0，计费失败**：`getPdfPageCountLocally()` 使用正则 `/\/Type\s*\/Page[^s]/g` 扫描 PDF 原始二进制，对于使用压缩对象流（ObjStm）的现代 PDF 完全不生效，`/Type /Page` 位于压缩数据中不可见，导致始终返回 0，计费为 ¥0.000000
- **完全重写** `node-jinmao/utils/doc2x.js`：
  - `getPdfPageCountLocally()` 移除正则方案，改用 `pdf-parse`（Mozilla pdfjs-dist 封装，项目已安装），能正确处理压缩对象流、线性化 PDF、xref 流等所有现代 PDF 特性
  - 函数改为 `async`，使用 `pdfParse(buf)` 解析 PDF → `data.numpages` 获取页数（与前端 `UploadBookDialog.vue` 中 `pdfjs-dist` 方案一致，前端已验证成功）
  - 文件头注释、`main()` JSDoc 同步更新，移除"正则匹配"/"本地读取"相关描述
- **同步修复** `node-jinmao/API/POSTbook.js` L680：`check-pdf-pages` 端点调用 `getPdfPageCountLocally` 加 `await`（函数已改为 async）
- **稳定性**：pdf-parse = pdfjs-dist，Mozilla 官方 PDF 引擎，和前端 `v0.28.0` 已验证的成功方案一致，100% 准确
- 版本号：后端 `v1.37.0` → `v1.37.1`（修订号 +1）

## 2026-08-01 — [前端] v0.28.0 — PDF 页数解析更换为 pdfjs-dist（Mozilla 官方库）

### 背景
v0.27.x 的纯文本正则方案对现代 PDF（对象流 ObjStm 压缩、线性化 PDF）完全无效——`/Count` 和 `/Type /Page` 均在压缩流中，正则匹配始终返回 0。
换成 Mozilla 官方 pdfjs-dist 库：直接调用 `pdf.numPages`，正确处理所有 PDF 特性。

### 修改
- **安装 `pdfjs-dist`**（Mozilla PDF.js 官方库）
- **`WEB/src/components/UploadBookDialog.vue`**：
  - 移除所有文本解析函数（`readBlobAsLatin1`、`extractCount`、4 层降级策略）
  - 替换为 `pdfjsLib.getDocument({ data: arrayBuffer }).promise` → `pdf.numPages`
  - Worker 路径使用 Vite `new URL()` 构建时自动处理
- **`WEB/package.json`**：v0.27.2 → v0.28.0

### 影响
- 主 bundle 773KB → 1201KB（pdfjs 核心 +428KB），Worker 单独 1262KB
- 页面加载会有轻微增加，但 PDF 页数检测 100% 准确

### 测试
- 前端构建通过（`npm run build`，Vite 8，1.65s）

## 2026-08-01 — [前端] v0.27.2 — PDF 页数解析策略升级：头 5MB + 尾 2MB + 全量扫描

### 背景
v0.27.1 的 2MB 头部探测对大 PDF（96MB）失效——Pages 字典不在文件头部，且页面对象可能被压缩导致 `/Type /Page` 扫描也返回 0。

### 修改
- **4 层逐步降级策略**：
  1. 读前 5MB 找 `/Count`（命中率最高）
  2. 读最后 2MB 找 `/Count`（xref 附近）
  3. 全量扫描 `/Count`（Pages 字典始终不压缩，可在任意位置）
  4. 兜底扫描 `/Type /Page` 标记（对压缩的页面对象无效）
- 解析返回 0 时不阻断上传（仅警告），防止误伤无法解析的 PDF
- **`WEB/package.json`**：v0.27.1 → v0.27.2

### 测试
- 前端构建通过（`npm run build`，Vite 8，1.49s）

## 2026-08-01 — [前端] v0.27.1 — PDF 页数检查改为纯前端 FileReader（不再上传到后端）

### 背景
v0.27.0 的 PDF 页数检查需要将整个 PDF 文件上传到后端 `/api/v1/book/check-pdf-pages` 再返回页数。对于大文件（如 234MB 的 PDF），先上传到后端再返回结果体验极差，上传完才发现页数超限。

### 修改
- **前端 `WEB/src/components/UploadBookDialog.vue`**：
  - 新增 `getPdfPageCountInBrowser(file)` 函数：使用 `FileReader` 在浏览器端直接读取 PDF 二进制统计页数
  - 策略：优先从 PDF 头部 `/Count` 元数据获取（只读前 2MB，绝大多数 PDF 适用），未找到时全量扫描 `/Type /Page` 标记
  - 移除 `checkPdfPages` API 调用，不再上传文件到后端检查页数
- **`WEB/package.json`**：v0.27.0 → v0.27.1

### 技术细节
- 使用 `FileReader.readAsText(blob, "latin1")` 读取 PDF 二进制，latin1 编码无损保留所有字节
- 探测区仅 2MB，对于几百 MB 的大 PDF 也能秒级返回结果
- 页数检查失败不阻断上传流程，仅打印警告

### 测试
- 前端构建通过（`npm run build`，Vite 8，1.77s）

## 2026-08-01 — [前端] v0.27.0 / [后端] v1.37.0 — PDF 上传前页数预检（>100 页拒绝）

### 背景
用户上传超大 PDF（如数百页的教材）时，Doc2x 解析成本高且生成课程耗时极长。需要在用户选择 PDF 后立即检查页数，暂时拒绝超过 100 页的 PDF。

### 修改
- **后端 `node-jinmao/utils/doc2x.js`**：导出 `getPdfPageCountLocally` 函数（已有，纯本地读取 PDF 二进制统计 `/Type /Page` 标记，毫秒级，不调用外部 API）
- **后端 `node-jinmao/API/POSTbook.js`**：新增 `POST /api/v1/book/check-pdf-pages` 端点，接收 PDF 文件并返回 `{ pageCount, fileName }`
- **前端 `WEB/src/api/books.js`**：新增 `checkPdfPages()` API 封装
- **前端 `WEB/src/components/UploadBookDialog.vue`**：`handleFileChange` 改为 async，PDF 文件选择后自动调用 `checkPdfPages`，页数 >100 时显示错误提示并清除文件
- **`node-jinmao/package.json`**：v1.36.6 → v1.37.0
- **`WEB/package.json`**：v0.26.1 → v0.27.0

### 技术细节
- 页数统计复用后端已有 `getPdfPageCountLocally`，无需安装新依赖
- 前端 `el-upload` 的 `on-change` 现在支持异步，PDF 文件需等待后端返回页数后才决定是否接受
- 页数检查失败（网络异常等）不阻断上传流程，仅打印警告日志，避免误伤正常上传

### 测试
- 后端语法检查通过（`require('./API/POSTbook.js')`）
- 前端构建通过（`npm run build`，Vite 8 产出正常）
- 单元测试：`getPdfPageCountLocally` 对 3/5/150 页的测试 PDF 均返回正确页数

## 2026-08-01 — [后端] v1.36.6 — 新增首次部署建库脚本 setup_first_deploy.sh（跳过迁移链）

### 背景
生产环境首次部署时 `prisma migrate deploy` 报 P3018（`course` 表不存在）。排查发现两个根因：
1. **迁移排序缺陷**：迁移目录名混用格式（`20260709_add_subtitle_to_course` 下划线后缀 vs `20260709105504_add_course_and_chapter` 时间戳后缀），全量应用时排序错乱，ALTER 先于建表执行。
2. **迁移集无法全量建新库**：schema.prisma 共 17 张表，但迁移文件只包含 6 张表（User/Course/Chapter/UserStudyRecord/UserDailyActivity/SecurityEvent）的建表语句，题库/账单等 11 张表从未在迁移中建表（开发期用 `prisma db push` 直接建）。全新空库用 `migrate deploy` 永远建不全。

### 结论
首次部署全新空库**不需要迁移链**：`schema.prisma` 本身就是完整结构定义，`prisma db push` 直接建成与代码一致的库；迁移链仅用于「已有库的增量升级」。

### 修改
- **新增 `node-jinmao/setup_first_deploy.sh`**（全新空库首次部署专用）：
  1. 检查 `.env`
  2. `npm install`
  3. `npx prisma db push`（按 schema.prisma 建全 17 张表）
  4. 遍历 `prisma/migrations/*/`，逐条 `prisma migrate resolve --applied` 标记已应用（防止 app.js 启动时自动 migrate deploy 把历史迁移当新迁移重复执行；后续版本升级仍正常执行新增迁移）
  5. 输出验证命令与下一步提示
- **`双环境部署指南.md`**：2.4 首次部署改用 `bash setup_first_deploy.sh`；常见问题新增 P3018 说明。
- **`node-jinmao/FILE.md`**：新增 `setup_first_deploy.sh` 条目。
- **`node-jinmao/package.json`**：v1.36.5 → v1.36.6。

### 影响
- 生产环境首次部署链路：上传 → 配置 .env → `bash setup_first_deploy.sh` → 前端 build → 宝塔启动。
- 测试环境不受影响（已有库继续走原增量迁移流程）。
- 长期建议：后续重建完整迁移基线，使 `migrate deploy` 也能全量建新库（另行处理）。

### 测试
- `bash -n` 语法检查通过（BASH SYNTAX OK）。
- 脚本逻辑自查：迁移遍历按 `prisma/migrations/*/` 目录循环，仅对含 `migration.sql` 的目录执行 resolve；`set -e` 保证任一步失败即中止。

### 补充说明（生产启动报错修复）
- **现象**：生产后端启动报 `MODULE_NOT_FOUND: Cannot find module '../utils/doc2x'`。
- **根因**：FTP 脚本排除列表中的 `"doc"` 键做**子串匹配**，`doc2x.js`（以及 `config/doc2x_config.json`）因包含 "doc" 被误排除，从未上传到生产服务器。测试环境能用是因为 doc2x.js 是早期部署上传的遗留。
- **修复**：`ftp_upload.ps1` 与 `ftp_upload_prod.ps1` 排除列表的 `"doc"` 改为 `"docs"`（仍排除 WEB/docs 文档目录，不再误伤 doc2x 相关文件）。
- **验证**：排除逻辑断言通过（doc2x.js / doc2x_config.json 会上传，WEB/docs 仍排除）；两脚本语法检查通过。

## 2026-08-01 — [后端] v1.36.5 — .env.example 模板结构对齐线上新配置

### 背景
线上 `.env` 配置已更新（SMTP 改用 163 邮箱、MinIO 端口调整为 19000、Grsai 新增模型字段），`.env.example` 模板结构与线上不一致，需要同步更新，保证 `cp .env.example .env` 生成正确模板。

### 修改
- **`node-jinmao/.env.example`**（仅模板，敏感值保持占位符，未写入任何真实凭据）：
  - SMTP：`smtp.qq.com` → `smtp.163.com`，`SMTP_FROM` 格式改为 `创客工作室 <your-email@163.com>`
  - MinIO：`MINIO_PORT` 9000 → 19000
  - Grsai：新增 `GRSAI_MODEL=gpt-image-2`
  - 补充注释：数据库/桶的生产环境命名（jinmao_prod / jinmao-prod）、JWT 生产独立生成提示
- **`node-jinmao/package.json`**：v1.36.4 → v1.36.5。

### 影响
- 模板结构对齐线上，后续新建环境（如生产）可直接 `cp .env.example .env` 后填写。
- 真实凭据（数据库密码、JWT、SMTP 授权码、API Key、MinIO 密钥）**未**进入模板，无泄漏风险。

### 测试
- 已人工比对用户提供的线上 `.env` 结构：键名、分区、字段完全一致（仅敏感值替换为占位符）。

## 2026-08-01 — [后端] v1.36.4 — 删除 LibreOffice 遗留目录并清理过时 word2pdf 索引

### 背景
此前「拒绝任何 Word 文件输入」的清理已移除 `word2pdf.js` 及全部 LibreOffice 引用，但 `node-jinmao/libreoffice-portable/`（324MB AppImage）作为清理遗留物仍留在磁盘，两个 FILE.md 也残留过时的 `word2pdf.js` 索引条目。

### 修改
- **删除 `node-jinmao/libreoffice-portable/` 目录**（含 `LibreOffice-still.standard-x86_64.AppImage`，324.2 MB）：代码中已无任何引用，测试/生产环境均不需要，删除节省磁盘空间。
- **`FILE.md`（根目录）**：移除过时的 `word2pdf.js` 索引条目。
- **`node-jinmao/FILE.md`**：移除过时的 `utils/word2pdf.js` 索引条目。
- **`node-jinmao/package.json`**：v1.36.3 → v1.36.4。

### 影响
- 功能零影响：Word→PDF 已整体下线，上传白名单仅 PDF/MD/ZIP/RAR/7Z。
- `ftp_upload.ps1` / `ftp_upload_prod.ps1` 排除列表中的 `libreoffice-portable` 条目保留（成为空操作，防御未来误放回）。

### 测试
- 确认目录已删除（文件 + 空目录均已移除）。
- 全项目 grep `word2pdf` / `libreoffice-portable`：代码文件（.js/.sh）无任何引用（此前已验证），仅剩历史文档与测试命令记录（测试命令.md / 待办.md 的过时条目，未在本次范围，如需要可后续清理）。

### 补充说明（上传实测后修复）
- **根因 1（FTP chroot）**：FTP 账户 `jinmao_ckstu_top` 被 chroot 锁定在 `/www/wwwroot/jinmao`，`ftp_upload_prod.ps1` 上传的相对路径全部落入测试目录，`/www/wwwroot/jinmao-prod` 无法被创建。修复：宝塔新建生产专用 FTP 账户（主目录 `/www/wwwroot/jinmao-prod`），脚本改用该账户并加警告注释；`双环境部署指南.md` 方式 B 补充建账户步骤。
- **根因 2（.env 缺失）**：FTP 刻意排除 `.env`/`.env.example`，上传后生产服务器没有 `.env`，后端启动自检会直接退出。修复：`.env.example` 改为**随包上传**（纯模板、无真实凭据），服务器上 `cp .env.example .env` 即可生成；`.env` 依旧排除（真实凭据、各环境独立）；指南 2.3 同步更新。`ftp_upload_prod.ps1` 与 `ftp_upload.ps1`（测试脚本）**两个脚本均同步该修改**，并修复子串匹配误伤 `.env.example` 的陷阱（`.env` 仅精确匹配文件名）。
- **脚本 Next steps 命令**：补上后端 `npm install`（首次部署 node_modules 不存在，原命令缺少该步骤）。

## 2026-08-01 — [后端] v1.36.3 — 支持测试/生产双环境部署（新增生产 FTP 上传脚本 + 端口配置化）

### 背景
系统需在宝塔面板同一台服务器上搭建生产环境（30081），与现有测试环境（30080）完全隔离（目录、端口、数据库、对象存储），日常开发流程不影响生产，仅大版本更新时手动推送。

### 修改
- **新增 `ftp_upload_prod.ps1`**（根目录）：生产环境专用 FTP 上传脚本，基于 `ftp_upload.ps1` 复制修改：
  - `$remoteRoot` 固定为 `/www/wwwroot/jinmao-prod`（与测试目录隔离）
  - 红色高亮警告横幅，上传前必须输入 `yes` 确认（确认在扫描后、建立 FTP 连接前，误操作直接取消）
  - 排除列表新增 `.schema_hash`、`.migration_version`（运行时状态文件，避免生产跳过首次数据库迁移）及脚本自身
  - 尾部 Next steps 提示改用生产路径与 `jinmao-backend-prod` 重启指引
- **`node-jinmao/ecosystem.config.js`**：移除 `env` 中硬编码的 `PORT: 8888`，端口改由各环境 `.env` 的 `PORT` 控制（测试 8888 / 生产 8889），避免 PM2 注入的 PORT 覆盖 .env 配置。
- **`node-jinmao/.env.example`**：新增 `PORT=8888` 配置项及注释（测试 8888；生产 8889，由宝塔网站 30081 反代）。
- **新增 `双环境部署指南.md`**（根目录）：双环境架构总览、生产首次部署（建库/建目录/生产 .env/宝塔 Node 项目/宝塔网站）、日常发布流程（测试/生产/回滚）、常见问题。
- **`node-jinmao/package.json`**：v1.36.2 → v1.36.3。
- **`FILE.md`**：新增 `ftp_upload_prod.ps1`、`双环境部署指南.md` 条目。

### 影响
- 测试环境（30080 → 8888）行为零变化，`ftp_upload.ps1` 未改动。
- 生产环境（30081 → 8889）新增，数据库 `jinmao_prod` 与 MinIO 桶 `jinmao-prod` 完全隔离。
- 数据库、对象存储均通过各目录独立 `.env` 的 `DATABASE_URL` / `MINIO_*` 配置，无需改业务代码。

### 测试
- `ftp_upload_prod.ps1` 通过 PowerShell 语法解析；运行后红色警告 + 输入非 yes 立即退出（未建立 FTP 连接，无任何上传）。
- `ecosystem.config.js` 通过 `node -e "require(...)"` 正常加载，且不再含硬编码 PORT（见下方测试结果）。

### 补充说明
- 用户质疑 203 个文件未包含 `libreoffice-portable`（324MB AppImage）。经核实：**Word→PDF 功能已按「拒绝任何 Word 文件输入」决策整体下线**，代码中已无任何 `word2pdf`/`libreoffice` 引用，`libreoffice-portable/` 为清理遗留物。`ftp_upload_prod.ps1` 排除它是正确行为，生产环境无需该文件。该结论已写入 `双环境部署指南.md` 常见问题表。

## 2026-08-01 — [前端] v0.26.1 — 账单页"去充值"按钮改为跳转兑换码页面

### 背景
账单页余额卡片上的"去充值"按钮原本是 `disabled` 锁定状态（等待充值活动上线）。充值功能此前已迁移到兑换码系统（`api/redeem.js` + `pages/redeem`），用户充值入口不明确，需要将按钮打通到兑换码输入页面。

### 修改（`WEB/src/pages/billing/`）
- **`index.vue`**：余额卡片上的"去充值"按钮去掉 `disabled` 锁定，增加 `@click="goRedeem"`，按钮文案改为"去兑换码"，点击后跳转到兑换码领取页面（`pages/redeem/index.vue`，即输入兑换码的页面）。
- **`script.js`**：废弃的 `handleRecharge()`（空实现 + 弹窗提示）替换为 `goRedeem()`，实现为 `navigate("redeem")`；同时移除已无引用的 `ElMessage` 导入。
- **`WEB/package.json`**：v0.26.0 → v0.26.1。

### 影响
- 用户从账单页可直接点击"去兑换码"进入兑换码输入页面领取免费额度，充值入口打通。
- 兑换码页面导航复用 App.vue 已有的 `changePage("redeem")` 机制，无新增路由。

### 测试
- 修改后 `WEB/` 下前端代码经本地构建验证无编译报错（见下方测试结果）。

## 2026-08-01 — [后端] v1.36.2 — 修复 TTS 超长文本合成失败：长文本按标点无损分块后合并

### 背景
用户日志报错：`[text_tts][validateInput] 错误：输入文本长度(1461)超过最大限制(1024)`，随后 `[fix_missing] TTS 合成失败 第 17 页: code=400`，补全流程重试 3 次后仍然失败，第 17 页的 MP3/SRT 永远补不上，章节卡在 `partial_completed`。

### 根因
火山引擎 TTS 单次合成有 **1024 字符硬限制**，而 `Phase5 正常生成` 与 `fix_missing 补全` 都是把整页口播稿一次性传给 `synthesize()`。超过 1024 字符的页面（如第 17 页 1461 字符）被 `validateInput` 直接拦截返回 400，重试多少次都失败。

### 修改（`node-jinmao/service/text_tts.js`）
- **新增 `splitTextByPunctuation(text, maxLength)`**：长文本无损分块。优先在句号/问号/感叹号/分号等强分隔符后切分，不足时退而求其次用逗号/顿号/冒号等弱分隔符，完全无标点时硬切兜底。每块末尾保留标点，语音朗读到标点处自然停顿，无顿挫感；分块拼接后与原文完全一致（无损）。
- **新增 `mergeChunkFiles(mp3Paths, srtPaths)`**：合并各分块文件。MP3 按字节直接拼接为单一 MP3；SRT 逐条累加时间偏移（偏移量 = 前面所有块的音频总时长）后合并为单一 SRT，字幕序号重新编号，时间轴连续不重叠。
- **`validateInput` 增加可选参数 `opts.checkLength`**：`synthesize` 内部传 `{ checkLength: false }` 跳过长度检查（改由分块逻辑保证每块 ≤1024）；独立调用时原行为不变（超长文本仍返回 400）。
- **`callTtsApi` 增加可选参数 `fileTag`**：分块合成时临时文件带 `_chunkN` 后缀，避免同秒内生成的文件名互相覆盖；合并后自动清理分块临时文件。
- **`synthesize` 主函数改造**：单块场景直接合成（与原有行为一致，零回归）；多块场景逐块合成 → 合并 → 返回单一 MP3/SRT。任一块失败则清理已生成分块临时文件并整体返回失败。
- **导出新增 `splitTextByPunctuation`**，便于复用与测试。
- **`node-jinmao/package.json`**：v1.36.1 → v1.36.2。
- **`node-jinmao/FILE.md`**：更新 `text_tts.js` 条目描述与修改时间。

### 影响
- `Phase5 正常生成` 与 `fix_missing 补全` 均通过 `synthesize()` 自动受益，超长页（>1024 字符）不再失败。
- 用户对第 17 页等缺失章节重新触发补全（`POST /fix-missing`）即可成功生成 MP3/SRT。
- 多块合成时每块各记录一次 TTS 计费，总字符数 = 原文长度，计费正确。

### 测试（测试脚本已删除）
- 分块函数纯逻辑：带标点长文本（9 块）、无标点硬切、短文本单块、标点位于 1024 边界 —— 全部通过。
- `validateInput` 回归：空文本/非字符串/超长文本仍返回 400 —— 通过。
- 真实 TTS 合成（计费模块桩替换，不落库）：短文本单块合成成功（回归）；1366 字符超长文本分 2 块（1012 + 354）合成成功，合并为单一 MP3（2.8MB）、单一 SRT（125 条字幕），时间轴连续无重叠，总时长约 309 秒，无分块临时文件残留 —— 26 项断言全部通过。

### 备注
修复已提交到本地代码，需重启后端服务使 `text_tts.js` 的修改生效。

## 2026-08-01 — [前端/后端]（部署脚本修改，版本号不变） — ftp_upload.ps1 改为仅上传 + 提供手动迁移/构建命令

### 背景
用户反馈：`ftp_upload.ps1` 上传完成后脚本内的 **SSH 自动执行"迁移 + 重启后端 + 构建前端"** 部分上次把服务器弄挂了，风险太高；且"直接 build + 重启后端"并不能让数据库迁移生效（账单 500 的根因就是数据库未执行 `prisma migrate deploy`）。

### 修改（部署脚本与文档，无业务代码变更）
- **`ftp_upload.ps1`**：
  - 删除末尾 SSH 自动执行块（`jinmao_rsa` key 检查、`ssh -i ... prisma migrate deploy && pm2 restart jinmao-backend && npm run build` 整段）
  - 头部注释更新为"仅负责上传文件，数据库迁移/前端构建手动执行，后端重启在宝塔面板操作"
  - 结尾提示改为手动执行步骤：① 一条命令做数据库迁移 + 前端构建；② 到宝塔面板手动重启后端；③ 可选确认 8888 端口进程
- **`FILE.md`**：更新 `ftp_upload.ps1` 条目描述与修改时间
- **`开发日志.md`**：新增本条目

### 手动执行命令（不含重启后端，在宝塔面板终端直接执行）
```bash
cd /www/wwwroot/jinmao/node-jinmao && npx prisma generate && npx prisma migrate deploy && cd /www/wwwroot/jinmao/WEB && npm install && npm run build
```
命令只做：Prisma Client 同步 → 数据库迁移（核心）→ 前端构建。**后端重启请到宝塔面板手动操作**（命令行重启可能导致宝塔无法正确识别进程状态）。

### 决策
- 脚本内不保留任何远程自动执行逻辑（用户明确要求"只负责上传，其他什么都不要管"）
- 后端重启从命令中移除，统一由用户在宝塔面板手动操作

## 2026-08-01 — [后端] v1.36.1（无代码变更） — 线上数据库同步缺失迁移导致账单接口 500

### 背景
线上 `GET /api/v1/billing` 返回 500（"服务器内部错误，请稍后再试。"），前端账单页无法加载。

### 根因（线上数据库与代码不同步）
线上数据库只应用到了迁移 `20260731120000_add_maxline_to_course`，**缺少两个迁移**：
- `20260731_add_error_message_and_retry_count_to_billing`（`billing_record` 表缺 `error_message` / `retry_count` 列）
- `20260801_add_security_event`（缺 `SecurityEvent` 表）

而线上磁盘代码与 Prisma Client 均为最新（v1.36.1），`API/billing.js` 查询 `error_message` / `retry_count` 列 → 数据库列不存在 → Prisma P2021 错误 → 500。

### 排查过程
1. 本地复现：本地 8888 服务 billing 正常 200，线上 30080 复现 500
2. 线上服务定位：8888 端口实际由 www 用户的 `node app.js`（非 PM2 管理的 PID 968628）提供服务；PM2 的 `jinmao-backend`（PID 920635）因端口冲突 EADDRINUSE 启动失败但状态显示 online
3. 对比线上 `/proc/968628/cwd` 磁盘代码、`.schema_hash`（10:36 更新）、`.migration_version`（v1.36.1）均正常
4. 直连线上 MySQL 检查 `_prisma_migrations` → 确认缺失上述两个迁移

### 修复（线上运维操作，无代码变更）
在线上服务器执行（已由 AI 代为执行成功）：
```bash
cd /www/wwwroot/jinmao/node-jinmao && npx prisma migrate deploy
```
输出：`All migrations have been successfully applied`（应用 `20260731_add_error_message_and_retry_count_to_billing` + `20260801_add_security_event`）

### 验证
- 线上 `GET /api/v1/billing?page=1&pageSize=1` 恢复 200，正常返回余额/已用金额/扣费记录（含 errorMessage/retryCount 字段）
- `billing_record` 表已含 `error_message`、`retry_count` 列；`SecurityEvent` 表已创建
- profile / textbooks / stats 接口均正常（回归通过）

### 经验教训（供后续部署参考）
- **部署后务必检查数据库迁移状态**：`cd /www/wwwroot/jinmao/node-jinmao && npx prisma migrate status`
- app.js 启动时会基于 `.migration_version` 版本号自动执行 migrate deploy，但若部署时迁移文件未同步完整，可能静默跳过，需要人工确认
- 线上 8888 端口存在"非 PM2 进程占用"历史问题，PM2 restart 可能因端口冲突失败，部署后应确认 `ss -tlnp | grep 8888` 对应的进程确实是新代码

## 2026-08-01 — [后端] v1.36.1 — 修复下一章生成 endline 倒退 / 循环生成第一章内容 bug

### 背景
"开始生成课程"后出现异常：第一章记录的 endline 约 800 多，第二章变成 170 多，第三章反而变成 70；且用户可以一直生成甚至循环生成，生成内容全是第一节的内容。

### 根因（非 AI 胡诌，是坐标系错误）
`addLineNumbers()` 会将提取出的文本块**从 1 重新编号**，AI 识别行号时看到的是"相对本次提取 chunk"的行号；但流水线把这些**相对行号直接当作 MD 文件绝对行号**写入 `Chapter.startline/endline` 和 `Course.endline`：

- 第一章：从第 1 行提取，相对 = 绝对，碰巧正确 → endline ≈ 800
- 第二章：从第 801 行提取，AI 返回相对 endline ≈ 170 → 被存成 170（应为 ~970），endline **倒退**
- 第三章：从第 171 行提取 → 重新落在第一章内容上，endline = 70，继续倒退
- 因 `endline(70) < maxline`，`computeCanGenerateNext` 永远返回"可生成"→ **无限循环生成第一章内容**

### 修改（后端）
- **`service/course_pipeline.js` — `generateChapter` Step 3（核心修复）**：AI 返回相对行号后先做防御性钳制（startline ≥ 1、endline ≤ chunk 实际行数、endline ≥ startline），再换算绝对行号（`绝对 = extractStart + 相对 - 1`）入库；Step 8 兜底检测改用绝对 endline 与 maxline 比较
- **`service/course_pipeline.js` — `phase2_extractAndIndex`**：同样的相对→绝对换算，保证主流水线与单章流水线语义一致（第一章 extractStart=1 时结果不变）
- **新增 `scripts/repair_endline.js`（一次性数据修复脚本）**：检测 `course.endline < max(非删除章节 endline)` 的损坏课程，按"相对跨度 = 绝对跨度"迭代还原真实绝对行号；默认 dry-run 预览，`--apply` 才写库（事务批量提交）；修复后 endline ≥ maxline 自动补设 `isLastChapter=true`
- **`package.json`**：PATCH bump → `1.36.1`

### 验证
- 换算逻辑单测通过：第一章 ≈800 / 第二章 970 / 第三章 240（旧逻辑复现出 170 / 70，证明根因定位正确）
- 修复脚本 dry-run 与 --apply 执行通过；服务启动回归无报错

## 2026-08-01 — [后端] v1.36.0 — 新增安全防护与攻击记录功能

### 背景
公网服务器持续遭受扫描攻击（如 `GET /?unix:AAAA...` 超长参数探测、`GET /testSchema/sc` 路径扫描），需要在应用层增加防护，并将攻击记录入库供分析研究。

### 新增（后端）
- **安全防护中间件 `middleware/security.js`**：在路由之前拦截并检测可疑攻击请求
  - 检测维度：超长 URL（>2048 → 414）、SQL 注入（403）、XSS（403）、路径遍历（403）、敏感文件探测（404 伪装）、已知扫描器路径（404 伪装）、恶意 User-Agent（404 伪装）、超长重复字符载荷（404 伪装）
  - 阻断策略：高危攻击直接阻断，探测类返回与正常 404 一致的响应，不暴露服务器指纹
  - 防刷屏：同一 IP 同一攻击类型 10 秒内控制台只输出一次日志，避免扫描流量淹没日志
  - 支持 `X-Forwarded-For` 获取真实客户端 IP（Nginx 反代场景）
- **数据库表 `SecurityEvent`（安全事件表）**：完整记录攻击信息
  - 字段：IP、方法、完整路径（Text）、完整 query（Text）、User-Agent、攻击类型、严重程度、触发原因、是否阻断、是否已处理、触发次数、首次触发时间
  - 迁移文件：`prisma/migrations/20260801_add_security_event/`
  - 去重合并：同 IP 同类型 5 分钟窗口内重复攻击合并为一条记录（count 累加），防止扫描器刷爆数据库
- **数据访问层 `utils/repo/security_repo.js`**：封装 `recordAttack`（去重写入）、`listEvents`（分页查询）、`countUnhandled`（未处理数统计）、`markHandled`（标记已处理）
- **管理员安全 API（`API/admin.js` 新增 3 个端点，沿用双重鉴权，不写 @openapi）**：
  - `GET /admin/:suffix/api/security/events` — 分页查询攻击事件（支持按类型/严重度/状态/IP 筛选）
  - `GET /admin/:suffix/api/security/unread-count` — 未处理攻击数（顶栏徽标提醒）
  - `PUT /admin/:suffix/api/security/events/:id/handle` — 标记攻击事件为已处理

### 修改（后端）
- **`app.js`**：在 cors 之后、路由之前挂载安全防护中间件
- **`admin/index.html`**：管理后台新增「🛡️ 安全防护」Tab（攻击事件表格 + 筛选 + 分页 + 标记已处理），顶栏新增未处理攻击数红色徽标（每 30 秒轮询，点击跳转安全 Tab）
- **`node-jinmao/package.json`**：MINOR bump → `1.36.0`

### 决策
- 不封禁 IP：仅对单次攻击请求阻断 + 记录，避免误伤正常用户（用户确认）
- 记录完整信息：path/query 用 Text 存完整原始值，供未来分析研究（用户确认）

## 2026-07-31 — [后端] v1.34.0 + [前端] v0.26.0 — 文生图计费修复 + LLM 重试机制 + 失败原因记录

### 修复（后端）
- **Bug 1：文生图模型未被正确计费**
  - **根因**：`create_image.js` 仅在图片生成成功时调用 `recordExternalCost`，失败时（任务失败/违规/超时/网络错误）未记录账单
  - **修复**：重写 `create_image.js`，将所有 API 调用封装在 `attemptImageGeneration()` 内，并在 `createImage()` 外层添加重试循环
    - 成功：记录 `status: "success"`, `imageCount: 1`, 正确计费
    - 失败：记录 `status: "failed"`, `imageCount: 0`, `errorMessage` 记录失败原因（不计费但记录调用事实）
- **Bug 2：LLM 调用失败后应自动重试并记录失败原因**
  - **根因**：`llm_client.js` 遇到 503/429/网络错误时直接返回失败，并发调用多个段落时高频触发 DeepSeek 限流
  - **修复**：`llm_client.js` `chat()` 函数新增最多 3 次自动重试（带指数退避 + 随机抖动）
    - 可重试错误：503（服务繁忙/限流）、429（请求过多）、网络超时/重置、500 内部错误
    - 不可重试错误：400/401/402 等客户端错误
    - 退避策略：重试延迟 = 1s × 2^retry，上限 8s，±25% 随机抖动避免惊群效应
    - 失败时 `errorMessage` 记录错误详情 + `retryCount` 记录重试次数
  - `chatStream()` 同样更新失败计费，记录 `errorMessage`
- **Bug 2 扩展：文生图重试**
  - `create_image.js` 新增整体重试逻辑：整个文生图流程（提交任务 + 轮询结果）最多重试 3 次
  - 退避延迟：2s × 2^retry，上限 10s，±25% 随机抖动
  - 失败时会根据错误类型自动判断是否值得重试（网络错误/HTTP 5xx 重试、内容违规不重试）
- **数据库迁移**：
  - `billing_record` 表新增 `error_message` (TEXT NULL) 字段 — 记录失败原因
  - `billing_record` 表新增 `retry_count` (INT DEFAULT 0) 字段 — 记录重试次数
  - 迁移文件：`prisma/migrations/20260731_add_error_message_and_retry_count_to_billing/`
- **`billing.js`**：`recordTokenUsage()` 和 `recordExternalCost()` 新增 `errorMessage` 和 `retryCount` 参数
- **`API/billing.js`**：账单查询响应中新增 `errorMessage` 和 `retryCount` 字段
- **`node-jinmao/package.json`**：MINOR bump → `1.34.0`

### 修改（前端）
- **`pages/billing/index.vue`**：状态列新增 `el-tooltip`，失败记录鼠标悬停时显示失败原因
  - 失败且有重试记录时显示 "(重试N次)" 标签
  - 状态列宽度从 100px 调整为 120px
- **`WEB/package.json`**：MINOR bump → `0.26.0`

## 2026-07-31 — [后端] v1.33.1 — 修复学习页幻灯片图片 ERR_SSL_PROTOCOL_ERROR + CSP 内联事件处理器违规

### 修复（后端）

**Bug 1：幻灯片图片 ERR_SSL_PROTOCOL_ERROR**
- **根因**：AI 生成的幻灯片 HTML 中图片 URL 为绝对路径（如 `https://jinmao.ckstu.top:30080/api/v1/files/...`），但 30080 端口仅支持 HTTP，浏览器用 HTTPS 直连该端口导致 SSL 协议错误
- **修复**：`API/files.js` — 文件代理路由中，对 HTML 文件在返回前自动将绝对 URL（`https?://domain:port/api/v1/files/...`）替换为相对路径（`/api/v1/files/...`），确保图片走同域 Nginx 代理
  - 新增 `normalizeFileUrls()` 函数和 `ABSOLUTE_FILES_URL_REGEX` 正则
  - HTML 文件不再走流式 pipe，改为先读取完整内容、规范化 URL、再发送（幻灯片 HTML < 500KB，内存开销可忽略）
  - HTML 文件 Cache-Control 设为 `no-cache`，确保客户端始终获取最新版本

**Bug 2：CSP inline event handler 违规**
- **根因**：Helmet v8 默认 CSP 将 `script-src-attr` 设为 `'none'`，阻止了幻灯片 HTML 中的内联事件处理器（`onclick="..."` 等），导致幻灯片交互功能失效
- **修复**：`app.js` — Helmet CSP 配置中新增 `scriptSrcAttr: ["'unsafe-inline'", "'unsafe-hashes'"]`，允许内联事件处理器和哈希验证的内联脚本

- **`node-jinmao/package.json`**：PATCH bump → `1.33.1`

## 2026-07-31 — [前端] v0.25.1 — "生成下一章"按钮交互优化：始终可见 + 禁用态文本提示

### 修改（前端）
- **`index.vue`**：按钮从 `v-if="canGenerateNext"` 改为始终渲染，用 `:disabled` + 文本切换控制交互
- **`script.js`**：
  - 新增 `generateBtnDisabled` computed：加载中/生成中/不可生成时禁用
  - 新增 `generateBtnText` computed：四态文本切换（加载中.../正在生成中.../生成下一章/已经是最后一章了）
  - `canGenerateNext` computed 拆分为 `generateBtnDisabled` + `generateBtnText`
- **`WEB/package.json`**：PATCH bump → `0.25.1`

## 2026-07-31 — [前端] v0.25.0 — 新增手机端学习首页入口 `/mobile`

### 新增（前端）
- **手机端首页页面** (`src/pages/mobile-home/`)：品牌蓝科技风手机端首页
  - 顶部品牌欢迎区 + 头像按钮（进入手机端个人中心）
  - 搜索框（本地过滤课程，不触发后端请求）
  - 4 宫格快捷入口：我的教材（定位滚动）、习题训练、题库市场、我的余额
  - "我的课程"区域：继续学习列表（纵向卡片），展示封面、章节名、页码进度、进度条
  - 数据来源：`getProfile()` + `listBooks()` + `getAllProgress()` + `getBilling()`
  - 空状态引导前往桌面版上传教材
- **手机端个人中心页面** (`src/pages/mobile-profile/`)：用户卡片 + 个人资料/我的余额/深色模式/退出登录
- **快捷入口宫格组件** (`src/components/mobile/MobileQuickActionGrid.vue`)：4 个固定入口按钮
- **课程列表卡片组件** (`src/components/mobile/MobileCourseListCard.vue`)：封面占位 + 进度条 + 操作按钮
- **`App.vue` 路径路由拓展**：根据 `window.location.pathname` 识别 `/mobile` 和 `/mobile/profile`，支持 popstate 浏览器返回键

### 修改（前端）
- **`WEB/package.json`**：MINOR bump → `0.25.0`（新增手机端首页功能）

## 2026-07-31 — [后端] v1.33.0 / [前端] v0.24.1 — 新增 maxline 字段 + "生成下一章"按钮稳定性改造

### 新增（后端）
- **Course 表新增 maxline 字段**：持久化 MD 文件总行数，用于权威判断教材是否已全部生成完毕
- **`book_repo.js` 新增 `updateMaxline()` 函数**：归一化/流水线中更新 maxline

### 修复（核心）
- **`can_generate_next.js` 重写**：新增三级防御体系（numeric > flag）
  - Level 1：maxline 数值权威校验 — `endline >= maxline` 明确已完成，`endline < maxline` 有剩余内容
  - Level 2：`isLastChapter` 标记 — 仅在 maxline=0（旧课程）时作为判断依据
  - Level 3：`pipelineStatus === "completed"` — 仅在 maxline=0 时作为判断依据
  - 关键修正：即使 `isLastChapter=true` 或 `pipelineStatus=completed`，只要 `endline < maxline` 就允许继续生成
- **修复"生成下一章"按钮无故消失的 bug**：根因是 `isLastChapter` 单向标记被误设后无独立验证手段

### 修改（后端）
- **`POSTbook.js`**：归一化完成后从本地 MD 文件计算总行数并写入 maxline（新课程首次写入）
- **`course_pipeline.js`**（3 处）：
  - Phase 1：MD 下载后补填 maxline（旧课程兼容）
  - Step 2：MD 下载后补填 maxline（旧课程兼容）
  - Step 8 兜底检测：从临时 `totalLineCount` 改用持久化 `course.maxline`
- **`detail.js`**：API 响应新增 `maxline` 字段 + **懒加载补填函数 `lazyFillMaxline()`**：
  - 当课程 maxline=0 时，从 MinIO 下载 MD 文件异步计算行数并写入数据库（fire-and-forget，不阻塞响应）
  - 解决已 completed 的旧课程无法通过流水线补填 maxline 的问题

### 修改（前端）
- **`script.js`**：`courseInfo` 新增 `maxline` 字段接收
- **`WEB/package.json`**：PATCH bump → `0.24.1`

### 数据库迁移
- `20260731120000_add_maxline_to_course`：`ALTER TABLE Course ADD COLUMN maxline INT UNSIGNED NOT NULL DEFAULT 0 AFTER endline`

## 2026-07-31 — [后端] v1.32.0 / [前端] v0.24.0 — 兑换码系统 + 管理员 CMS

### 新增（后端）
- **兑换码数据库表**：新增 `redeem_code` 表（Prisma model），包含 code/amount/isUsed/usedBy/usedAt/createdBy/createdAt 字段，code 唯一索引
- **管理员鉴权中间件**：`middleware/admin.js`，实现双层防护
  - 第一层：URL安全后缀校验（从 `config/admin_config.json` 读取，不匹配返回404伪装）
  - 第二层：JWT Token + 管理员角色验证（role === 'admin'）
- **管理员 API 路由**：`API/admin.js`，所有端点需双重鉴权
  - `POST /admin/:suffix/api/codes/generate` — 生成兑换码（默认10个，最大50个）
  - `GET /admin/:suffix/api/codes` — 分页查询兑换码列表（支持状态过滤）
  - `GET /admin/:suffix/api/config` — 获取系统配置（后缀脱敏显示）
  - `PUT /admin/:suffix/api/config` — 修改安全后缀
- **兑换码兑换 API**：`API/redeem.js`，替换原有 `recharge.js` 的空壳
  - `POST /api/v1/redeem` — 用户输入兑换码兑换余额（需JWT登录）
  - 使用 Prisma 事务保证原子性（查询→标记→增余额）
  - 频率限制：每用户每小时最多3次
  - 充值后自动解锁
- **管理员配置文件**：`config/admin_config.json`，存储安全后缀（默认 `Ghost_axe`）
- **管理员 CMS 页面**：`admin/index.html`，独立 SPA 页面
  - Vue 3 + Element Plus CDN 构建
  - 邮箱验证码登录（复用现有登录API）
  - 选项卡布局：兑换码管理 / 用户管理(占位) / 账单管理(占位) / 消费统计(占位) / 系统设置
  - 兑换码管理：一键生成10个兑换码、列表分页查询、一键复制
  - 系统设置：查看/修改安全后缀
  - 暗黑模式支持

### 修改（后端）
- **app.js**：
  - 新增管理员页面路由 `GET /admin/:suffix`（后缀校验后渲染admin/index.html）
  - 新增管理员API路由挂载
  - 新增加兑换码兑换路由挂载（替换原有充值路由）
  - 启动横幅新增安全后缀和入口地址显示
  - 移除原有的 `rechargeRouter` 引用
- **.gitignore**：新增 `config/admin_config.json`（安全后缀配置不提交）
- **prisma/schema.prisma**：新增 `redeem_code` model

### 新增（前端）
- **兑换码领取页面**：`pages/redeem/index.vue`
  - 居中卡片布局，兑换码输入框 + 提交按钮
  - 输入自动转大写、格式校验
  - 成功提示（到账金额、最新余额、解锁状态）
  - 返回首页按钮
- **兑换码 API 封装**：`api/redeem.js`
  - `redeemCode(code)` — 兑换码兑换请求

### 修改（前端）
- **HomeSidebar.vue**：
  - 新增新用户福利弹窗（余额上方，渐变色+弹跳动画）
  - 显示条件：余额 ≤5元且24小时内未关闭
  - emit `navigate-redeem` 事件
- **home/index.vue**：新增 `@navigate-redeem` 事件监听
- **home/script.js**：新增 `navigateToRedeem()` 方法
- **App.vue**：新增 RedeemPage 组件导入和 `'redeem'` 路由
- **api/billing.js**：移除旧的 `recharge()` 函数

### 删除
- **API/recharge.js**：原有充值空壳（被 `API/redeem.js` 替换），虽保留文件但路由已移除

---

## 2026-07-31 — [后端] v1.31.0 / [前端] v0.23.0 — 实现余额扣费与截断机制

### 新增（后端）
- **余额管理模块**：新增 `utils/balance.js`，统一管理余额检查、扣费、锁定/解锁
  - `checkCanUseAI(userId)`：前置校验，检查用户是否被锁定（balanceLocked=true），锁定用户拒绝 AI 操作
  - `lockUserIfNegative(userId)`：后置检查，操作完成后若余额 < 0 自动锁定用户
  - `deductBalance(userId, amount)`：原子扣减余额（Prisma decrement），由 billing.js 自动调用
  - `unlockUserOnRecharge(userId, newBalance)`：充值后若余额 > 0 自动清除锁定
- **充值 API**：新增 `POST /api/v1/recharge` 端点 (`API/recharge.js`)
  - 接收充值金额（1-10000 元），使用 Prisma increment 原子更新余额
  - 充值后自动调用 `unlockUserOnRecharge` 解锁用户
  - JWT 鉴权，完整 `@openapi` JSDoc 注释
- **user_repo.js 余额操作**：新增 `deductBalance`、`setBalanceLocked`、`getBalanceState` 三个函数

### 数据库变更
- **User 表**：新增 `balance_locked` 字段（TINYINT(1), DEFAULT 0），用于标记余额锁定状态
- 迁移文件：`prisma/migrations/20260731_add_balance_locked/migration.sql`

### 修改（后端）
- **billing.js**：`recordTokenUsage()` 和 `recordExternalCost()` 写入账单记录后，自动调用 `deductBalance()` 扣减用户余额
- **generate-next-chapter.js**：在 `computeCanGenerateNext` 检查之后新增余额校验，锁定用户返回 402
- **course_pipeline.js**：`generateChapter()` 完成后调用 `lockUserIfNegative()` 后置锁定（成功和失败两个路径均覆盖）
- **md2json.js / pdf2quiz.js / format-text.js**：在创建任务/AI 调用前新增余额前置校验，锁定用户返回 402
- **format-text.js**：AI 调用完成后新增后置余额锁定检查
- **task-runner.js**：任务完成后（成功和失败两个路径）新增后置余额锁定检查
- **billing.js（账单 API）**：响应中新增 `balanceLocked` 字段
- **app.js**：注册充值路由 `/api/v1/recharge`

### 新增/修改（前端）
- **billing API**：新增 `recharge(amount)` 函数 (`WEB/src/api/billing.js`)
- **账单页面**：启用"去充值"按钮，点击弹出金额输入框完成充值；余额卡片新增"已锁定"红色标记
- **HomeSidebar**：余额显示区域新增锁定状态标识（余额为负时变红色 + ⚠ 前缀）

### 核心设计
- **前置校验 + 后置锁定**：操作前检查 `balanceLocked` 状态，操作完成后若余额 < 0 自动锁定
- **锁定即阻断**：`balanceLocked=true` 时拒绝所有 AI 消费操作（章节生成、题库导入、文本格式化）
- **充值即解锁**：充值后余额 > 0 自动清除锁定状态
- **余额为 0 不锁定**：仅当余额变为负数时才锁定，允许用户"用完最后一分钱"
- 版本号：后端 `v1.30.2` → `v1.31.0`，前端 `v0.22.2` → `v0.23.0`

## 2026-07-31 — [后端] v1.30.2 / [前端] v0.22.2 — 修复 completed 章节每次进入都触发补全检测的 bug

### 修复（后端）
- **API_course_slides**：`slides.js` 完整性自动检测逻辑从 `completed || partial_completed` 改为仅对 `partial_completed` 触发
  - `completed` 章节表示文件已完整，不应再触发完整性检测
  - 避免 completed → partial_completed → completed 的不必要状态翻转
  - 解决每次打开已完成课程的学习页面时都显示"正在补全"横幅的问题

### 修复（前端）
- **学习页面 script.js**：`loadChapterSlides` 中新增 PPT URL 变化检测
  - 如果补全完成后重新加载同一章节且 PPT URL 未变，不再重置 `pptLoading = true`
  - 避免 iframe 不重新渲染导致 `onPptLoad` 无法触发、PPT 一直显示 loading 的问题
- 版本号：后端 `v1.30.1` → `v1.30.2`，前端 `v0.22.1` → `v0.22.2`

## 2026-07-30 — [后端] v1.30.1 / [前端] v0.22.1 — 新增文本粘贴导入题库页面

### 新增（后端）
- **AI 文本格式化 API**：新增 `POST /api/v1/quiz/format-text` 端点，接收原始题目+答案混合文本，调用 DeepSeek v4-pro 将其格式化为结构化题目 JSON
  - `API/quiz/format-text.js`：Express 路由模块，含完整 `@openapi` JSDoc 注释
    - 文本长度限制：单次最大 100000 字符
    - 参数校验：text（必填）、textbookName（必填）
    - AI 调用：使用 `llmClient.chat()` 大模型，`response_format: json_object`，callTag: `quiz_format_text`
    - JSON 解析容错：自动清洗 ```json 代码块包裹，支持 `{questions: []}` 和直接数组两种格式
  - `config/quiz-format-text-prompt.md`：AI 格式化专用 Prompt 配置
    - 定义 6 条核心规则（不编造、不猜测答案、选项格式统一、题型自动识别、答案格式规范、必须返回有效 JSON）
    - 包含 6 种常见文本格式识别模式示例
    - 注意事项：缺选项自动补前缀、非题目内容忽略、中文标点识别
- 版本号：后端 `v1.30.0` → `v1.30.1`

### 新增（前端）
- **文本粘贴导入题库页面**：`WEB/src/pages/quiz-import/`
  - `index.vue`：双栏布局（左 40% + 右 60%），顶部工具栏含返回按钮、题库名称输入、试卷名称输入、AI 格式化按钮、导入按钮
    - 左侧：大文本域粘贴原始题目文本，实时字符计数，超长文本警告
    - 右侧：可编辑题目卡片列表，每张卡片含题型下拉、题干、选项（ABCD 四栏）、答案区（按题型动态切换控件）、解析
    - 答案区控件按题型动态切换：单选用 `el-select`、多选用 `el-select multiple`、判断用 `el-radio-group`、填空/简答用 `el-input`
    - 题目可删除，题型切换时自动重置答案字段以匹配新题型
    - 统计信息栏：题目总数 + 各题型数量分类标签
    - 空状态提示 + 加载动画
  - `script.js`：业务逻辑
    - AI 格式化：调用 `formatQuizText()` → 存入 `questions` ref 数组
    - 导入题库：调用 `importQuiz()` → 成功后自动返回首页
    - `handleTypeChange()`：题型切换时自动转换答案格式
  - 遵循设计规范：纯黑纯白文字、10px 圆角、500ms 过渡、暗黑双轨适配、防重复点击
- **首页入口**：题库区域新增"文本导入"按钮（位于"导入题库"左侧），点击导航到 `quiz-import` 页面
- **前端 API**：`api/quiz.js` 新增 `formatQuizText()` 函数
- **路由注册**：`App.vue` 新增 `quiz-import` 页面路由（`changePage` 处理 + 组件导入 + 条件渲染）
- 版本号：前端 `v0.22.0` → `v0.22.1`

## 2026-07-30 — [后端] v1.30.0 / [前端] v0.22.0 — 新增删除单个试卷功能

### 新增（后端）
- **删除试卷 API**：新增 `DELETE /api/v1/quiz/exams/:examId` 端点，支持删除单个试卷及其关联的所有数据（题目、作答记录、错题、刷题会话、报告）
  - `repo/quiz_repo.js`：新增 `deleteExam(examId, userId)` 函数
    - 事务级联删除试卷关联的所有数据（reportItems → reports → answers → wrongQuestions → sessions → questions → exam）
    - 删除后统计剩余试卷数：若剩余 0 则连题库一并删除，若剩余 > 0 则仅更新题库的 totalExams 和 totalQuestions
  - `API/quiz/detail.js`：新增 `router.delete("/exams/:examId", ...)` 路由，含完整 `@openapi` JSDoc 注释
  - 权限控制：仅题库所有者可删除试卷（借用者无权限）
- 版本号：后端 `v1.29.3` → `v1.30.0`

### 新增（前端）
- **试卷删除按钮**：题库详情页试卷行新增"删除"红色按钮（仅 `ownType === 'own'` 时可见）
  - `api/quiz.js`：新增 `deleteExam(examId)` API 封装
  - `pages/quiz-detail/script.js`：新增 `deletingExamId` 防重复状态 + `handleDeleteExam(exam)` 函数（确认弹窗 → 删除 → 刷新/跳转）
  - 若题库被一并删除则自动跳回首页，否则原地刷新列表和统计
- 版本号：前端 `v0.21.1` → `v0.22.0`

## 2026-07-30 — [后端] v1.29.3 — 合并 md2quiz 的独立 DeepSeek 客户端到 llm_client.js

### 重构（后端）
- **消除重复代码**：删除 `service/md2quiz/deepseek-client.js`（333 行），其原生 fetch + 手动 SSE 解析完全被 OpenAI SDK 的流式接口替代
- **`utils/llm_client.js`**：新增 `chatStream()` 方法，使用 `openai.chat.completions.create({ stream: true })` + `for await` 实现流式调用，支持 per-delta 回调和自动计费，附带心跳/看门狗机制
- **`service/md2quiz/quiz-splitter.js`**：`requestDeepseekJsonCompletionStream()` → `chatStream(userId, "md2quiz_split", { modelSize: "big", messages, response_format })`
- **`service/md2quiz/quiz-chunk-processor.js`**：`requestDeepseekJsonCompletionStream()` → `chatStream(userId, "md2quiz_format", { modelSize: "big", messages, response_format })`
- **收益**：所有 DeepSeek 调用统一入口、计费逻辑不再双份维护、自动获得 SDK 断线重连等基础设施，净减 243 行代码
- 版本号：后端 `v1.29.2` → `v1.29.3`

## 2026-07-30 — [后端] v1.29.2 — 修复 md2quiz AI 返回嵌套答案格式导致全部答案无法匹配

### 修复（后端）
- **quiz-chunk-processor.js**：AI 返回的"仅答案"块中，答案可能按题型分组为嵌套格式（如 `{"single": {"1":"A"}, "judge": {"1":"正确"}}`），旧代码直接存入 answerMap 导致值变为 `[object Object]`，合并器无法解析致使所有题目"未找到对应答案"
  - 新增 `TYPE_TO_PREFIX` 映射表（single→单、multiple→多、judge→判 等），检测嵌套对象后自动扁平化为带中文前缀的键（如 `"单1":"A"`），与 `resolvePrefixedKey` 的 `PREFIX_TO_TYPE` 互逆
  - 同时保留扁平格式兼容（`{"1":"A"}` 直接存入）
- **task-runner.js**：`resolvePrefixedKey` 的 `PREFIX_TO_TYPE` 新增英文类型名前缀（single/multiple/judge/fill/short_answer），作为防御层——即使 AI 直接返回 `"single5":"A"` 格式也能正确解析
- 版本号：后端 `v1.29.1` → `v1.29.2`

## 2026-07-30 — [后端] v1.29.1 — 修复 md2quiz 题库生成错误使用 flash 模型

### 修复（后端）
- **md2quiz DeepSeek 模型切换**：`service/md2quiz/deepseek-client.js` 将模型从 `DEEPSEEK_API_SMALL`（`deepseek-v4-flash`）切换为 `DEEPSEEK_API_BIG`（`deepseek-v4-pro`），题库转换涉及题型识别、答案提取等需要深度推理的任务，不应使用轻量级 flash 模型
- 版本号：后端 `v1.29.0` → `v1.29.1`

## 2026-07-30 — [后端] v1.29.0 — 新增 md2quiz 题库生成流水线的 LLM Token 计费

### 新增（后端）
- **md2quiz DeepSeek 调用计费**：`service/md2quiz/deepseek-client.js` 绕过了统一的 `llm_client.js`（因需 per-delta 流式回调），之前所有题库生成、分段、格式化环节的 DeepSeek API 调用都未触发计费
- `service/md2quiz/deepseek-client.js`：
  - 引入 `recordTokenUsage`，在 `requestDeepseekJsonCompletionStream` 中通过 `options.userId` / `options.callTag` 支持自动计费
  - 流式调用成功后根据 `latestUsage`（prompt_tokens / prompt_cache_hit_tokens / completion_tokens）自动写入账单
- 调用链传递 userId：
  - `task-service.js` → `runMarkdownJsonTask(taskId, ..., userId)` — 新增 userId 参数
  - `task-runner.js` → `splitQuizIntoChunks(md, userId)` / `processAllChunks(chunks, userId)` / `generateQuestionsForChunk({..., userId})` — 逐层传递
  - `quiz-splitter.js` → `requestDeepseekJsonCompletionStream(..., { userId, callTag: "md2quiz_split" })`
  - `quiz-chunk-processor.js` → `requestDeepseekJsonCompletionStream(..., { userId, callTag: "md2quiz_format" })`
- `utils/billing.js` — CALL_TAG_LABELS 新增：md2quiz_split（题库分段）、md2quiz_format（题库格式化）、md2quiz_generate（题库生成）
- 版本号：后端 `v1.28.5` → `v1.29.0`（次版本号 +1，新增功能）

## 2026-07-30 — [后端] v1.28.5 — 重构 doc2x 计费：改为从 PDF 本地读取页数（API 不返回页数）

### 修复（后端）
- **doc2x PDF 计费方案重构**：Doc2x API v2 根本不会返回解析成功的页数，之前依赖 `statusData.result.pages` 的计费逻辑完全不生效，导致所有 doc2x 计费为 ¥0
- `utils/doc2x.js`：
  - `pollParseStatus()` 精简：移除所有从 API 返回中提取页数的逻辑（包括调试日志），API 不返回页数，直接 `return { success: true }`
  - `getPdfPageCountLocally()` 升级为主方案：从 PDF 文件二进制中匹配 `/Type /Page` 标记统计页数，注释更新为"主要方案"
  - `main()` 重构计费时序：文件校验通过后立即调用 `getPdfPageCountLocally(absolutePath)` 读取页数，不等待任何 API 返回，确保计费一定使用本地读取的真实页数
- 版本号：后端 `v1.28.4` → `v1.28.5`

## 2026-07-30 — [后端] v1.28.4 — 修复 doc2x PDF 页数统计异常导致计费为 0

### 修复（后端）
- **doc2x PDF 解析计费为 0 问题**：Doc2x API 在某些 PDF 上返回的 `result.pages` 为空数组，导致 `pollParseStatus()` 获取的 `pageCount` 为 0，进而计费为 ¥0.000000
- `utils/doc2x.js`：
  - `pollParseStatus()` 增强调试日志：当 `pageCount` 为 0 时，输出 `statusData.result` 完整结构（排除过大的 md 内容）和顶层键，方便诊断 API 返回的实际结构
  - 新增 `getPdfPageCountLocally()` 函数：从 PDF 文件二进制内容中用正则 `/Type\s*\/Page[^s]` 统计页数（排除 `/Type /Pages` 父节点），无需额外依赖
  - `main()` 新增备用方案：当 API 返回的 `pageCount` 为 0 时，自动调用 `getPdfPageCountLocally()` 从 PDF 文件本地读取页数
- 测试：本地 PDF 页数读取测试（1/3/5/10/50/100 页）全部通过，正则边界测试（8 种场景）全部通过，共 14 项断言
- 版本号：后端 `v1.28.3` → `v1.28.4`

## 2026-07-30 — [前端] v0.21.1 — 个人设置页视觉增强：添加表单图标和标签徽章

### 优化（前端）
- **个人设置页视觉增强**：表单标签左侧新增 SVG 图标（邮箱/用户名/昵称/签名/验证码），"不可修改"标签改为圆角徽章样式
- `WEB/src/pages/profile/index.vue` — 表单行结构优化：
  - 每个标签区域新增 `w-28` 容器，内含 20px SVG 图标 + 标签文字
  - 图标使用 `var(--color-text-secondary)` 着色，支持亮暗双模
  - "不可修改"文字改为 `rounded-[4px]` 徽章样式，背景色 `var(--color-card-hover)`
  - 邮箱值新增 `truncate` 类防止超长溢出
- 版本号：前端 `v0.21.0` → `v0.21.1`

## 2026-07-30 — [后端] v1.28.3 — 修复 md2quiz 题型前缀答案键匹配失败导致 179 条警告

### 修复（后端）
- **mergeResults 答案匹配失败**：AI 处理答案块时，因不同类型的题有重复编号（都从 1 开始），使用了题型前缀键（"单1""多1""判1""简1"），但题目使用纯数字 ID（1、2、3...），导致 `String(id)` 在 answerMap 中查找不到、题目和答案互相找不到对方，产生 179 条警告
- `service/md2quiz/task-runner.js` — `mergeResults()` 函数重构：
  - 新增 `questionsByType` 预处理：按 type 分组并排序，建立 `(type, localIndex) → questionObj` 映射表
  - 新增 `resolvePrefixedKey()` 函数：将题型前缀键（"单5"）解析为匹配题目的纯数字 ID
  - 新增 `PREFIX_TO_TYPE` 映射表：单→single、多→multiple、判→judge、简→short_answer、填→fill
  - 新增预处理环节：遍历 answerMap，用 `resolvePrefixedKey()` 将所有前缀键解析为纯数字 ID 存入 `resolvedAnswers`
  - 原直接 `answerMap.get(String(id))` 查找逻辑不变作兜底，优先使用 `resolvedAnswers`
  - 修复"未使用"计数日志：避免前缀键双重标记导致的负数显示
  - 导出 `mergeResults` 和 `normalizeAnswerFromRaw` 供测试用
- `config/quiz-format-prompt.md` — 优化答案格式 prompt：
  - 明确要求 key 必须是纯数字字符串
  - 严禁使用题型前缀（"单1""多1""判1""简1"为错误格式）
  - 说明系统会按题型自动匹配，即使不同题型有重复编号也适用
- 测试：6 组场景覆盖纯数字键匹配、题型前缀键匹配、混合键格式、无答案边界情况、答案归一化、多选题前缀匹配，39 项断言全部通过
- 版本号：后端 `v1.28.2` → `v1.28.3`

## 2026-07-30 — [后端] v1.28.2 — 修复 billing_record 表缺失 page_count / page_unit_price 列

### 修复（后端）
- **billing_record 表缺列导致 doc2x 计费写入失败**：Prisma schema 中已定义但数据库中缺少 `page_count`（INT）和 `page_unit_price`（DECIMAL(18,7)）两列，导致 `saveBillingRecord()` 写入时报错 "The column page_count does not exist in the current database"
- 原因：billing_record 表初始创建时未包含 doc2x 相关列，后续只在 schema 中添加了定义但未生成迁移文件
- 新增迁移文件 `prisma/migrations/20260730_add_billing_page_count/migration.sql`：ALTER TABLE 添加 `page_count` 和 `page_unit_price` 列
- 部署步骤：将迁移文件同步到生产服务器后，在 `node-jinmao/` 目录下执行 `npx prisma migrate deploy`
- 版本号：后端 `v1.28.1` → `v1.28.2`

## 2026-07-30 — [后端] v1.28.1 — 修复 PDF 转题库时 doc2x 计费未执行的问题

### 修复（后端）
- **PDF 转题库不计费**：`API/quiz/pdf2quiz.js` 第 168 行调用 `convertPdfToMd(filePath)` 时缺少第二个参数 `userId`，导致 `pdf-to-md.js` 中 `if (userId)` 判断为 false，`recordExternalCost()` 计费永远不会执行。修复：传入 `req.userId`
- **页数为 0 调试日志**：`utils/doc2x.js` 的 `pollParseStatus()` 中新增调试日志——当页数为 0 时打印 `result` 对象的键和 `pages` 的类型信息，帮助排查 API 返回的 `pages` 为空的原因
- 版本号：后端 `v1.28.0` → `v1.28.1`

## 2026-07-30 — [前端] v0.20.2 — 修复题库市场页面 _withKeys 崩溃（MarketScript 未调用）

### 修复（前端）
- **题库市场页面崩溃**：`market/script.js` 导出的是一个工厂函数 `useMarketScript()`，但 `market/index.vue` 中将其当作对象直接解构 `const { ... } = MarketScript`，导致所有变量均为 `undefined`，Vue 处理 `@keyup.enter` 时访问 `undefined._withKeys` 崩溃
- `market/index.vue`：`import MarketScript` → `import useMarketScript`，并调用 `useMarketScript()` 获取实例
- `market/script.js`：补充 `Search` 图标的导入和导出（此前 `:prefix-icon="Search"` 使用未定义变量）
- 版本号：前端 `v0.20.1` → `v0.20.2`

## 2026-07-30 — [后端] v1.27.1 — 修复题库查询 500 错误（is_shared 列缺失 & QuizBookBorrow 表未迁移）

### 修复（后端）
- **数据库迁移缺失**：`QuizTextbook.is_shared` 列和 `QuizBookBorrow` 表在 Prisma schema 中已定义但未通过迁移同步到数据库，导致题库列表查询和题库市场功能全部不可用
- 新增迁移文件 `prisma/migrations/20260730_add_quiz_is_shared_and_borrow/migration.sql`：
  - `ALTER TABLE QuizTextbook ADD COLUMN is_shared BOOLEAN NOT NULL DEFAULT false`
  - `CREATE TABLE IF NOT EXISTS QuizBookBorrow`（id/user_id/textbook_id/create_time，联合唯一索引）
- 部署步骤：将迁移文件同步到生产服务器后，执行 `npx prisma migrate deploy`

## 2026-07-30 — [后端] v1.27.0 / [前端] v0.20.0 — 新增题库详情页

### 新增（前端）
- **题库详情页**：`WEB/src/pages/quiz-detail/`（新建目录），用户从首页题库卡片点击进入
  - `index.vue`：页面模板（顶栏导航 + 题库信息 + 左右统计卡片 + 操作按钮 + 试卷列表）
  - `script.js`：页面逻辑（数据加载、SVG 环形正确率图、el-progress 进度条、试卷维度刷题入口）
  - 统计区域：SVG 环形进度条展示正确率 + Element Plus 进度条展示已做题数/总题数
  - 试卷列表：每行显示试卷名称和题数，右侧提供"顺序刷题"和"随机刷题"两个按钮
  - "添加试卷"按钮占位（提示即将上线），"错题本入口"按钮跳转复习会话
  - 所有按钮 `:loading` 防重复点击，含暗黑双轨 `rounded-[10px]` `duration-500` 样式
- **路由**：`App.vue` 新增 `quiz-detail` 路由，通过 `provide/inject` 传递 `quizDetailParams`
- **API 封装**：`api/quiz.js` 新增 `getTextbookStats`、`startExamSequentialSession`、`startExamRandomSession`
- **HomePage 修复**：`home/index.vue` + `home/script.js` 补充 `QuizTextbookCard` 的 `@open` 事件监听（此前遗漏）

### 新增（后端）
- **API/quiz/detail.js**（新文件）：3 个端点
  - `GET /quiz/textbooks/:id/stats`：题库统计（totalQuestions/doneCount/correctCount/accuracy/wrongCount），通过 QuizUserAnswer 联表 + QuizWrongQuestion 聚合计算
  - `POST /quiz/exams/:examId/sequential-session`：基于试卷的顺序刷题（按 sortOrder 升序）
  - `POST /quiz/exams/:examId/random-session`：基于试卷的随机刷题（每种题型随机抽5题）
  - 全部含完整 `@openapi` JSDoc 注释
- **service/quiz_service.js**：新增 `startExamSequentialSession`、`startExamRandomSession`（基于 examId 的刷题会话创建，启用数据库已预留的 QuizSession.examId 字段）
- **repo/quiz_repo.js**：新增 `getAllQuestionIdsByExamId`、`sampleQuestionIdsByExamId`、`findActiveExamSession`
- **app.js**：挂载 `quizDetailRouter`

## 2026-07-30 — [后端] v1.26.0 / [前端] v0.19.0 — 新增题库市场功能

### 新增（后端）
- **题库市场**：新增题库共享与借用功能，用户可将题库共享到市场，其他用户可浏览并借用
  - 数据库：`QuizTextbook` 表新增 `isShared` 字段（是否共享到题库市场），新建 `QuizBookBorrow` 题库借用表（记录用户的借用关系，userId + textbookId 联合唯一）
  - `repo/quiz_repo.js`：新增 `toggleShareStatus`（切换共享状态）、`listMarketTextbooks`（市场列表）、`borrowTextbook`（借用题库）、`unborrowTextbook`（取消借用）、`getUserBorrowedTextbookIds`（获取用户已借用的题库ID列表）
  - `repo/quiz_repo.js`：修改 `listTextbooks`（合并自建题库 + 借用的题库，标注 ownType/creatorNickname）、`getTextbookDetail`（权限放宽：所有者/借用者/共享题库均可查看）、`deleteTextbook`（级联删除借用记录）
  - `API/quiz/textbooks.js`：新增 `PUT /textbooks/:id/share` 共享切换端点
  - `API/quiz/market.js`（新文件）：4个市场端点（`GET /market` 列表、`GET /market/:id` 详情、`POST /market/:id/borrow` 借用、`DELETE /market/:id/borrow` 取消借用），含完整 `@openapi` JSDoc 注释
  - 独立性保证：QuizSession/QuizUserAnswer/QuizWrongQuestion/QuizReport 均有独立 userId，多用户共用一个题库时互不干扰

### 修改（前端）
- **侧边栏**：`components/HomeSidebar.vue` 新增加"题库市场"导航入口（地球图标），点击切换到市场页面
- **题库市场页面**（新文件）：`pages/market/index.vue`（卡片网格布局，展示题库名称/描述/题目数/创建者）、`pages/market/script.js`（列表加载、搜索、分页、借用/取消借用逻辑）
- **题库卡片**：`components/QuizTextbookCard.vue` 新增加共享切换（三点菜单中"共享到市场"/"取消共享"）、借用标签（"借用的"蓝色标签）、共享标签（"已共享"绿色标签）、创建者信息显示
- **首页**：`pages/home/index.vue` 渲染 `MarketPage` 组件、`pages/home/script.js` 处理 market 菜单切换、新增 `onShareToggled` 回调
- **API 封装**：`api/quiz.js` 新增 `listMarketTextbooks`/`getMarketTextbookDetail`/`borrowTextbook`/`unborrowTextbook`/`toggleShareTextbook` 函数
- 版本号：后端 `v1.25.0` → `v1.26.0`，前端 `v0.18.0` → `v0.19.0`

## 2026-07-30 — [后端] v1.25.0 / [前端] v0.18.0 — 新增顺序刷题功能 & 题库卡片删除按钮改为三点菜单

### 新增（后端）
- **顺序刷题功能**：新增顺序刷题完整 CRUD，与随机刷题并列
  - `API/quiz/session.js`：新增4个顺序刷题端点（`POST /sequential-sessions`、`GET /sequential-sessions/:sessionId`、`PUT /sequential-sessions/:sessionId/progress`、`POST /sequential-sessions/:sessionId/complete`），含完整 `@openapi` JSDoc 注释
  - `service/quiz_service.js`：新增 `startSequentialSession`（按题目 ID 升序取全部题目）和 `getAllQuestionIds`（获取题库所有题目 ID），`getSequentialSessionDetail` / `saveSequentialSessionProgress` / `completeSequentialSession` 复用随机模式相同逻辑
  - `repo/quiz_repo.js`：新增 `findActiveSequentialSession`（查找进行中的 SEQUENTIAL 模式会话）

### 修改（前端）
- **顺序刷题按钮**：`components/QuizTextbookCard.vue` 新增"顺序刷题"按钮（绿色 `type="success"`），原"开始刷题"改名"随机刷题"，新增 `startSequential` emit 事件
- **题库卡片删除按钮改为三点菜单**：`components/QuizTextbookCard.vue` 移除红色删除按钮，改为左上角三点菜单（参考 `CourseCard.vue` 样式），使用 `@vueuse/core` 的 `onClickOutside` 监听点击外部关闭
- **首页顺序刷题处理**：`pages/home/script.js` 新增 `onStartSequentialQuiz` 处理函数，调用 `startSequentialSession` API 并传入 `sessionMode: "sequential"`
- **刷题页双模式适配**：`pages/quiz/script.js` 根据 `quizParams.sessionMode` 自动选择随机/顺序刷题 API
- **API 封装**：`api/quiz.js` 新增 `startSequentialSession` / `getSequentialSessionDetail` / `saveSequentialSessionProgress` / `completeSequentialSession` 四个函数
- 版本号：前端 `v0.17.4` → `v0.18.0`

### 新增（后端）
- 版本号：后端 `v1.24.0` → `v1.25.0`

## 2026-07-30 — [后端] v1.24.0 — 新增 doc2x PDF 解析按页计费

### 新增（后端）
- **doc2x PDF 解析计费**：doc2x 调用按 0.02 元/页计费，实现与现有 LLM Token / 文生图 / TTS 计费体系统一
  - `utils/doc2x.js`：`pollParseStatus` 解析成功时从 `result.pages` 提取页数，`main` 函数返回值新增 `pageCount` 字段
  - `utils/billing.js`：`CALL_TAG_LABELS` 新增 `doc2x: "PDF解析"`；`recordExternalCost` 新增 doc2x 按页计费分支（`pageCount * per_page`）
  - `config/billing_pricing.json`：三个时段均新增 `doc2x.doc2x-api-v2` 定价，`per_page: 0.02` 元/页
  - `prisma/schema.prisma`：`billing_record` 表新增 `page_count`（Int?）和 `page_unit_price`（Decimal(18,7)）字段
  - `service/POSTbook.js`：PDF 分支调用 doc2x 成功后异步记录计费
  - `service/md2quiz/pdf-to-md.js`：`convertPdfToMd` 新增可选 `userId` 参数，调用 doc2x 成功后异步记录计费

## 2026-07-30 — [后端] v1.23.7 — 修复 AI 生成题目选项不显示的问题

### 问题
- AI 生成流水线（`md2quiz/task-runner.js`）直接存储字符串数组格式的 options（如 `["A. 螺杆螺纹", "B. 齿轮齿条"]`），但 `quiz_service.js` 的 `parseOptions` 只兼容对象数组格式（如 `[{key: "A", value: "..."}]`），导致字符串被过滤掉，前端选项区域为空。

### 修改（后端）
- **`node-jinmao/service/quiz_service.js`**：`parseOptions` 函数兼容两种入库格式：
  - 对象数组（传统导入）：`[{key, value}]`
  - 字符串数组（AI 生成）：`["A. 选项内容"]`，通过正则解析 `key` 和 `text`

## 2026-07-29 — [后端] v1.23.4 / [前端] v0.17.2 — 全面清理 Word 文件输入功能

### 重要决策
- **本版本起，项目拒绝任何 Word 文件输入**。移除所有 `.docx` / `.doc` 文件处理相关代码、依赖和配置。

### 删除
- **`node-jinmao/utils/word2pdf.js`**（481 行）：LibreOffice Word → PDF 转换核心模块
- **`node-jinmao/config/docx2json_prompt.md`**（73 行）：题库格式化模式专用 AI 提示词
- **`node-jinmao/libreoffice-portable/` 目录**：LibreOffice AppImage 便携版（已清理）
- **测试文件**：`test/` 目录下的 `.docx` 测试文件

### 修改（后端）
- **`node-jinmao/service/POSTbook.js`**：移除 `word2pdf` 引用、`ALLOWED_EXTENSIONS` 中的 `.docx`/`.doc`、Word 转换分支（约 40 行）
- **`node-jinmao/API/POSTbook.js`**：移除 multer 白名单中的 `.docx`/`.doc`，更新错误消息
- **`node-jinmao/service/md2quiz/task-runner.js`**：移除 format 模式全部代码（`loadFormatPromptTemplate`、`buildFormatPrompt`、`validateFormatResult`、`runFormatTask`，约 330 行）
- **`node-jinmao/API/quiz/pdf2quiz.js`**：移除 format 模式参数处理和分支逻辑
- **`node-jinmao/.gitignore`**：移除 `libreoffice-portable/` 忽略规则
- **`node-jinmao/setup.sh`**：移除 LibreOffice 依赖检查

### 修改（前端）
- **`WEB/src/components/UploadBookDialog.vue`**：移除 `.docx`/`.doc` 接受格式
- **`WEB/src/components/ImportQuizDialog.vue`**：移除 PDF Tab 中的 Word 接受格式、删除题库格式化 Tab 全部代码（模板 + script）

### 保留（不受影响）
- Doc2x API 集成（`utils/doc2x.js`、`config/doc2x_config.json`）— PDF → MD 仍需要
- `adm-zip` 依赖 — PDF 的 Doc2x 结果 zip 解压仍需要
- PDF 上传/转换全部功能

## 2026-07-29 — [前端] v0.17.4 — 题库格式化 Tab 改为 PDF 上传

### 修改
- **`WEB/src/components/ImportQuizDialog.vue`**：
  - 将「题库格式化」Tab 从 DOCX 改为接受 PDF 文件上传
  - 使用 `formatForm` / `formatFile` 响应该 Tab 的表单控件
  - 新增 `handleFormatSubmit` 提交函数，发送 `mode: "format"` 到后端（AI 解析已有题目而非生成新题）
  - 新增 footer 按钮「上传并解析」
  - `resetAll` 同步清理格式化 Tab 状态

## 2026-07-29 — [前端] v0.17.3 — 修复题库导入弹窗 DOCX Tab 报错（已废弃）

### 修复
- **`WEB/src/components/ImportQuizDialog.vue`**：
  - 移除未实现的 DOCX Tab（模板引用了未定义的 `docxForm` 等变量，导致运行时崩溃）

## 2026-07-29 — [后端] v1.23.5 — pdf-to-md 改用 7z 解压 zip

### 修复
- **`node-jinmao/service/md2quiz/pdf-to-md.js`**：
  - `extractMdFromZip()` 改为调用项目统一的 `utils/extract_zip.js`（7z 命令行解压），替代之前依赖的 `adm-zip` npm 包
  - 与 POSTbook.js 共享同一套 7z 解压方案，无需额外安装依赖
- **`node-jinmao/package.json`**：移除 `adm-zip` 依赖（已无代码引用）

## 2026-07-29 — [后端] v1.23.4 — 移除 Word 上传，仅接受 PDF

### 修改
- **`node-jinmao/API/quiz/pdf2quiz.js`**：
  - 移除 Word（.docx/.doc）文件上传支持，`fileFilter` 仅接受 PDF
  - 移除 `convertWordToMd` 导入和 Word 转换分支逻辑
  - 简化文件名生成（统一 `pdf-` 前缀）、日志和响应消息
- **`node-jinmao/service/md2quiz/pdf-to-md.js`**：
  - 移除 `convertWordToMd()` 函数（含 `word2pdf` 依赖和 LibreOffice 转换链路）
  - 更新模块描述和导出列表

## 2026-07-29 — [后端] v1.23.3 — 修复 LibreOffice Word 转 PDF 卡死

### 修复
- **`node-jinmao/utils/word2pdf.js`**：
  - **核心修复**：新增 `SAL_USE_VCLPLUGIN=gen` 和 `SOFFICE_HEADLESS=1` 环境变量，强制 LibreOffice 在无桌面服务器上使用通用 VCL 插件，不再尝试连接 X 显示服务器导致进程永久挂起
  - **优先使用内部 soffice 二进制**：在 `findLibreofficeBin` 中，对已解压的 squashfs-root，新增候选 `squashfs-root/usr/lib/libreoffice/program/soffice`（直接调用二进制文件，跳过 AppRun shell 脚本包装层，更可靠）
  - 新增 `--norestore` 命令行参数，禁用文档恢复对话框，避免交互式弹窗卡住进程
  - 新增 stdout 实时消费监听器，防止管道缓冲区阻塞
  - 新增 X11/显示服务错误检测（`cannot open display` / `display not set`），在 stderr 监听中快速识别 VCL 配置问题并立即终止进程
  - 对已解压 squashfs-root 同样设置 `APPIMAGE_EXTRACT_AND_RUN=1`，兼容无 FUSE 容器环境

## 2026-07-29 — [后端] v1.23.0 / [前端] v0.17.0 — 实现账单页面功能

### 新增
- **`node-jinmao/API/billing.js`**（新文件）：`GET /api/v1/billing?page=1&pageSize=20` 端点，返回用户 VIP 等级、开通计划、余额、已使用金额和扣费记录分页列表
- **`WEB/src/api/billing.js`**（新文件）：前端 `getBilling()` API 封装函数
- **`WEB/src/pages/billing/index.vue`**（新文件）：账单页面模板（VIP/计划/余额/已用金额卡片 + 扣费明细表格 + 分页 + "去充值"按钮）
- **`WEB/src/pages/billing/script.js`**（新文件）：账单页面业务逻辑（数据加载、分页、金额格式化、返回导航）
- **`node-jinmao/prisma/migrations/20260729_add_user_billing_fields/migration.sql`**（新文件）：数据库迁移 SQL

### 数据库变更
- **`node-jinmao/prisma/schema.prisma`**：
  - **User 表**新增 `vip_level`（VARCHAR(20)，默认 "free"）、`balance`（DECIMAL(18,7)，默认 0）、`plan`（VARCHAR(50)，默认 NULL）三个字段
  - **billing_record 表**中 8 个金额字段（input_cost、output_cost、total_cost、input_unit_price、input_cache_hit_price、output_unit_price、image_unit_price、tts_unit_price）从 `Float` 改为 `Decimal(18,7)`，确保 7 位小数精度

### 修改
- **`node-jinmao/utils/billing.js`**：导出 `CALL_TAG_LABELS` 映射表供 billing API 使用；新增 `ceilTo7Decimals()` 向上取整函数；`recordTokenUsage()` 和 `recordExternalCost()` 中所有金额计算应用向上取整到 7 位小数
- **`node-jinmao/service/auth/profile.js`**：`getProfile()` 和 `updateProfile()` 返回结构新增 `vipLevel`、`balance`、`plan` 字段
- **`node-jinmao/app.js`**：注册 `billingRouter` 到 `/api/v1` 前缀
- **`WEB/src/components/HomeSidebar.vue`**：底部区域新增"余额"入口（替换硬编码积分进度条），点击进入账单页面，挂载时自动加载余额；新增 `navigate-billing` emit
- **`WEB/src/pages/home/index.vue`**：HomeSidebar 新增 `@navigate-billing` 事件绑定
- **`WEB/src/pages/home/script.js`**：新增 `navigateToBilling()` 函数，处理侧边栏余额入口点击事件
- **`WEB/src/App.vue`**：新增 `BillingPage` 条件渲染和导航支持

## 2026-07-29 — [后端] v1.21.13 / [前端] v0.15.0 — 实现首页 4 项统计功能

### 新增
- **`node-jinmao/API/stats.js`**（新文件）：`GET /api/v1/stats` 端点，聚合返回累计学习时长、已完成章节数、习题正确率、连续学习天数 4 项指标
- **`node-jinmao/utils/repo/stats_repo.js`**（新文件）：封装 4 个统计查询函数（`getTotalStudyDuration`、`getCompletedChapters`、`getQuizAccuracy`、`getConsecutiveDays`），使用 Prisma aggregate + count 实现高效聚合
- **`node-jinmao/utils/repo/activity_repo.js`**（新文件）：`recordDailyActivity()` 函数，使用 upsert 记录用户每日活动（联合唯一约束防止重复）
- **`WEB/src/api/stats.js`**（新文件）：前端 `getStats()` API 封装函数

### 数据库变更
- **`node-jinmao/prisma/schema.prisma`**：新增 `UserDailyActivity` 表（字段：userId, activityDate，联合唯一约束），用于记录用户每日学习/刷题活动，支持连续学习天数计算

### 修改
- **`node-jinmao/API/progress.js`**：`PUT /progress` 新增可选参数 `studyDuration`（本次学习时长增量，秒），保存成功后自动记录每日活动
- **`node-jinmao/utils/repo/progress_repo.js`**：`upsertProgress()` 新增 `studyDuration` 参数，使用 Prisma `increment` 累加到已有记录
- **`node-jinmao/service/quiz_service.js`**：`saveRandomSessionProgress()` 和 `completeRandomSession()` 中增加每日活动记录（非阻塞）
- **`node-jinmao/app.js`**：注册 `statsRouter` 到 `/api/v1` 前缀
- **`WEB/src/api/progress.js`**：`saveProgress()` 新增可选参数 `studyDuration`
- **`WEB/src/pages/home/script.js`**：新增 `stats` 响应式状态、`loadStats()` 加载函数、`formatDuration()` 时长格式化函数，在 `onMounted` 中调用
- **`WEB/src/pages/home/index.vue`**：4 个统计卡片从硬编码 `--`+`即将上线` 改为数据绑定，底部标签根据数据状态显示不同文案
- **`WEB/src/pages/study/script.js`**：新增 `studySessionStart` 和 `accumulatedStudySeconds` 时间追踪变量，`saveProgressDebounced()` 和卸载清理中计算并上报学习时长增量

## 2026-07-29 — [后端] v1.21.12 / [前端] v0.14.6 — 重构"生成下一章"判断逻辑为后端单一真相来源

### 重构
- **`node-jinmao/utils/can_generate_next.js`**（新文件）：统一计算函数 `computeCanGenerateNext(course, chapters)`
  - 5 层优先级判断：课程完成 → 最后一章标记 → 有生成中 → 有已完成章节 → 无已完成章节
  - 后端 detail API 和 generate-next-chapter API 共用同一函数，消除前后端判断不一致
- **`node-jinmao/API/book/detail.js`**：响应新增 `canGenerateNext` 布尔字段
- **`node-jinmao/API/book/generate-next-chapter.js`**：3 个独立 if 检查块替换为单次 `computeCanGenerateNext` 调用
- **`WEB/src/pages/study/script.js`**：`canGenerateNext` computed 从 5 层条件简化为 3 层（加载中 + 防重复 + 后端权威值）

### 健硕化
- **`node-jinmao/service/course_pipeline.js`**（generateChapter）：
  - `extractLines` 返回 416 时也持久化 `isLastChapter=true`（原漏掉此场景）
  - 章节完成后兜底检测：`endline >= 文件总行数` 时补设 `isLastChapter=true`
- **`node-jinmao/utils/extractor_md.js`**：成功响应新增 `totalLineCount` 字段（供兜底检测使用，向后兼容）

## 2026-07-29 — [后端] v1.21.11 — 修正 imageBaseUrl 硬编码 /image 导致路径与实际目录不匹配

### 问题
- v1.21.9 引入的 `imageBaseUrl` 始终拼 `/image` 后缀，且 `.replace(/^image\//, "")` 只 strip `image/` 不处理 `images/`
- Doc2x 输出目录名为 `images`（复数），AI 从原文提取 PIC path 时也是 `images/xxx.jpg`
- 当 PIC path = `images/10_xxx.jpg` 时，strip 失败，最终 URL 变成 `/api/v1/files/.../xxx/image/images/10_xxx.jpg`（双重目录）
- MinIO 实际路径是 `.../images/10_xxx.jpg`，statObject 404 → 尺寸查询 0/N

### 修复
- `imageBaseUrl` 不再拼接 `/image` 后缀，改为纯基础路径（如 `/api/v1/files/usercourse/1/21/xxx`）
- imageInfos 构造时不再 `.replace(/^image\//, "")`，PIC 的 `path` 字段原样拼接（已自带 `image/` 或 `images/` 子目录）
- 影响 4 处 `imageBaseUrl` 构造 + 4 处 imageInfos 拼接（主流水线 / generateChapter / fixMissingFiles 各两处）
- `outline_prompt.txt` PIC 路径格式说明兼容 `image/` 和 `images/` 两种写法

## 2026-07-29 — [后端] v1.21.10 — 还原：图片上传路径跟随本地目录名（已在 v1.21.11 中被更彻底的方案替代）

## 2026-07-29 — [后端] v1.21.9 — PPT 图片引用支持传递像素尺寸，AI 可据此规划布局空间

### 新增
- **`node-jinmao/utils/image_size.js`**（新文件）：纯 Node.js 图片尺寸提取工具
  - 从 MinIO 仅读取图片头部（最多 64KB），通过解析二进制文件头获取像素宽高
  - 支持 JPEG（SOF0/SOF2）、PNG（IHDR）、GIF、WebP（VP8/VP8L/VP8X）、BMP 共 5 种格式
  - 含自动格式检测（通过魔数 fallback），不依赖任何第三方图像库
  - 导出 `getImageSize(minioClient, bucket, objectKey)`，返回 `{code, width?, height?, message?}`

### 修改
- **`node-jinmao/service/course_pipeline.js`**：
  - 新增 `deriveMinioKey()` 函数：从代理 URL 推导 MinIO 对象键
  - 新增 `enrichImageInfosWithSize()` 函数：批量并发查询图片尺寸并注入到 imageInfos
  - Phase4、generateChapter Step7、fixMissingFilesForChapter 三处均调用该函数
  - 尺寸查询失败不阻塞流程（容错设计），控制台记录查询耗时和成功率
- **`node-jinmao/utils/htmlppt.js`**：
  - imageInfos 格式化输出新增 `原始尺寸=WxHpx` 字段（有尺寸时）或 `原始尺寸=未知`（无尺寸时）
- **`node-jinmao/config/html_ppt_prompt.txt`**：
  - 输入说明：图片信息新增"原始尺寸"描述
  - 规则 C：新增"根据原始尺寸规划占位空间"约束，宽图/高图分别给予布局建议
  - 规则 D：新增"尺寸决定空间"约束，大图给大空间、小图给小空间

### 原理
- AI 生成 PPT HTML 时拿到每张图片的像素尺寸，可以更精确地规划 `<img>` 标签的占位面积和布局方向
- 宽图（如 2000x800）适合全宽横向排列，高图（如 600x1200）适合侧边栏布局

## 2026-07-29 — [后端] v1.21.8 — 修复 PPT 内图片引用因 AI 生成绝对 HTTPS URL 导致浏览器 SSL 错误

### 问题
- 测试环境站点通过 `http://jinmao.ckstu.top:30080` (HTTP) 访问，端口 30080 的 Nginx 仅支持 HTTP 不支持 HTTPS
- AI 生成 PPT HTML 时有时会将相对路径 `/api/v1/files/...` "聪明地"补全为绝对 URL（如 `https://jinmao.ckstu.top:30080/api/v1/files/...`）
- 浏览器尝试用 HTTPS 协议连接 30080 端口获取图片，报 `ERR_SSL_PROTOCOL_ERROR`，图片无法显示

### 修复
- **`node-jinmao/utils/htmlppt.js`**：新增 URL 规范化后处理
  - 在 LLM 生成 HTML 后、返回前，用正则将 `src="https?://任意域名/api/v1/files/..."` 还原为 `src="/api/v1/files/..."`
  - 同时支持 HTML 属性格式（`src="..."`、`href="..."`）和 CSS 函数格式（`url("...")`）
  - 外部 CDN URL（如 `https://cdn.jsdelivr.net/...`）不受影响
- **`node-jinmao/config/html_ppt_prompt.txt`**：
  - 规则 C（图片引用方式）新增约束：**严禁**将相对 URL 补全为绝对 URL，直接原样复制即可

### 原理
- 双保险策略：Prompt 层面约束 AI 行为 + 代码层面兜底清理，即使 AI 不遵守 Prompt 也能自动修复

## 2026-07-29 — [后端] v1.21.7 / [前端] v0.14.5 — 修复截断触发后仍可点击"生成下一章"的 BUG

### 问题
- `extractLines` 返回 206（内容截断到文件末尾）时，`isLastChapter` 仅为局部变量，未持久化到数据库
- 若后续 Steps（大纲/扩写/PPT/TTS）抛异常，catch 块不会设置 `pipelineStatus = "completed"`
- 用户仍能点击"生成下一章"创建无效章节，endline 已推进导致无限循环

### 修复
- **`node-jinmao/service/course_pipeline.js`**：
  - `isLastChapter` 从 try 块内 `const` 提升为外层 `let`，使 catch 块可访问
  - Step3 检测到 206 时**立即**调用 `bookRepo.updatePipelineProgress()` 持久化 `isLastChapter: true` 到 `pipelineProgress`
  - catch 块新增判断：若 `isLastChapter` 为 true，即使流水线异常也调用 `updatePipelineStatus(courseId, "completed")`
- **`node-jinmao/API/book/detail.js`**：API 响应新增 `pipelineProgress` 字段（含 `isLastChapter` 标记），供前端判断
- **`WEB/src/pages/study/script.js`**：
  - `canGenerateNext` 新增 `pipelineProgress.isLastChapter` 检测
  - `courseInfo` 新增接收 `pipelineProgress` 字段

### 原理
- API 端 `generate-next-chapter.js` 已有 `pipelineProgress.isLastChapter` 检查（L174-191），但因该字段从未被持久化而一直失效
- 现在 Step3 截断时立即写入，API 大门的第二重检查**首次真正生效**

## 2026-07-29 — [后端] v1.21.6 — 修复 PPT HTML 内容混入 LLM 解释文字导致渲染异常

### 问题
- PPT 幻灯片 HTML 中出现"我已经根据你的要求，生成了一个涵盖..."等 LLM 对话前缀文字，浏览器将这些文字当做正文渲染，导致幻灯片无法正常显示

### 修复
- **`node-jinmao/utils/htmlppt.js`**：新增自动提取 HTML 内容逻辑
  - 从 LLM 返回内容中查找 `<!DOCTYPE html>` 位置，自动截断前缀解释文字
  - 兼容无 DOCTYPE 声明的情况（从 `<html` 标签截取）
  - 兜底处理：从 `<body` 标签截取（限制偏移 < 100 字符，防止误判）
  - 每次截断操作会在控制台输出日志，便于排查
- **`node-jinmao/config/html_ppt_prompt.txt`**：强化输出格式约束
  - 将原简单的"仅输出HTML代码"改为"输出格式铁律"章节
  - 明确列举禁止的行为（前缀问候语、后缀总结、markdown代码块）
  - 提供正确/错误示例对比，让 LLM 更准确理解要求
  - 增加"第一个字符必须是 `<`，最后一个字符必须是 `>`" 等硬约束

## 2026-07-29 — [后端] v1.21.5 — 修复章节和课程流水线进度条始终显示 0 的 BUG

### 修复
- **`node-jinmao/service/course_pipeline.js`**：
  - `generateChapter()` Step7：`incrementChapterProgress(chapterId, "filesCompleted", 1/2)` → `"filesProgress.current"`，修复单章生成进度条"正在生成课件 0/N"始终不更新的问题
  - 主流水线 `phase4_generatePpt()` + `phase5_generateTts()`：`incrementPipelineProgress(courseId, "filesCompleted", 1/2)` → `"filesProgress.current"`，修复主流水线进度条同样的问题

### 根因
- 后端初始化进度数据时写入 `filesProgress: { current: 0, total: N }` 嵌套结构
- 但递增的是顶层 `filesCompleted` 字段
- 前端读取 `filesProgress.current`，该值从未被更新，始终显示 0

## 2026-07-29 — [前端] v0.14.4 — 修复翻页时进度条归零 + iframe 缩放居中

### 修复
- **`WEB/src/pages/study/script.js`**：`goToPage()` 函数修复两个问题：
  - 手动翻页前进时，`chapterElapsedTime` 没有累加当前页时长（之前仅依赖 `onAudioEnded` 累加，手动点击"下一页"不触发），导致进度条显示"当前时间"被低估
  - `progressPercent` 翻页时被直接归零，修复为基于 `chapterElapsedTime / chapterTotalTime` 计算，确保进度条不会从0开始跳动
  - 自动翻页（`shouldAutoResume=true`）时不重复累加，避免 `onAudioEnded` 中已累加过的时长被重复计算
- **`WEB/src/pages/study/index.vue`**：iframe 缩放策略从左上角原点缩放改为居中等比缩放：
  - `left: '50%'; top: '50%'; transform: 'translate(-50%, -50%) scale(...)'` 替代 `top: 0; left: 0; transformOrigin: '0 0'; transform: scale(...)'
  - 解决容器比例变化时（全屏、窗口拖拽）PPT 内容不居中导致布局错乱的问题

## 2026-07-29 — [后端] v1.20.4 — 强化 HTML PPT 图片引用约束并修复图片 URL 丢失 BUG

### 修改
- **`node-jinmao/config/html_ppt_prompt.txt`**：
  - 图片处理部分重构为四规则结构：禁止自行绘制专业图片（规则A）、仅允许简单图形自绘（规则B）、图片引用方式（规则C）、排版与位置（规则D）
  - 新增严格约束：严禁使用 SVG/CSS/Canvas 绘制照片、截图、科学图表等专业图片，有可用图片 URL 时必须使用 `<img>` 标签引用
  - 明确仅简单流程图、编号圆圈、装饰元素等允许自绘

### 修复
- **`node-jinmao/service/course_pipeline.js`**：
  - `generateChapter()` Step7（单章PPT生成）：原先硬编码传入空数组 `[]`，修复为从 `slide.PIC` 构造完整 `imageInfos` 并传入 `generateHtmlPpt()`
  - `fixMissingFilesForChapter()` PPT补全：原先硬编码传入空数组 `[]`，修复为从 `slide.PIC` 构造完整 `imageInfos` 并传入 `generateHtmlPpt()`
  - 两个修复使用与 `phase4_generatePpt()` 一致的 imageInfos 构造逻辑

## 2026-07-29 — [后端] v1.20.3 — HTML PPT 提示词新增字体大小约束

### 修改
- **`node-jinmao/config/html_ppt_prompt.txt`**：
  - 视觉设计部分新增字体大小规范：正文/要点等需要用户看清的内容禁止小于 2rem，辅助文字（标注、脚注等）最小不允许小于 1.5rem，标题建议 2.5rem ~ 3.5rem

## 2026-07-29 — [后端] v1.21.3 / [前端] v0.14.3 — 修复扩写进度 totalSlides 为 0 + ElMessage 位置超出页面

### 修复
- **`node-jinmao/service/course_pipeline.js`**：`phase3_generateOutline()` 中，将 `totalSlides` 写入 `pipelineProgress` 的时机从扩写完成后提前到扩写开始前，修复前端轮询进度 API 时 `elaborationProgress.total` 为 0 导致显示 "3/0" 的问题
- **`WEB/src/pages/home/script.js`**：`onOpenCourse()` 中 `ElMessage.warning` 调用增加 `offset: 80` 配置，确保提示框不超出页面顶部区域

## 2026-07-29 — [前端] v0.14.2 — 首页添加 "normalizing" 状态的中文标签映射

### 修复
- **`WEB/src/components/CourseCard.vue`**：`statusLabel` switch 映射中新增 `case "normalizing": return "归一化处理中"`，修复首页教材卡片显示英文 `normalizing` 角标的问题（之前该状态落入 `default` 分支直接显示了原始英文值）

## 2026-07-29 — [后端] v1.21.2 / [前端] v0.14.1 — 已有章节访问时自动补全 + 前端补全状态轮询

### 修复
- **`node-jinmao/API/course/slides.js`**：
  - 导入 `fixMissingFilesForChapter`，新增 `checkedChapters` Set
  - `GET /courses/:courseId/chapters/:chapterId/slides`：对 `completed`/`partial_completed` 章节首次访问时触发完整性检测
  - **关键**：触发补全时立即 `await chapterRepo.updateChapter({ status: "partial_completed" })` 更新 DB 状态，再异步执行 `fixMissingFilesForChapter`
  - 响应中新增 `isFixingMissing: true` 字段，前端据此立即显示横幅+启动轮询
- **`node-jinmao/service/course_pipeline.js`**：
  - `fixMissingFilesForChapter()`：当 `completed` 章节发现缺失文件时，先更新状态为 `partial_completed` 再补全
- **`WEB/src/pages/study/script.js`**：
  - `loadChapterSlides()`：响应中检测 `chapter.isFixingMissing`，若为 `true` 立即显示补全横幅+启动 3 秒间隔轮询，跳过前端采样检测（避免重复触发 fixMissingFiles API）

### 背景
- 之前自动补全仅在后台异步执行，前后端状态不同步：后端正在补全但前端仍显示绿色对号（completed），导致音频加载 404 且无任何提示
- 修复后：前端第一时间感知补全状态 → 显示"正在自动补全"横幅 → 轮询等待完成 → 刷新章节数据

## 2026-07-29 — [后端] v1.21.1 — Phase 6 缺失文件自动补全 + isLastChapter 状态修复

### 修复
- **`node-jinmao/service/course_pipeline.js`**：
  - `phase6_validate()`：检测到 `missingFiles.length > 0` 时自动调用 `fixMissingFilesForChapter(courseId, chapter.id)` 启动后台异步补全（之前只标记 `partial_completed` 不触发补全，需用户手动调用 fix-missing API）
  - `phase6_validate()`  `isLastChapter` 分支：修复最后一章无条件设为 `completed` 的 bug——当最后一章仍有缺失文件时，章节状态设为 `partial_completed` 并自动触发补全（之前无论是否有缺失文件都标记 `completed`，导致左侧显示绿色对号但文件实际缺失）
  - `generateChapter()` Step 8：同样在非全部成功时自动触发 `fixMissingFilesForChapter`（之前只标记 `partial_completed`）
  - `generateChapter()` Step 8  `isLastChapter` 分支：同样区分全部成功/部分缺失两种情况，缺失时触发补全
  - `fixMissingFilesForChapter` 内部有去重 Map 机制（`fixingChapters`），同一章节多次触发自动合并

### 背景
- 流水线 Phase 6 完整性校验发现 SRT/MP3/PPT 文件缺失时，仅输出警告日志并标记 `partial_completed`，无自动补全机制
- `isLastChapter` 分支无条件标记 `completed`，导致最后一章即使文件缺失也显示绿色对号，用户误以为文件完整

## 2026-07-29 — [后端] v1.21.0 — 修复章节生成5个问题（行号校验/416处理/API拦截）

### 修复
- **`node-jinmao/service/course_pipeline.js`**：
  - `phase2_extractAndIndex()` + `generateChapter()` Step 3：AI 返回行号后新增 endline 校验逻辑，当 AI 返回的 endline 超出提取范围实际最大行号时自动截断（根因：AI 可能返回如 1818 这样超出文件总行数的幽灵行号，导致后续逻辑异常）
  - `generateChapter()` Step 3：新增 416 错误码处理（起始行超出文件总行数），当检测到 extractLines 返回 416 时，将课程 pipelineStatus 标记为 `"completed"`，将空章节标记为 `failed`，优雅返回而非抛异常
- **`node-jinmao/API/book/generate-next-chapter.js`**：
  - 新增课程完成状态双重检查：① `pipelineStatus === "completed"` 时拒绝生成 ② `pipelineProgress.isLastChapter` 标记存在时拒绝生成，均返回 400 并提示「所有章节已生成完毕」

## 2026-07-29 — [前端] v0.14.0 — 章节状态图标区分 + 课程完成后禁用生成按钮

### 修复
- **`WEB/src/pages/study/index.vue`**：
  - 将 `partial_completed` 的图标从绿色对号改为橙色警告图标（`&#9888;`），并与 `completed` 分开独立判断，避免不完整章节误导用户
- **`WEB/src/pages/study/script.js`**：
  - `canGenerateNext` 计算属性新增 `courseInfo.value?.pipelineStatus === "completed"` 判断，课程整体完成后不再显示"生成下一章"按钮（后端 API 同时也有拦截保护）



## 2026-07-29 — [后端] v1.20.4 — Phase 6 发现文件缺失时自动触发补全重试

### 修复
- **`node-jinmao/service/course_pipeline.js`**：
  - `phase6_validate()`：当检测到 `missingFiles.length > 0` 时，自动调用 `fixMissingFilesForChapter(courseId, chapter.id)` 启动后台文件补全任务（之前只标记 `partial_completed`，不触发补全，用户需手动调用 fix-missing API）
  - `generateChapter()` Step 8：同样在标记 `partial_completed` 前自动触发 `fixMissingFilesForChapter`，确保单章生成场景下也能自动补全
  - `fixMissingFilesForChapter` 内部有去重 Map 机制（`fixingChapters`），同一章节多次触发会被合并，不会重复执行

### 背景
- 流水线 Phase 6 完整性校验发现 SRT/MP3/PPT 文件缺失时，仅输出警告日志并标记 `partial_completed`，没有任何自动重试/补全机制。用户需手动访问 API 端点触发补全
- 修复后在两个入口（主流水线 + 单章生成）均自动触发后台补全，对前端透明

## 2026-07-29 — [前端] v0.13.0 — 文件补全期间禁用章节切换，防止用户误操作

### 新增
- **`WEB/src/pages/study/index.vue`**：
  - 章节列表点击逻辑增强：当 `isFixingMissing === true`（后台正在补全缺失文件）时，所有章节行显示 `cursor-not-allowed opacity-60` 样式，点击被禁用，防止用户乱点干扰补全过程
  - 当前活跃章节下方新增"正在修复文件"内联提示条，含旋转加载图标和 `fixingBannerText` 提示文字，颜色使用语义 amber（警告色）
- **`WEB/src/pages/study/script.js`**：
  - `switchChapter()` 函数新增 `isFixingMissing` 守卫：文件补全进行中时弹出 `ElMessage.warning("正在修复文件，请稍候再切换章节")` 并返回，从函数级别确保切换被阻止

### 背景
- 当章节状态为 `partial_completed` 时，后台可能正在自动检测并补全缺失的 PPT/Audio/SRT 文件。此前用户可在补全期间随意切换章节，可能导致文件状态不一致或补全中断。现通过前端双重防护（模板层 + 函数守卫）确保补全期间用户无法切换章节。

## 2026-07-29 — [后端] v1.20.3 — HTML PPT 提示词新增字体大小约束

### 修改
- **`node-jinmao/config/html_ppt_prompt.txt`**：
  - 视觉设计部分新增字体大小规范：正文/要点等需要用户看清的内容禁止小于 2rem，辅助文字（标注、脚注等）最小不允许小于 1.5rem，标题建议 2.5rem ~ 3.5rem

## 2026-07-29 — [后端] v1.20.2 — 修复 TTS 合成因 userId 未传递导致计费失败返回 500

### 修复
- **`node-jinmao/service/text_tts.js`**：
  - `callTtsApi(text, ttsConfig)` 函数签名缺少 `userId` 参数，导致内部调用 `recordExternalCost()` 时 `userId` 未定义抛出 ReferenceError
  - 该错误被外层 try-catch 捕获后返回 `{ code: 500, message: "文件写入失败: userId is not defined" }`，但此时 MP3 和 SRT 文件实际上已成功写入
  - **修复**：给 `callTtsApi` 添加 `userId` 参数，`synthesize()` 调用时传入
  - **加固**：将计费记录逻辑用独立 try-catch 包裹，确保计费失败不会影响文件输出的成功响应

### 根因分析
- `synthesize(userId, text)` 接收 `userId` 但未传递给内部函数 `callTtsApi(text, ttsConfig)`
- `callTtsApi` 内 `recordExternalCost({ userId: userId })` 引用了不存在的变量，抛出 `ReferenceError`
- 文件写入（MP3/SRT）与计费记录在同一个 try-catch 块中，计费异常导致整体返回 500
- 计费记录从未成功执行，因此 TTS 调用无计费记录

## 2026-07-29 — [后端] v1.20.1 — 修复章节生成轮询因 status 被硬编码为 pending 而提前停止

### 修复
- **`node-jinmao/utils/repo/chapter_repo.js`**：
  - `createChapter()` 中将硬编码的 `status: "pending"` 改为 `status: data.status || "pending"`，允许调用方传入指定状态（如 `generating`），否则默认 `pending`
  - 此前 `generate-next-chapter` API 传入 `status: "generating"` 被忽略，导致数据库中章节状态始终为 `pending`
- **`node-jinmao/service/course_pipeline.js`**：
  - `generateChapter()` 流水线启动时显式调用 `updateChapter(chapterId, { status: "generating" })` 作为安全加固，确保即使创建时状态不对，流水线启动后前端也能通过轮询正确识别

### 根因分析
- `generate-next-chapter` API 调用 `chapterRepo.createChapter({ status: "generating" })` 创建章节，但 `createChapter` 硬编码了 `status: "pending"`，导致传入的 `generating` 被丢弃
- 前端轮询 `getChapterGenerationProgress` 时，后端查询数据库发现章节状态为 `pending`，而 `pending` 在终端状态白名单中（`["completed", "partial_completed", "failed", "pending"]`），因此返回 `isTerminal: true`
- 前端收到 `isTerminal: true` 后更新本地章节状态为 `pending`，下一次轮询发现无 `generating` 状态章节即停止轮询
- 但此时后台 `generateChapter` 流水线仍在正常运行，导致前端无法获取实时进度

## 2026-07-29 — [前端] v0.12.1 — 优化章节未生成提示为内联展示

### 修改
- **`WEB/src/pages/study/index.vue`**：
  - 章节列表项下方新增 pending/failed 状态的内联提示条（amber 色系警告风格，与 generating 进度条设计一致）
  - pending 显示"该章节尚未生成，请点击下方"生成下一章"按钮"
  - failed 显示"该章节生成失败，请点击"生成下一章"重新生成"
- **`WEB/src/pages/study/script.js`**：
  - `showGeneratingTip()` 函数清空为 no-op，提示信息已内联到模板中展示，不再弹出 ElMessage 浮动消息

### 改进
- 非已完成章节的状态提示从浮动弹窗改为页面内联展示，用户体验更好

## 2026-07-29 — [前端] v0.11.0 — PPT iframe 改为固定尺寸缩放渲染

### 修改
- **`WEB/src/pages/study/index.vue`**：
  - ppt-container 添加 `overflow-hidden`，配合 iframe 缩放防止内容溢出
  - iframe 从百分比宽高（`w-full h-full inset-0`）改为固定 1920×1080 像素渲染
  - iframe 绑定动态 `transform: scale()` 缩放，通过 `pptScale` 响应式变量驱动
- **`WEB/src/pages/study/script.js`**：
  - 新增 `pptScale`、`pptBaseWidth`、`pptBaseHeight` 响应式变量
  - 新增 `updatePptScale()` 函数：根据 `pptContainer` 实际尺寸动态计算缩放比，使用 requestAnimationFrame 防抖
  - `onMounted` 中添加 ResizeObserver 监听 `pptContainer` 尺寸变化，自动触发缩放重算
  - `onUnmounted` 中清理 ResizeObserver 和 RAF 定时器
  - `toggleFullscreen()` 全屏切换后通过 `nextTick` 重新计算缩放比
  - `onPptLoad()` iframe 加载完成后重新计算缩放比

### 改进
- iframe 内部视口始终固定为 1920×1080，不再受容器尺寸影响，避免 AI 生成的 PPT 幻灯片因响应式布局导致的内容错位/变形
- 缩放策略采用 `contain`（取 `Math.min(scaleX, scaleY)`），全屏模式下也能保证内容完整可见

## 2026-07-29 — [后端] v1.19.0 — 新增 LLM 统一客户端 & 计费系统

### 新增
- **`utils/llm_client.js`**：统一 OpenAI 客户端管理模块
  - 惰性管理大/小模型两种客户端实例，消除 5 个 utils 文件中的重复 getOpenAI 代码
  - 暴露 `chat(userId, callTag, options)` 统一调用接口
  - 自动提取 `completion.usage`（含 `cache_hit_tokens` / `cache_miss_tokens`），调用 billing 模块计费
  - 集成心跳定时器和看门狗定时器，无需各模块独立管理
- **`utils/billing.js`**：计费模块
  - `recordTokenUsage()` — LLM Token 计费（按缓存命中/未命中分别计价）
  - `recordExternalCost()` — 文生图/ TTS 计量计费（图片按张数、TTS 按字符数）
  - `getActivePeriod()` — 时段匹配，支持多时段独立定价（高峰/低谷），支持跨天时段
  - 价格从 `config/billing_pricing.json` 实时读取，修改后无需重启
  - 计费记录写入 `billing_record` 表（Prisma）
- **`config/billing_pricing.json`**：价格配置文件（独立于代码）
  - DeepSeek pro/flash 官方定价（元/百万tokens），支持缓存命中/未命中分别计价
  - Grsai 文生图 ¥0.037/张
  - 火山引擎 TTS seed-tts-2.0 ¥0.0002/字符（¥2/万字符）
  - 三段时段：default（00:00-24:00）、peak（08:00-15:00）、off_peak（15:00-08:00 跨天）
  - 默认未启用时段分价，仅使用 default 段
- **数据库 `billing_record` 表**：记录每次 API 调用的详细账单
  - 字段覆盖 LLM（token 明细、缓存命中/未命中）、文生图（图片数量、分辨率）、TTS（字符数、音频时长）
  - 按 user_id 和 created_at 建索引

### 重构
- **5 个 DeepSeek utils 统一改用 `llm_client.chat()`**：
  - `utils/create_title.js` — 删除 getOpenAI，改用 llm_client.chat(userId, "title", ...)
  - `utils/get_line.js` — 删除 getOpenAI，改用 llm_client.chat(userId, "getline", ...)
  - `utils/elaboration.js` — 删除 getOpenAI，改用 llm_client.chat(userId, "elaboration", ...)
  - `utils/generate_outline.js` — 删除 getOpenAI，改用 llm_client.chat(userId, "outline", ...)
  - `utils/htmlppt.js` — 删除 getOpenAI，改用 llm_client.chat(userId, "htmlppt", ...)
  - 所有文件删除独立的看门狗/心跳定时器（由 llm_client 统一管理）
  - 所有函数签名新增 `userId` 作为第一个参数
- **`utils/create_image.js` 文生图计费**：新增 userId 参数，成功后调用 `recordExternalCost()`
- **`service/text_tts.js` TTS 计费**：新增 userId 参数，成功后按字符数调用 `recordExternalCost()`
- **`service/course_pipeline.js` 全部调用方适配**：所有 llm utils / createImage / synthesize 调用均已传入 userId
- **`service/create_cover_image.js`**：createImage 调用传入 userId
- **`service/create_title.js`**：createTitle 调用传入 userId
- **`config/index.js`**：新增加载 `billing_pricing.json`

### 涉及文件
- `node-jinmao/utils/llm_client.js` （新建）
- `node-jinmao/utils/billing.js` （新建）
- `node-jinmao/config/billing_pricing.json` （新建）
- `node-jinmao/prisma/schema.prisma`
- `node-jinmao/config/index.js`
- `node-jinmao/utils/create_title.js`
- `node-jinmao/utils/get_line.js`
- `node-jinmao/utils/elaboration.js`
- `node-jinmao/utils/generate_outline.js`
- `node-jinmao/utils/htmlppt.js`
- `node-jinmao/utils/create_image.js`
- `node-jinmao/service/text_tts.js`
- `node-jinmao/service/course_pipeline.js`
- `node-jinmao/service/create_cover_image.js`
- `node-jinmao/service/create_title.js`
- `node-jinmao/package.json`

## 2026-07-29 — [后端] v1.18.6 — htmlppt.js 重构：改用统一 llm_client 调用

### 重构
- **`utils/htmlppt.js` 重构**：将 DeepSeek API 直接调用改为使用统一的 `llmClient.chat()` 模块
  - 删除 `getOpenAI()` 惰性加载函数和 `import("openai")` 相关代码
  - 删除 `const { deepseek: config, DEEPSEEK_TIMEOUT } = require("../config")` 直接依赖
  - 新增 `const llmClient = require("./llm_client")` 统一调用
  - `main()` 函数新增 `userId` 作为第一个参数，用于 `llm_client` 自动计费关联
  - 删除独立的看门狗定时器（`watchdogTimer`）和心跳定时器（`heartbeatInterval`），改由 `llm_client` 统一处理
  - 将 `openai.chat.completions.create()` 替换为 `llmClient.chat(userId, "htmlppt", { modelSize: "big", ... })`，并使用 `chatResult.code` / `chatResult.message.content` 进行结果处理
  - 保留所有输入验证（`validateString` 字符串校验、`imageInfos` 数组校验）、prompt 模板替换、markdown 代码块清理、HTML 有效性校验等后处理逻辑
  - 更新 JSDoc 注释，新增 `@param {string} userId` 参数说明
- **`service/course_pipeline.js` 同步适配**：三处 `generateHtmlPpt()` 调用均添加 `userId` 作为第一个参数
  - `phase4_generatePpt()` 函数签名新增 `userId` 参数，调用处传入 `course.userId`
  - `generateChapter()` Step7（line 1037）：传入 `course.userId`
  - `fixMissingFilesForChapter()` fix PPT（line 1384）：从 `courseResult.course.userId` 提取 `fixUserId` 传入

### 涉及文件
- `node-jinmao/utils/htmlppt.js`
- `node-jinmao/service/course_pipeline.js`
- `node-jinmao/package.json`

## 2026-07-29 — [后端] v1.18.4 — get_line.js 重构：改用统一 llm_client 调用

### 重构
- **`utils/get_line.js` 重构**：将 DeepSeek API 直接调用改为使用统一的 `llmClient.chat()` 模块
  - 删除 `getOpenAI()` 惰性加载函数和 `import("openai")` 相关代码
  - 删除 `const { deepseek: config, DEEPSEEK_TIMEOUT } = require("../config")` 直接依赖
  - 新增 `const llmClient = require("./llm_client")` 统一调用
  - `getLine()` 函数新增 `userId` 作为第一个参数，用于 `llm_client` 自动计费关联
  - 删除独立的看门狗定时器（`watchdogTimer`），改由 `llm_client` 统一处理
  - 将 `openai.chat.completions.create()` 替换为 `llmClient.chat(userId, "getline", {...})`，并使用 `chatResult.code` / `chatResult.message.content` 进行结果处理
  - 保留所有输入验证（`validateString`）、JSON 解析、markdown 代码块清理、格式校验等后处理逻辑
  - 更新 JSDoc 注释，新增 `@param {string} userId` 参数说明
- **`service/course_pipeline.js` 同步适配**：两处 `getLine()` 调用均添加 `course.userId` 作为第一个参数
  - `phase2_extractAndIndex()`（line 163）：`getLine(course.userId, indexedText)`
  - `generateChapter()`（line 884）：`getLine(course.userId, indexedText)`

### 涉及文件
- `node-jinmao/utils/get_line.js`
- `node-jinmao/service/course_pipeline.js`
- `node-jinmao/package.json`

## 2026-07-29 — [后端] v1.18.3 — generate_outline.js 重构：改用统一 llm_client 调用

### 重构
- **`utils/generate_outline.js` 重构**：将 DeepSeek API 直接调用改为使用统一的 `llm_client.chat()` 模块
  - 删除 `getOpenAI()` 惰性加载函数和 `import("openai")` 相关代码
  - 删除 `const { deepseek: config, DEEPSEEK_TIMEOUT } = require("../config")` 直接依赖
  - 新增 `const llmClient = require("./llm_client")` 统一调用
  - `main()` 函数新增 `userId` 作为第一个参数，用于 `llm_client` 自动计费关联
  - 删除独立的心跳定时器（`heartbeatInterval`）和看门狗定时器（`watchdogTimer`），改由 `llm_client` 统一处理
  - 将 `openai.chat.completions.create()` 替换为 `llmClient.chat()`，并使用 `chatResult.code` / `chatResult.message.content` 进行结果处理
  - 保留所有输入验证（`validateFields`）、JSON 解析、数组自动包装等后处理逻辑
  - 更新 JSDoc 注释，新增 `@param {string} userId` 参数说明
- **`service/course_pipeline.js` 同步适配**：两处 `generateOutline()` 调用均添加 `course.userId` 作为第一个参数
  - `phase3_generateCourse()`（line 238）：`generateOutline(course.userId, chapterText, course.name || "课程")`
  - `generateChapter()`（line 922）：`generateOutline(course.userId, chapterText, course.name || "课程")`

### 涉及文件
- `node-jinmao/utils/generate_outline.js`
- `node-jinmao/service/course_pipeline.js`
- `node-jinmao/package.json`

## 2026-07-28 — [后端] v1.18.2 / [前端] v0.10.2 — 新增文件缺失自动补全功能

### 新增
- **文件缺失自动补全**：当用户学习 `partial_completed` 章节时，前端自动检测文件完整性并触发后端补全任务
  - 后端 `course_pipeline.js` 新增 `fixMissingFilesForChapter()` 函数：从 MinIO 读取大纲 JSON → 逐页检查 PPT/MP3/SRT → 对缺失文件调用 TTS/PPT 重新生成 → 上传 MinIO → 更新章节状态为 `completed`
  - 后端新增 `API/book/fix-missing.js` 路由模块（两个端点）：
    - `POST /api/v1/courses/:courseId/chapters/:chapterId/fix-missing` — 触发文件补全（含完整 OpenAPI JSDoc）
    - `GET /api/v1/courses/:courseId/chapters/:chapterId/fix-status` — 查询补全状态（供前端轮询）
  - 内存去重 Map（`fixingChapters`）：同一章节同时只允许一个补全任务，防止多次刷新重复创建
- **前端补全横幅提示**：学习页顶栏下方显示琥珀色横幅，带旋转加载图标 + "检测到部分文件缺失，正在自动补全..." 提示文字
- **前端轻量取样检测**：加载 slides 后对第一页/中间页/最后一页的 SRT 做 HEAD 请求探测，发现 404 即触发补全
- **自动状态刷新**：补全完成后自动刷新 slides 数据和章节状态，横幅消失

### 后端变更
- **修改文件**：
  - `node-jinmao/service/course_pipeline.js`：新增 `fixMissingFilesForChapter()`（约 200 行）+ `getFixStatus()` + 内存去重 Map，导出新函数
  - `node-jinmao/API/book/index.js`：注册 `fix-missing` 路由
- **新文件**：
  - `node-jinmao/API/book/fix-missing.js`：补全 API 路由模块（296 行，含完整 OpenAPI JSDoc）

### 前端变更
- **修改文件**：
  - `WEB/src/pages/study/script.js`：新增 `detectAndFixMissingFiles()`、`startFixStatusPolling()`、`stopFixStatusPolling()` 函数 + 文件补全相关响应式状态 + 导入新 API
  - `WEB/src/pages/study/index.vue`：新增补全横幅 UI（顶栏下方琥珀色横幅）
  - `WEB/src/api/books.js`：新增 `fixMissingFiles()` 和 `getFixStatus()` API 封装

### 涉及文件
- `node-jinmao/service/course_pipeline.js`
- `node-jinmao/API/book/fix-missing.js`（新建）
- `node-jinmao/API/book/index.js`
- `node-jinmao/package.json`
- `WEB/src/pages/study/script.js`
- `WEB/src/pages/study/index.vue`
- `WEB/src/api/books.js`
- `WEB/package.json`
- `开发日志.md`
- `FILE.md` / `node-jinmao/FILE.md`

## 2026-07-28 — [后端] v1.18.1 / [前端] v0.10.1 — 修复 Prisma Client require 缓存 & partial_completed 章节支持

### 修复
- **Prisma Client require 缓存未清理问题**：`prisma generate` 成功执行后，`progress_repo.js` 仍报 `UserStudyRecord` 缺失
  - **根因**：启动自检中的模型验证代码 `require("@prisma/client")` 先缓存了旧版模块，即使 `prisma generate` 更新了文件，后续 `require` 仍返回缓存中的旧版本
  - **修复（`app.js`）**：`prisma generate` 成功后清除所有 `.prisma` 和 `@prisma` 相关的 Node.js require 缓存
- **"生成下一章"按钮不显示**：章节状态为 `partial_completed`（完整性校验失败导致部分生成完成）时按钮不出现
  - **根因**：前端 `canGenerateNext`、`switchChapter`、`loadCourseData` 等逻辑只认 `status === "completed"`，不识别 `partial_completed`
  - **修复（`script.js` + `index.vue`）**：定义 `VALID_COMPLETED_STATUSES = ["completed", "partial_completed"]`，所有相关判断统一使用该常量
  - 影响范围：按钮显示、章节点击守卫、进度恢复、默认章节选择、状态图标

### 已知问题
- 课程 19 缺少 `usercourse/1/19/chapter_01/SRT/slide_15.srt` 文件，导致第 15 页字幕 404。这是流水线生成阶段的问题，需要检查完整性校验日志定位具体是哪一步失败

## 2026-07-28 — [后端] v1.18.1 — 修复 Prisma Client 模型缺失导致学习进度 API 500 错误

### 修复
- **学习进度 API 500 错误修复**：`GET /api/v1/progress?courseId=19` 返回 `Cannot read properties of undefined (reading 'findFirst')`
  - **根因**：`ftp_upload.ps1` 排除 `node_modules` 但同时上传了 `.schema_hash`，导致服务器上的 Prisma Client（旧版，未包含 `UserStudyRecord` 模型）与 `schema.prisma` 哈希匹配，跳过了 `prisma generate`，`prisma.userStudyRecord` 为 `undefined`
  - **修复 1（`app.js`）**：在 schema 哈希匹配后增加 Prisma Client 模型完整性验证——读取 `schema.prisma` 提取所有 model 名称，创建临时 `PrismaClient` 实例逐一检查模型是否存在。若发现缺失模型则强制重新执行 `prisma generate`，而非盲目信任哈希匹配
  - **修复 2（`progress_repo.js`）**：模块加载时检查 `prisma.userStudyRecord` 是否存在，缺失时输出清晰的错误日志并提示运行 `npx prisma generate`

## 2026-07-25 — [后端] v1.18.0 / [前端] v0.10.0 — 下一章生成 & 自动循环生成

### 新增
- **下一章生成功能**：侧边栏新增"生成下一章"按钮，点击后触发后台流水线生成教材的下一章
  - 根据课程 `endline` 自动识别未处理的内容，创建新章节并异步生成
  - API：`POST /api/v1/courses/:courseId/generate-next-chapter`（含完整 OpenAPI JSDoc）
- **自动循环生成**：开启"自动生成"开关后，学习第 N 章时自动触发第 N+1 章的生成
  - 防止过量生成浪费积分，确保用户学习不中断
  - 开关状态通过 `localStorage` 持久化，每个课程独立记忆
- **章节生成进度条**：侧边栏章节项下方显示微型进度条（参考首页 CourseCard 样式）
  - 实时显示阶段（大纲生成/口播扩写/课件生成/校验）及百分比
  - API：`GET /api/v1/courses/:courseId/chapters/:chapterId/generation-progress`
  - 前端 3 秒轮询获取进度，生成完成后自动停止并提示
- **章节点击守卫**：仅 `completed` 状态的章节可点击进入学习，`generating`/`pending`/`failed` 状态点击时弹出友好提示

### 数据库变更
- **Chapter 表新增 `generation_progress` 字段**（VARCHAR(1000) NULL）：记录单个章节的生成流水线实时进度 JSON
  - 迁移文件：`prisma/migrations/20260725_add_chapter_generation_progress/migration.sql`

### 后端变更
- **新文件**：
  - `node-jinmao/API/book/generate-next-chapter.js`：下一章生成 API + 章节进度查询 API（含完整 OpenAPI JSDoc 注释）
- **修改文件**：
  - `node-jinmao/service/course_pipeline.js`：新增 `generateChapter(courseId, chapterId)` 函数并导出，复用 Phase 2-6 逻辑完成单章节生成
  - `node-jinmao/utils/repo/chapter_repo.js`：新增 `updateChapterProgress()` 和 `incrementChapterProgress()`，对标 `book_repo` 的同名函数
  - `node-jinmao/API/book/detail.js`：章节数据返回时包含 `generationProgress` JSON 字段
  - `node-jinmao/API/book/index.js`：注册新路由 `generate-next-chapter`
  - `node-jinmao/prisma/schema.prisma`：Chapter 表新增 `generationProgress` 字段定义

### 前端变更
- **修改文件**：
  - `WEB/src/pages/study/index.vue`：侧边栏新增进度条区域、"生成下一章"按钮、自动生成开关、章节点击守卫
  - `WEB/src/pages/study/script.js`：新增生成、自动生成、进度轮询、进度条辅助函数等完整业务逻辑（新增 ~330 行，添加超长说明）
  - `WEB/src/api/books.js`：新增 `generateNextChapter()` 和 `getChapterGenerationProgress()` API 封装

### 涉及文件
- `node-jinmao/prisma/schema.prisma`
- `node-jinmao/prisma/migrations/20260725_add_chapter_generation_progress/migration.sql`
- `node-jinmao/service/course_pipeline.js`
- `node-jinmao/utils/repo/chapter_repo.js`
- `node-jinmao/API/book/generate-next-chapter.js`
- `node-jinmao/API/book/detail.js`
- `node-jinmao/API/book/index.js`
- `node-jinmao/package.json`
- `WEB/src/pages/study/index.vue`
- `WEB/src/pages/study/script.js`
- `WEB/src/api/books.js`
- `WEB/package.json`
- `FILE.md` / `node-jinmao/FILE.md`
- `开发日志.md`

## 2026-07-25 — [后端] v1.17.0 — PPT 生成新增原文图片引用功能

### 新增
- **PPT 生成原文图片引用链路**：补全了从大纲生成到 HTML PPT 输出的完整图片引用能力
  - 大纲生成阶段：`outline_prompt.txt` 升级 PIC 字段格式，从 `string[]`（仅路径）升级为 `object[]`（`{path, desc}` 含图片描述），让 AI 理解每张图的内容
  - 流水线阶段：`course_pipeline.js` Phase 4 从 `course.textbookPath` 推导图片代理 URL，将 PIC 转为完整图片 URL + 描述后传入 `generateHtmlPpt()`
  - PPT 生成阶段：`htmlppt.js` 支持新旧两种图片信息格式，将图片 URL 和描述格式化为可读文本嵌入 prompt
  - 提示词强化：`html_ppt_prompt.txt` 新增图片描述利用指引，指导 AI 根据描述将图片放在对应内容区域

### 后端变更
- **配置**：
  - `node-jinmao/config/outline_prompt.txt`：PIC 字段格式升级（`[{path, desc}]`），新增图注提取指引
  - `node-jinmao/config/html_ppt_prompt.txt`：更新图片输入格式说明，强化图片排版和使用指引
- **工具模块**：
  - `node-jinmao/utils/htmlppt.js`：参数 `imageUrls` → `imageInfos`，支持 `string[]` 和 `object[]`（`{url, desc}`）两种格式，新版输入验证兼容字符串和对象类型
- **流水线**：
  - `node-jinmao/service/course_pipeline.js`：`phase4_generatePpt()` 新增 `imageBaseUrl` 参数；`pipeline()` 主函数从 `course.textbookPath` 推导图片基础 URL；Phase 4 循环中处理 PIC 字段并构造完整图片信息传给 AI

### 涉及文件
- `node-jinmao/config/outline_prompt.txt`
- `node-jinmao/config/html_ppt_prompt.txt`
- `node-jinmao/utils/htmlppt.js`
- `node-jinmao/service/course_pipeline.js`
- `node-jinmao/package.json`
- `FILE.md` / `node-jinmao/FILE.md`

## 2026-07-24 — [后端] v1.16.0 / [前端] v0.9.0 — 新增课程记忆功能

### 新增
- **课程记忆（学习进度自动保存与恢复）**
  - 用户浏览教材时自动保存学习进度（当前课程、章节、页码）到数据库
  - 下次进入同一教材时自动恢复到上次离开的位置（章节 + 页码）
  - 保存时机：翻页后 2 秒防抖保存 + 切换章节后保存 + 页面卸载时立即保存
  - 进度以 `(用户, 课程, 章节)` 三维保存，同一组合仅保留最新一条记录

### 后端变更
- **数据库**：Prisma Schema 新增 `UserStudyRecord` 模型（联合唯一约束 `user_id + course_id + chapter_id`），新增迁移文件 `20260724_add_user_study_record`
- **Repository 层**：新建 `node-jinmao/utils/repo/progress_repo.js`，封装 `upsertProgress`（保存/更新进度）、`getProgress`（获取单课程进度）、`getAllProgress`（获取所有课程进度）
- **API 路由层**：新建 `node-jinmao/API/progress.js`，新增 `PUT /api/v1/progress`（保存进度）和 `GET /api/v1/progress`（获取进度，可选 courseId 参数）
- **路由注册**：`app.js` 注册 progress 路由

### 前端变更
- **API 封装**：新建 `WEB/src/api/progress.js`，封装 `saveProgress`、`getProgress`、`getAllProgress` 三个接口
- **学习页**：`WEB/src/pages/study/script.js`
  - 新增 `saveProgressDebounced()` 防抖保存函数
  - 翻页（goToPage）后自动保存进度
  - 切换章节（switchChapter）后自动保存进度
  - 页面卸载（onUnmounted）时立即保存进度
  - 课程加载（loadCourseData）完成后，查询历史进度并自动恢复

### 涉及文件
- `node-jinmao/prisma/schema.prisma`：新增 UserStudyRecord 模型及反向关系
- `node-jinmao/prisma/migrations/20260724_add_user_study_record/migration.sql`：**新建**
- `node-jinmao/utils/repo/progress_repo.js`：**新建**，学习进度 Repository
- `node-jinmao/API/progress.js`：**新建**，学习进度 API 路由
- `node-jinmao/app.js`：注册 progress 路由
- `WEB/src/api/progress.js`：**新建**，前端进度 API 封装
- `WEB/src/pages/study/script.js`：新增防抖保存 + 进度恢复逻辑
- `node-jinmao/package.json`：版本号 1.15.1 → 1.16.0
- `WEB/package.json`：版本号 0.8.1 → 0.9.0

## 2026-07-24 — [后端] v1.15.1 / [前端] v0.8.1 — 修复口播稿提示词 & 教材状态展示

### 修复
- **bug1：口播稿提示词优化**
  - 新建 `elaboration_prompt_first.txt`：第一页（首页）专用扩写提示词，强调简练概述、字数控制更严格（1.5倍系数）、允许开场问候语
  - 修改 `elaboration_prompt.txt`：新增过渡语要求（第5条），禁止直接说"第X页"，改为"接下来我们看..."等自然衔接
  - 修改 `outline_prompt.txt`：更新示例中的过渡语从"接着来看第二页"→"接下来我们看第二页"、"第三页"→"接下来我们关注..."
  - `elaboration.js` 新增 `slideIndex` 参数：第一页加载 `elaboration_prompt_first` 提示词，其他页加载 `elaboration_prompt` 提示词
  - `course_pipeline.js` Phase3：扩写时传递页码索引 `i`，第一页字数系数从 3x 降为 1.5x
- **bug2：教材状态标签修正**
  - `CourseCard.vue`：`partial_completed` 状态标签从"部分完成"改为"已完成"，标签颜色从橙色 warning 改为绿色 success
  - `course_pipeline.js` Phase2：检测 extractLines 206 返回码记录 `isLastChapter` 标记
  - `course_pipeline.js` Phase6：在清理进度数据前读取 isLastChapter，若为最后一章则强制设置状态为 `completed`

### 涉及文件
- `node-jinmao/config/elaboration_prompt_first.txt`：**新建**，第一页专用扩写提示词
- `node-jinmao/config/elaboration_prompt.txt`：更新过渡语要求，新增第5条
- `node-jinmao/config/outline_prompt.txt`：更新示例和约束文字
- `node-jinmao/config/prompt.json`：新增 `elaboration_prompt_first` 配置
- `node-jinmao/utils/elaboration.js`：函数签名增加 slideIndex 参数，根据页码选择提示词
- `node-jinmao/service/course_pipeline.js`：Phase2 记录 isLastChapter、Phase3 传递 slideIndex + 字数系数调整、Phase6 读取 isLastChapter 并优先判断
- `WEB/src/components/CourseCard.vue`：partial_completed 标签改为"已完成"、颜色改为 success
- `node-jinmao/package.json`：版本号 1.15.0 → 1.15.1
- `WEB/package.json`：版本号 0.8.0 → 0.8.1

## 2026-07-24 — [后端] v1.15.0 / [前端] v0.8.0 — 前端实时生成进度提示功能

### 新增
- **进度数据存储**：Course 表新增 `pipeline_progress` VARCHAR(1000) 字段，以 JSON 格式存储流水线阶段进度（outlineStartTime / totalSlides / elaborationCompleted / filesCompleted）
- **进度埋点**：`course_pipeline.js` 在各阶段关键位置写入进度数据——大纲生成启动时间、总页数、扩写完成数、文件生成计数
- **进度查询 API**：新增 `GET /api/v1/book/:book_id/progress` 端点，返回结构化进度数据（phase / outlineProgress / elaborationProgress / filesProgress / isTerminal）
- **原子递增加载函数**：`book_repo.js` 新增 `incrementPipelineProgress()`，使用 MySQL JSON_SET + JSON_EXTRACT 在数据库层面做原子递增加载，避免 PPT/TTS 并发计数时的读写竞态
- **前端进度展示**：CourseCard 组件根据当前阶段显示不同内容——大纲生成显示百分比进度条（15分钟看门狗）、口播稿扩写显示 X/Y 计数、PPT/TTS/SRT 生成显示已完成数/总数
- **前端进度轮询**：首页轮询逻辑增强，为每个处理中的课程调用 `getCourseProgress()` 获取详细进度，通过 `courseProgressMap` 传递给 CourseCard

### 涉及文件
- `node-jinmao/prisma/schema.prisma`：Course 模型新增 `pipelineProgress` 字段
- `node-jinmao/utils/repo/book_repo.js`：新增 `updatePipelineProgress()` 和 `incrementPipelineProgress()` 函数
- `node-jinmao/service/course_pipeline.js`：Phase3（大纲生成/扩写）、Phase4（PPT）、Phase5（TTS）、Phase6（校验）添加进度埋点
- `node-jinmao/API/POSTbook.js`：新增 `GET /book/:book_id/progress` 端点
- `WEB/src/api/books.js`：新增 `getCourseProgress()` 函数
- `WEB/src/components/CourseCard.vue`：新增 progress prop、progressLabel/progressBarWidth/progressCountText 计算属性、phase 差异化进度 UI
- `WEB/src/pages/home/script.js`：轮询逻辑增强，导入 getCourseProgress，新增 courseProgressMap
- `WEB/src/pages/home/index.vue`：CourseCard 传递 progress prop
- `node-jinmao/package.json`：版本号 1.14.1 → 1.15.0
- `WEB/package.json`：版本号 0.7.0 → 0.8.0

## 2026-07-23 — [后端] v1.11.0 — Phase3 口播稿扩写并行化 + 模型从 pro 切换到 flash

### 修改
- **口播稿扩写模型切换**：`utils/elaboration.js` 将 DeepSeek 模型从 `deepseek-v4-pro`（`DEEPSEEK_API_BIG`）切换到 `deepseek-v4-flash`（`DEEPSEEK_API_SMALL`），API 超时从 15 分钟降为 5 分钟，因口播稿扩写属于文本润色类任务，不需要 pro 级别的深度推理能力
- **Phase3 扩写并行化**：`service/course_pipeline.js` 将口播稿扩写从串行 `for` + `await` 改为 `p-queue` 并发队列（并发上限 5），与 Phase4（PPT 生成）并发模式一致，预计加速比 10~15 倍

### 涉及文件
- `node-jinmao/utils/elaboration.js`：4 处 `DEEPSEEK_API_BIG`/`BIG_MODEL` 替换为 `DEEPSEEK_API_SMALL`/`SMALL_MODEL`，thinking 模式保留
- `node-jinmao/service/course_pipeline.js`：Phase3 3.3 扩写部分从串行循环改为 `p-queue` 并发队列（concurrency: 5）

## 2026-07-23 — [后端] v1.10.5 — 修复文生图轮询超时过短（3分钟 → 15分钟）

### 修复
- **文生图轮询超时延长**：将 Grsai API 异步任务轮询超时从 3 分钟（60次×3秒）延长到 15 分钟（180次×5秒），解决封面生成因 API 处理时间超过 3 分钟而频繁 504 超时的问题
- **轮询间隔调整**：从 3 秒调整为 5 秒，减轻对 API 服务器的请求压力

### 涉及文件
- `node-jinmao/config/grsai_config.json`：`GRSAI_POLL_INTERVAL_MS` 3000→5000，`GRSAI_POLL_MAX_RETRIES` 60→180

## 2026-07-23 — [前端] v0.4.0 — NERV 设计稿合并：三页全面重构 + 新增课程学习页

### 新增
- **课程学习页** (`WEB/src/pages/study/`)：三栏可拖动布局（目录/PPT播放区/AI助教），B站风格播放控件 hover 显示，PPT 全屏切换，AI 对话模拟，口播稿面板
- **侧边栏拖动 composable** (`WEB/src/composables/useResize.js`)：封装 mousedown/mousemove/mouseup 拖动逻辑，支持左右侧边栏独立拖动
- **App.vue 三页导航**：通过 `provide/inject` 实现登录页、首页、课程学习页三态切换，无需 Vue Router
- **首页侧边栏 NERV 状态轨**：导航激活项左侧 3px 蓝色竖线指示器，积分进度条，会员信息区域

### 修改
- **设计 Token 升级** (`tokens.css`)：从 gray 系升级为 NERV 蓝色战术色彩体系，新增 `--color-rail`/`--color-card`/`--color-grid` 等变量
- **全局样式扩展** (`index.css`)：新增 `.nerv-brackets`/`.nerv-grid-bg`/`.nerv-rail` 等 NERV 装饰 CSS 类
- **登录页模板重构** (`pages/login/index.vue`)：NERV 双栏布局，战术网格背景+CRT扫描线，角括号装饰，品牌展示+金句
- **登录页脚本** (`pages/login/script.js`)：集成主题切换逻辑 (useTheme)，isDark/toggleTheme 暴露给模板
- **首页模板重构** (`pages/home/index.vue`)：NERV 蓝色战术风格，标题角括号装饰，统计卡片蓝色状态轨，CSS token 驱动的背景色
- **HomeTopbar 重构** (`HomeTopbar.vue`)：NERV 风蓝色标签+下划线，透明搜索框，圆形蓝色头像按钮
- **HomeSidebar 重构** (`HomeSidebar.vue`)：NERV 品牌区，蓝色状态轨导航，积分进度条
- **App.vue**：三页条件渲染 + provide('navigate') 跨组件导航
- **首页导航** (`script.js`)：`onOpenCourse` 改为跳转学习页（通过 inject navigate）

### 设计规范合规
- 圆角统一 `rounded-[10px]`（规则2）
- 文字 `text-black` / `dark:text-white`（规则1）
- 过渡 `duration-500`（规则3）
- Element Plus 组件优先（规则6）
- 暗黑双轨适配（规则7）
- 主色 blue-500 / blue-400（规则9）

## 2026-07-20 — [后端] v1.10.4 — 修复 DeepSeek API 保活超时（120s → 15min）+ 补全心跳日志
