# 数据库结构设计

> 数据类型基于 MySQL 8.0 规划，引擎统一使用 InnoDB，字符集 utf8mb4。

## 用户表 User
- id:          BIGINT UNSIGNED 主键，自增
- username:    VARCHAR(50)  用户名，非唯一（使用邮箱或手机号登录）
- password:    VARCHAR(255) 密码，bcrypt/argon2 加密存储
- nickname:    VARCHAR(50)  昵称，非唯一
- email:       VARCHAR(100) 邮箱，唯一
- phone:       VARCHAR(20)  手机号，唯一，可选(DEFAULT NULL)
- role:        ENUM('admin','user') 角色，默认 'user'
- create_time: DATETIME 创建时间，DEFAULT CURRENT_TIMESTAMP
- update_time: DATETIME 更新时间，DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
- is_banned:   TINYINT(1) 是否被禁用，DEFAULT 0（1=已禁用）
- ban_reason:  VARCHAR(255) 禁用原因，可选(DEFAULT NULL)
- is_deleted:  TINYINT(1) 是否删除，DEFAULT 0（1=已删除）

## 用户学习记录表 UserStudyRecord
- id:             BIGINT UNSIGNED 主键，自增
- user_id:        BIGINT UNSIGNED 用户ID，外键引用 User 表
- course_id:      BIGINT UNSIGNED 课程ID，外键引用 Course 表
- chapter_id:     BIGINT UNSIGNED 章节ID，外键引用 Chapter 表
- progress:       INT UNSIGNED 用户学到了多少页ppt
- study_duration: INT UNSIGNED 学习时长，单位秒
- create_time:    DATETIME 创建时间，DEFAULT CURRENT_TIMESTAMP
- update_time:    DATETIME 更新时间，DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
- is_deleted:     TINYINT(1) 是否删除，DEFAULT 0（1=已删除）
- 联合唯一约束: UNIQUE(user_id, course_id, chapter_id)

## 课程表 Course
- id:                BIGINT UNSIGNED 主键，自增
- user_id:           BIGINT UNSIGNED 用户ID，外键引用 User 表（暂不支持多用户共享）
- name:              VARCHAR(100) 课程名称
- description:       TEXT 课程描述
- textbook_filename: VARCHAR(255) 教材原文件名
- textbook_path:     VARCHAR(500) 教材存储路径
- create_time:       DATETIME 创建时间，DEFAULT CURRENT_TIMESTAMP
- update_time:       DATETIME 更新时间，DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
- is_deleted:        TINYINT(1) 是否删除，DEFAULT 0（1=已删除）

## 章节表 Chapter
- id:          BIGINT UNSIGNED 主键，自增
- course_id:   BIGINT UNSIGNED 课程ID，外键引用 Course 表
- name:        VARCHAR(100) 章节名称
- description: TEXT 章节描述
- chapter_root: VARCHAR(500) 章节根目录路径
- startline:   INT UNSIGNED 开始行号
- endline:     INT UNSIGNED 结束行号
- total_pages: INT UNSIGNED 本章节 PPT 总页数
- create_time: DATETIME 创建时间，DEFAULT CURRENT_TIMESTAMP
- update_time: DATETIME 更新时间，DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
- is_deleted:  TINYINT(1) 是否删除，DEFAULT 0（1=已删除）

## 题库表 Bank
> 题库本身不持有用户信息，所有权和共享权限通过「题库用户关联表」管理。
> course_id 可选：课程自动生成的题库绑定课程，独立导入的题库不绑定。
- id:          BIGINT UNSIGNED 主键，自增
- course_id:   BIGINT UNSIGNED 课程ID，外键引用 Course 表，可选(DEFAULT NULL)
- type:        ENUM('course_generated','imported') 题库类型，DEFAULT 'imported'
- name:        VARCHAR(100) 题库名称
- description: TEXT 题库描述
- create_time: DATETIME 创建时间，DEFAULT CURRENT_TIMESTAMP
- update_time: DATETIME 更新时间，DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
- is_deleted:  TINYINT(1) 是否删除，DEFAULT 0（1=已删除）

## 题库用户关联表 BankUserAssociation
> 管理题库的所有权和共享权限。创建题库时插入一行 permission='owner' 的记录。
> 共享功能上线后，直接插入 permission='editor'/'viewer' 的记录即可。
- id:          BIGINT UNSIGNED 主键，自增
- bank_id:     BIGINT UNSIGNED 题库ID，外键引用 题库表
- user_id:     BIGINT UNSIGNED 用户ID，外键引用 User 表
- permission:  ENUM('owner','editor','viewer') 权限，DEFAULT 'viewer'
- create_time: DATETIME 创建时间，DEFAULT CURRENT_TIMESTAMP
- is_deleted:  TINYINT(1) 是否删除，DEFAULT 0（1=已删除）
- 联合唯一约束: UNIQUE(bank_id, user_id)

## 题目表 Question
> options 和 answer 统一用 TEXT 存储，格式为 JSON 字符串，不同题型结构不同（见下表）。
> sort_order 用于题目在题库中的展示排序，值越小越靠前。
- id:          BIGINT UNSIGNED 主键，自增
- bank_id:     BIGINT UNSIGNED 题库ID，外键引用 题库表
- type:        ENUM('single_choice','multiple_choice','true_false','fill_blank','short_answer') 题目类型
- content:     TEXT 题目内容（题干）
- options:     TEXT 选项，JSON 格式，选择/判断题有值，其他题型为 NULL
- answer:      TEXT 正确答案，JSON 格式（详见下表）
- analysis:    TEXT 题目解析/解答思路，可选(DEFAULT NULL)
- difficulty:  TINYINT UNSIGNED 难度等级 1-5，DEFAULT 1
- tags:        VARCHAR(500) 知识点标签，逗号分隔，可选(DEFAULT NULL)
- sort_order:  INT UNSIGNED 排序号，DEFAULT 0
- create_time: DATETIME 创建时间，DEFAULT CURRENT_TIMESTAMP
- update_time: DATETIME 更新时间，DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
- is_deleted:  TINYINT(1) 是否删除，DEFAULT 0（1=已删除）

### 各题型 options / answer JSON 格式示例
| 题型 | options | answer |
|------|---------|--------|
| single_choice（单选题） | `["A. 选项1","B. 选项2","C. 选项3","D. 选项4"]` | `"A"` |
| multiple_choice（多选题） | `["A. 选项1","B. 选项2","C. 选项3","D. 选项4"]` | `["A","C"]` |
| true_false（判断题） | `["正确","错误"]` | `"正确"` |
| fill_blank（填空题） | NULL | `["答案1","答案2"]`（按空位顺序） |
| short_answer（简答题） | NULL | `{"keywords":["关键词1","关键词2"],"reference":"参考答案文本"}` |
- 联合唯一约束: UNIQUE(bank_id, type, content)